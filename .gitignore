# Session artifacts — never commit to main
.current-session.json
*.log

# Secrets
hcloud.json
resp05.json
state/.gh_token

# Agent-generated state — lives on agent-state branch only
state/handoffs/

# Nested grotap-platform clone (separate private repo — never track here)
platform/

# OS
.DS_Store
Thumbs.db

# python bytecode
__pycache__/
# nested repos and platform worktrees living beside this checkout
grotap-landing/
public-reports/
reports/
platform-worktrees/
platform-scale/
lt-run/
wt-*/
# owner document archive and local tool caches
DocsforClaude/
androidtools/
apk-out/
.browser-*/
