# agents/GLOBAL.md — Load order: GLOBAL.md → SERVERS.md → MODULE.md → ROLE.md → handoff.md
# Max 200 lines enforced by session-init. Dated incident ledgers: agents/LESSONS-ARCHIVE.md (not auto-loaded).

## Platform
grotap — multi-tenant AI-powered SaaS. Every feature = discrete app. Tenants subscribe to apps.
Code: `platform/` | Docs: `docs/` | Tasks: `agents/tasks/` | Fleet scripts: platform repo `agents/*.sh`

## Stack
Frontend React+Vercel `platform/frontend/` · Backend FastAPI+Railway `platform/backend/` · Auth WorkOS JWT · DB Neon Postgres (pooled shards + FORCE RLS; dedicated project = premium; control `green-rice-76766370`) · Jobs INNGEST + Agents LangGraph/LangSmith (TS only) `platform/agent-worker/`
· Storage Cloudflare R2 · Billing Stripe metering (all 3rd-party via `app/providers/*`) · Mobile Expo `platform/mobile/` · Cobrowse `lib/cobrowse.ts` · Secret scan GitGuardian MCP

## ⛔ Absolute Rules — All Agents, No Exceptions
| # | Rule |
|---|---|
| 1 | **DOPPLER ONLY** — No `.env` in CI, no GitHub secrets (except `DOPPLER_SERVICE_TOKEN`). Local: `doppler run -- <cmd>`. Update secrets in Doppler (`grotap` prd/dev). NEVER tell a human to update GitHub secrets. **NEVER put secret VALUES in chat/prompts/task files/logs/commits.** Humans supply secrets via the Doppler dashboard or a terminal OUTSIDE any AI session. |
| 2 | **NO PYTHON FOR AGENTS** — TypeScript/JS only. Python = FastAPI backend only. |
| 3 | **NO DIRECT 3RD-PARTY CALLS** — All SDK calls via `app/providers/` wrappers. |
| 4 | **NO CROSS-TENANT DATA** — Every DB query scoped to the tenant. No cross-tenant reads. |
| 5 | **TENANT ISOLATION VIA RLS** — Default placement is POOLED: tenants share a Neon shard, isolated by FORCE RLS keyed on `current_setting('app.current_tenant_id')::uuid` (set per-connection by `tenant_db.py`). Dedicated Neon project = premium/override path. Never bypass RLS or weaken a policy. |
| 6 | **NO SKIPPING COMPLIANCE** — GitGuardian MCP + compliance node before every deploy. |
| 7 | **NO MERGE WITHOUT 4-REVIEWER SIGN-OFF** — Build Validator + Logic + Security + Perf = all PASS. From platform repo root: `./agents/review-pipeline.sh <branch>` then `./agents/collect-reviews.sh --wait <branch>`. ANY FAIL = branch blocked. |
| 8 | **AppShell + COBROWSE MANDATORY** — All apps render `AppShell`. Never remove Cobrowse components. SDK only via `lib/cobrowse.ts`. |

## ⚠ Common FAIL Causes — Check Before Committing

### SQL & migrations
- Control-plane DDL goes ONLY in `backend/db/migrations/control_plane/vNNN_*.sql` — idempotent, one file per txn, FK children after parents. Never inline DDL in `backend/app` (CI guard blocks it); never the retired `backend/migrations/` path. App schemas: repo-root `migrations/apps/<slug>/vNNN_*.sql` AND copy each file to `ingestion-worker/migrations/apps/<slug>/` (its Docker image only ships its own dir; a registered-but-absent file = `schema_status='failed'` on first subscription).
- Pick the next FREE `vNNN` — grep the dir AND `git ls-tree origin/master` AND peer case branches (parallel cases both took v037). Two branches must never both CREATE a table or claim the same number — declare the dependency and let ONE branch own the table's canonical schema; reviewer confirms distinct versions on any ≥2-migration batch.
- Grep the real migration for schema-qualified table + column names before writing SQL — never invent columns. Need a new column? Ship `ADD COLUMN IF NOT EXISTS` + backfill FIRST. Consuming a sibling case's schema? grep ITS migration for the real names.
- A migration recorded in prod's `schema_migrations` is FROZEN — every byte (a one-word seed tweak trips the startup checksum guard and blocks ALL deploys; f661e2f0). NEVER amend an applied migration file — the runner skips recorded filenames, so amended DDL silently never runs; cut a new vNNN, mirror it in BOTH dirs, keep manifest updates ADDITIVE. Any script executing SQL as "proof" must `assert_not_prod(dsn)` (`backend/scripts/_lib_guard.py`).
- asyncpg: JSONB binds = `json.dumps(value)` + `$N::jsonb` (no codec); `->>` text vs `->` jsonb. Never `.get()` on Records — `dict(r)` first (recurred 3BA7FB). An error POISONS the open transaction — `to_regclass()` checks or savepoints for optional tables; NEVER swallow schema errors (`except: log-and-continue` hides missing columns).
- `pipeline_cases` tenant column is `org_id`; `tenants.tenant_id` UUID but `tenant_users.tenant_id` TEXT (`str()` first); WorkOS ids (`org_01…`/`user_01…`) are TEXT — never UUID columns or `uuid.UUID()`. RLS policies keyed EXACTLY on `current_setting('app.current_tenant_id')::uuid` — any other GUC name silently returns zero rows. No tenant-specific seed rows in shared app migrations (they run on EVERY tenant).
- UNIQUE with COALESCE is invalid — `CREATE UNIQUE INDEX`; a new unique/invariant index ships WITH its dedupe migration; enforce invariants in every write path, not just backfill. No `SELECT SUM(...) FOR UPDATE` (invalid) — lock the parent row or advisory lock. Dynamic UPDATE SET builders: never hardcode a column AND loop it.
- Schema rename on an existing tenant: `ALTER SCHEMA … RENAME` (guard `to_regnamespace`) + re-GRANT — never re-CREATE; rotate role creds via `ALTER ROLE … WITH PASSWORD`, never `DROP ROLE`. `GENERATED ALWAYS AS` needs IMMUTABLE expressions (42P17 otherwise) — plain column + app sync. A migration remapping live values updates EVERY consumer query in the SAME change — grep first.

### Auth & security
- `request.state.organization_id` (NOT `tenant_id` → AttributeError → 500). Real WorkOS access tokens carry NO `email` claim (test JWTs do) — middleware backfills it, but probe new email-gated logic with a claim-less JWT.
- Status fields need explicit allowlist validation. UPDATEs carry the same scope as their SELECT (`session_id`, org). A DELETE feature covers EVERY delete path with the same org/tenant scope — grep every `DELETE FROM <table>` and router delete before done.
- PUBLIC_PATHS covers ALL non-JWT auth (OAuth callbacks, webhooks, shared-secret internal RPC) — TenantAuthMiddleware 401s them and callers silently mis-default. Webhooks: verify signature, dedup on event id, non-2xx on transient failure, survive out-of-order/duplicate delivery.
- Security gates FAIL CLOSED in prd (missing key = block + log critical, never silently pass) — and a fail-closed consumer gate requires closing EVERY producer path of that state in the same change (provision-or-cleanup, not warn-and-continue). A key/token FORMAT validator must match what the minting path actually issues — grep the mint endpoint before writing the regex (8BC416).
- `require_company_role` checks the caller's role in their OWN tenant only — money-movement and cross-brand endpoints must verify ownership of the specific resource or gate grotap-admin.
- A connect/OAuth flow requests the scopes its CONSUMER case needs — trace the whole feature across split cases. Rebasing onto a hardened master must preserve auth guards master added; never re-add a provider wrapper that already exists.
- NEVER pass user-submitted strings to readFile/exec/path APIs — resolve against an allowlisted root, reject absolute paths and `..`, cap read size (a raw `readFile(source_doc)` would have exfiltrated orchestrator secrets into agent context). This includes FastAPI query params flowing into `Path(...)` (print-cloud `download_agent(version=…)`) — allowlist-regex the value first.

### Wiring & contracts
- Importing/creating ≠ wiring: a new router is dead until `include_router` lands in main.py; an imported helper needs a real call site; every UI action needs an endpoint that EXISTS (grep the router — extend it if the task is "UI-only" and it's missing). Verify referenced modules/pages exist before wiring routes; check master before re-building landed work.
- Frontend + backend built in separate runs: PROVE the wire contract — hit the real endpoint, diff field names/shapes/enum casing against the TS types; map DB rows→UI in ONE api layer. A frontend-only branch consuming a NEW endpoint silently 404s if the backend isn't on master — grep master backend for the route and land the backend sibling FIRST; a case marked `done` may never have merged (`git branch -r --contains`).
- 3rd-party integrations: prove WE emit the key you filter by (current format, current master); probe the REAL artifact's export shape (2-min headless `typeof`/constructability check) before coding against it — never trust docs-memory or in-repo "precedent" unexercised, and surface `err.message` in SDK-init catches (CobrowseViewerPopout). A contract SPIKE must include ONE live end-to-end proof that every remote call it depends on SETTLES (resolve or reject) — `ctx.setTool()` was type-correct yet never acked (0/24); cosmetic calls are best-effort with a deadline, never a hard gate (61c63fc0).
- A JSON contract quoted verbatim in the task prompt is LAW: emit EVERY contract key with the EXACT names (additive extras fine; renames/omissions are defects — `key_facts` vs contract `notable_facts` left the Leads dossier viewer half-empty, CASE-20260714-9F95A6). Reviewers: diff the emitted keys against the prompt's contract before approving.
- Config VALUES quoted in the task (URLs, repo slugs, schema shapes mirroring a verified environment) are LAW the same way — copy them byte-exact, never reconstruct from memory; and a "verify" step must assert the VALUES (grep exact strings, parse + type-check shapes), not just syntax/`json.load` — syntax-only checks passed three hallucinated values into master (JSP-1, CASE-20260715-2F8C3D).
- "Most recent N": verify ORDER BY delivers recency (sort AND limit window). Unbounded endpoints paginate BEFORE ship (limit/offset + stable tie-breaker); grids get server-side filters; escape ILIKE metachars (`% _ \`).
- Post-query row injection (companion tiles, synthetic defaults) must re-apply EVERY filter the base query applied — hidden-apps set, visibility, tenant scope — or user prefs are silently undone on every response (Scan M pairing re-added a hidden companion, CASE-20260710-1D7D90).
- App slugs: verify against the `apps` table, copy slug facts from the task VERBATIM. `pipeline` (📋) ≠ `agent-pipeline` (⚡); `document-upload` displays as "AI Knowledge Base".
- Anthropic Messages API: any request whose messages contain `tool_use`/`tool_result` blocks MUST still pass the `tools` param — a "final answer" fallback that strips tools 400s on tool-bearing transcripts; use `tool_choice={"type":"none"}` instead (found in CASE-20260706-8F1C60).

### Frontend
- Global `body` CSS is DARK (`#0f0f0f`/white) — every light surface sets its OWN text `color` (grids `#374151`); e2e asserts VISIBILITY (computed-style luminance), not DOM presence.
- New event domain → NEW BroadcastChannel (existing listeners have bare `onmessage` refetch handlers, no type filter).
- `window.open(url, '_blank', 'noopener…')` returns NULL even when the popup opens — an `if (!popup)` fallback then ALSO navigates the current tab (double-open, opener lost). Internal same-origin popups: plain `window.open(url, '_blank')`.
- Optimistic-UI reconcile (dropping a local temp message once its server copy lands) must match ONLY rows persisted by THIS turn — never content-match against pre-existing history: a message textually identical to an older one + a failure before the server insert = the user's pending message silently deleted. Row-REUSE flows (re-send of a failed turn) may count an existing row as this turn's copy only with positive evidence of reuse (e.g. no OLD assistant reply after it in the refetch), never just "last user row matches" (F5F1C1 gate REJECT 2026-07-08).

### State machines & jobs
- A failure path releases everything the happy path claimed (status, slot, lock, row) in the SAME transaction — "nobody un-claims it" = a leak that starves the pipeline. Auto-retry needs backoff or a circuit breaker; infra-caused failures are `failed_infra`, not `failed` (don't burn case strikes).
- Background loops register in `background_loops.py::start_leader_locked_loops()` — never raw `asyncio.create_task` in lifespan (leader lock prevents web+worker double-execution).
- Never re-enter LangGraph threads paused at the human gate: detect with `snapshot.next?.includes('human_gate')`, NEVER `next.length>0`; re-invoking a live thread re-executes finished work.
- Billing idempotency keys are deterministic (derive from stable ids like `invoice-{brand_id}-{period}`) — never `uuid4()` per call.

### Build & ship
- `py_compile` misses import-time crashes — boot-test `python -c "import app.main"` before any backend push. FastAPI 0.115: bodyless status codes (204) need `response_model=None` (a `-> None` annotation asserts at route registration).
- Actually RUN `tsc --noEmit` + `py_compile` — "it compiles" is a command, not a claim (recurred TWICE in one day, D7A410/8DC100: a frontend deliverable is done only when the UI renders AND calls its endpoints). Unused TS imports = `noUnusedLocals` hard errors. Grep your diff for conflict markers (`^<<<<<<<`). `| head -n 4`, never `| head -4`.
- A "re-land with integrated fixes" means fixes actually INTEGRATED — byte-identical = auto re-reject (49C9DD). Any rebuild/re-push is a FRESH landing: rebase onto CURRENT master right before the final push, DROP hunks master now owns (357E52), and PRESERVE master-side surface — `git grep` every symbol/endpoint you deleted or renamed for master consumers (2533BA). Before finalizing, re-diff against CURRENT master: master already landed/rewrote the region → adapt or report-and-stop, never commit a stale copy of a hot file. Verify APIs exist in the PINNED lib version; Dockerfiles that `npm run build` need devDeps (`npm ci` then prune).
- Rename/refactor: `git grep <old>` returns zero (minus shims) AND app-catalog seeds updated (`seed_apps.sql`, `seed_brands_apps.sql`, `control_plane.py`). Stay in scope: no `npm install`/lockfile edits/dep bumps unless the task IS an upgrade.
- `railway up` is not done until `railway status --json` shows latestDeployment SUCCESS (failed healthcheck leaves the OLD image live); use `RAILWAY_API_TOKEN`. No `--build-arg` flag — inject Dockerfile ARGs as service variables; verify new CLI flags against `--help`. Railway env vars are STATIC — after any Doppler rotation run `platform/scripts/railway_secret_audit.py` (runbook: `docs/SECRET_ROTATION_RUNBOOK.md`).
- Frontend `npm run build` needs ~3 GB heap since AG Grid Enterprise (2026-07-06): vite OOMs at node's default ~2 GB during "computing gzip size" on 4 GB fleet boxes — tsc passes, all modules transform, THEN fatal OOM, so verify fails on good code. Keep `--max-old-space-size=3072` in frontend package.json `build` + `build.reportCompressedSize:false` in vite.config.ts; never revert to bare `vite build`. Verify error text is NOT persisted to any DB table — reproduce in the preserved `/home/agent/worktrees/CASE-*` on the failing box.
- `ON CONFLICT (col)` can NOT use a PARTIAL unique index unless the predicate is repeated verbatim: `ON CONFLICT (col) WHERE <index predicate> DO UPDATE`. Without it every execute raises at runtime — and mock tests that assert the SQL string contains "ON CONFLICT" won't catch it. Check `pg_indexes` for a WHERE clause before writing the upsert.
- CI installs `-r requirements.txt -r requirements-dev.txt`. Tests import the production code they verify (no reimplemented formulas/tautologies); mock fixture types match REAL return types; daemon/loop tests mock `sleep`, cap iterations, run bounded (`timeout 300`); per-file isolation = pytest-forked as a CI-only flag (never in `pytest.ini` — `os.fork` breaks Windows); "unconfigured provider" tests monkeypatch env EMPTY + mock transport or ambient Doppler creds send a real SMS.
- A connectivity feature (WebRTC, WebSocket, live socket) is NOT proven by an e2e that mocks its transport — a mocked `RTCPeerConnection` driven to "connected" verifies UI chrome only. 964525 shipped call controls whose offer was never SENT (no signaling path); the spec passed the gate green. Gate rule: connect-to-something features need ONE unmocked proof the two ends actually exchange data, or the review states the gap explicitly (C0TURN).
- Verification/gate tasks (no code expected) report success without commits — say so in the task file. Never relax a mandatory safeguard off ONE anecdotal survival — `git grep` master to confirm the fixes it waits on actually landed (3B0DEB).
- Reviewer conduct rules (judge the REAL branch not the diff artifact; verify deliverable SUBSTANCE against scope; machine-visible BLOCK first) live in platform repo `agents/reviews/COMMON.md` — auto-prepended to every reviewer prompt by review-pipeline.sh; edit THERE, not here.
- Task text marked VERBATIM is copied byte-for-byte, never regenerated from memory — and reviewers DIFF the deliverable against spec-supplied text: CASE-20260709-152165 shipped invented triage recipes (a different failure than the documented incident) plus a symlink "exception" contradicting the spec's own NEVER rule, and passed 4/4 reviewers. Fix: CASE-20260709-EAF6B0.
- Visibility/authz changes cover EVERY read+write path (list, my-apps, subscribe, vote, brands…) in ONE case; scope to the owning TENANT, not the creator user. One owner per hot file. Appending to a shared init hunk: use your OWN `pool.execute` block; don't re-ADD siblings' columns.
- One owner per hot FUNCTION and DISJOINT files per sibling (05572B+8584FE both rewrote `record_answer_from_hold`; 357E52 duplicated its sibling's hunks with a WEAKER variant) — check whether a sibling's branch already adds the helper (`git ls-remote` + `git show`) and wire to its interface; coalesce onto one sequential branch when in doubt. A shared NEW file must be created by exactly ONE subtask — both shipping full versions = add/add structural conflict, not union-mergeable (255A13/152345). At dedup, diff BOTH versions against the spec and keep the compliant one — spec said `secrets`, survivor used `random` (E8600B).
- **ONE-TOUCH HUMAN STEPS** — owner time is the scarcest resource. Before any HI hold that puts a human in a console: derive the COMPLETE end-state from the consumer's actual code + the verbatim error (every field, permission, scope), deliver ALL steps in ONE message, state what NOT to touch, and batch every other pending item for that console into the visit. Verify against reality, not memory.
- **REBOOT WATCHDOG — 5-MINUTE RULE (owner directive 2026-07-09)** — after ANY expected reboot/reset/restart of a server or service, poll max 5 minutes (normal boot ≈1 min). No ping/SSH at the 5-min mark → IMMEDIATELY file an HI hold (`human_holds`, priority high, one-touch recovery steps: console reset, rescue mode, credential procedure) + push-notify the owner + state the blocker in chat. Silent multi-hour ping-watch loops are FORBIDDEN as the primary response — a background watch may run only IN ADDITION to the alert (GEX44 kernel brick sat overnight because escalation came at min 45 instead of min 5).
- **SCREEN-CONTEXT DOCS (owner directive 2026-07-13)** — a branch that adds/renames a route or screen, or materially changes what a screen does (grid columns, actions, data flow), MUST update the matching `docs/screen-contexts/<app_slug>/<screen>.md` (Capabilities / Data Mechanics / Common Edge Cases) in the SAME branch — and once `scripts/sync_screen_contexts.py` exists (SCTX-C), run it so the `screen_contexts` index row follows. Enrich seeded skeletons, never delete them. These files are injected into the live AI-support agent's system prompt: a stale doc = the agent confidently wrong in front of a customer.

## Key IDs
- Control plane Neon: `green-rice-76766370`
- Grotap tenant Neon: `proud-union-74070434` / ID: `c7d02593-955c-4ff4-8117-3b2bb267f518`
- Railway project: `f9bf333c-f929-413e-a95c-7923e10b5777`

## Fleet (full roster, hardware, Hetzner accounts: agents/SERVERS.md)
- Dispatch pool = **agent-02…06** (02–05: Execute ×3 slots + reviewer roles; 06: Deploy Ops + pipeline monitoring + Execute ×2 — its crons must always run: review gate 4h, deploy/health watchdogs, dispatch reconciler, Wasabi backups).
- **Not in the pool, never dispatch:** `grotap-cobrowse-01` (5.161.189.143 — recycled old agent-01 IP; Cobrowse AI support runner) and the Lane C model engine `LLM-LOCAL-02`. agent-01/07/08 DELETED; agent-09/10/11 cancelled in Hetzner Robot (awaiting wipe).
- SSH: always `ssh agent-NN` aliases, never raw IP. Key `~/.ssh/grotap_agents`; cobrowse box `User agent`, others `User root`. Max 3 tasks/server via worktrees.
- Git auth on exec servers: `credential.helper = /home/agent/bin/git-credential-doppler` (fetches `GITHUB_TOKEN` per call). NEVER set a static token in `~/.env`; if git auth fails, check `doppler me` as the `agent` user first.
- No unbounded concurrent SSH to one host — pool, serialize, backoff (sshd MaxStartups drops stampedes).

## Dispatch — CONTINUOUS
Backend loop assigns every 3 min + completion-webhook refill; the LangGraph orchestrator (Railway)
owns run lifecycle and SSHes dispatches to the fleet. Manual one-off (from platform repo root):
```bash
bash agents/dispatch.sh <task.md> <server-ip> <session>   # manual
bash agents/dispatch-execute.sh <task.md> <session>       # auto-route (most free slots)
```
- **Dispatch fast-fail signatures** (API-limit $0/90s deaths · inode exhaustion/EMPTY branch · node_modules symlink sabotage): full triage recipes in platform repo `agents/PIPELINE_TRIAGE.md` § "Dispatch fast-fail signatures" — read them BEFORE decomposing or re-queuing a failed run. node_modules symlinks from the shared clone into a worktree remain FORBIDDEN, no exceptions.
- **"Approve breakdown:" holds are AUTOPILOT — never a human task (owner rule 2026-07-15).** The answer is always yes; the backend-worker 60s loop auto-approves P2–P4 decomposition holds with the standing answer (`pipeline_autopilot.py`, dual gates `PIPE_AUTO_BREAKDOWN` env + `pipeline_automation.auto_breakdown_enabled` — both LIVE since 2026-07-15). A breakdown hold pending >10 min = the autopilot is broken (worker deploy / env gate / org flag) — fix the loop, don't hand-answer. P0/P1 decompositions still page a human BY DESIGN.

## Agent Teams (dispatch routing — TEAM2-DISPATCH contract, owner-approved 2026-07-07)
- **team1** = existing Claude agents (agent-02…06 pool, `orchestrator-run.sh`), the default. **team2** = open-model agents (aider via OpenRouter, default qwen-2.5-coder-32b + escalation ladder, `agents/team-run.sh`, pool agent-20/21; inactive until provisioned in platform repo `agents/config.sh`).
- Routing: explicit `case_data.team` > `DEFAULT_TEAM` env (default team1); team registry in platform `agents/config.sh`. Cutover knob = `DEFAULT_TEAM=team2`; rollout: provision agent-20/21 → config.sh pool → a few P3 cases via `case_data.team=team2` → evaluate → widen. Daily cap `TEAM2_DAILY_CAP` (default 10). Disable: `DEFAULT_TEAM=team1`/unset + no `case_data.team` = byte-identical pre-teams dispatch.
- **Model availability is per-EGRESS-REGION — verify from the executor box, never from local/bench** (2026-07-12 team3 kickoff: `x-ai/grok-4.5` benched fine from US egress but xAI 403s "not available in your region" from FSN1/EU, so team3's primary rung could never succeed on its own agent-30/31 boxes while `grok-4.20-multi-agent` worked; fix CASE-20260712-05BF08). Before registering a team model or routing lane: `curl` the model once from EACH pool box (`ssh <box> ... doppler run -- curl openrouter.../chat/completions`), and re-verify when a pool changes DC/region.
- Fallback: a team2 run that exhausts its ladder emits `status=failed_open_model` → dispatcher re-queues the SAME task to team1, tagged `meta.team_fallback=true` in dispatch_log.
- Coding Pilot (`CODING_PILOT=1`, default OFF, owner approval required) = back-compat alias for team2 routing, `simple` tasks only, never P0/P1; full parameters: platform repo `agents/GLOBAL.md` + agents/LESSONS-ARCHIVE.md.

## Code Review
`/codex:review` before every commit (mandatory; separate from Rule 7 pipeline), then the Rule 7 review pipeline. ANY reviewer FAIL = branch blocked. No exceptions.
The moment a reviewer/gate decides BLOCK, make it machine-visible FIRST — flip the dispatch row to `rejected` and/or park the case BEFORE writing any report; a prose-only block gets auto-merged by the approve cron (EA3E8D).

## Deployment
Frontend (Vercel) via CI on push to `master` (paths `frontend/**`). Backend Railway GitHub auto-deploy WORKS but GATES ON GREEN CI (RLWAY1 root cause, confirmed 2026-07-12: 3 days of "silent skips" were a red Backend CI, not a webhook fault) — after a backend push, check the Backend CI run first; if CI is red, fix the tests (never bypass), then verify `/health` git_sha updates. Manual fallback only if CI is green and no build appears: `doppler run -- railway up --service grotap-backend --detach` from `backend/`. Orchestrator: GitHub Actions `deploy-railway.yml` runs `railway up` on master pushes touching `orchestrator/**` (tip-commit detection only until CASE-20260705-C29667 lands — multi-commit pushes may silently skip; verify, fall back to manual `railway up`). Agents on Hetzner: push branch → request merge+deploy from coordinator.
- **Orchestrator redeploy kills in-flight fleet runs (2026-07-07):** a redeploy severs every live SSH run (silent deaths, stale dispatch rows). Before ANY master push: `git diff --name-only origin/master..HEAD -- orchestrator/` — non-empty → drain in-flight runs (or pause automation) first. Full story: agents/LESSONS-ARCHIVE.md.

## Git Discipline
| # | Rule |
|---|---|
| 1 | Branch is `master` — not `main`. `git pull origin master --rebase` before pushing. |
| 2 | Branch from CURRENT master; always push YOUR case branch (stale bases → monster diffs; no branch → unreviewable). |
| 3 | Never `git add -A` or `git add .` — stage specific files by name, and stage every NEW file you create (verify with `git status`; an uncommitted imported module crashes the backend on startup — #1 cause of broken merges). |
| 4 | Task NOT done until merged to master and deployed. Pushed ≠ done. Reviewed ≠ done. |
| 5 | Task files are gitignored — `agents/tasks/pending/active/done/archive/` not tracked. |
| 6 | Type-check before commit — `cd frontend && npx tsc --noEmit`. Fix errors first. |
| 7 | ONE app changed at once = ONE branch, built in sequence. Separate branches only for genuinely independent apps/subsystems. |
- Duplicate case cuts of the SAME endpoint (663AF1 + D290F7 both built GET /support/turn-credentials): before building, `git log origin/master` + grep the router for the route — if a twin case already landed, ship a superseded marker doc instead of a second implementation (two `@router.get` defs for one path merge silently and shadow each other).
- Bash harness under `set -euo pipefail`: a helper must NOT end with a bare `[[ cond ]] && cmd` — false cond → rc=1 → `set -e` aborts the whole script with ZERO output (61647F ran 0 of 10 tests). Use `if…fi` or `return 0`; RUN shipped tests in default no-flag mode before claiming green.
- Before decomposing an auto-filed infra incident, search open cases for the same failure signature (repo + error_type + host) and ATTACH — never file a twin parent; children across ALL active parents touching one file set serialize, never parallel; at the gate, twin branches of one fix = merge the best ONE, close the rest superseded (2026-07-09 storm E4AC51+1CCDBD; durable fix CASE-20260709-541957).
- **Verify the output channel EXISTS before building on it (2026-07-09):** AIS-C (E4C9F5) closed `done` with its user-visible deliverable (screen annotations) terminating in a log-only stub (`loggingAnnotations` → `annotation_skipped`); the render channel it assumed (@openreplay/tracker-assist draw) is call/remote-control-gated and never mounts for chat-only agents. A deliverable resting on a stub or assumed-but-unverified adapter seam is NOT done: grep the seam for stubs and prove the visible effect end-to-end before building AND at the gate. Fix: CASE-20260709-09C6D1.
- **Migrating a SHARED storage key to a new backend must update EVERY reader, not just the writer (2026-07-10):** 88162F moved the AI-support session id from `localStorage` to `sessionStorage` in the dock (`announceAiSupportSession`) but left `AnnotationOverlay.tsx` — a second component landed in parallel (09C6D1) — still gating on the old `localStorage.getItem` + `storage` event, so the overlay went permanently inactive after reload (old key never written again). When you branch from a stale master and change a key/event both a writer and reader share, grep the whole tree for the key and route both through ONE shared helper. Fix: CASE-20260710-72AF65.
- **Never assign `window.name` for per-tab state (2026-07-10):** 88162F used `window.name = 'grotap-win-'+rand` as a window-scope discriminator, but the platform uses `window.name` as the `window.open` TARGET NAME so app tiles refocus instead of stacking (`WorkspaceContext.openApp` names windows by app slug; `ClaudeCodePage` uses `'claude-code-seat'`). Clobbering it made every app open a duplicate window after AI support ran in it. To distinguish a same-tab reload from a `window.open` sessionStorage copy, use a discriminator that does NOT hijack `window.name` (and note plain sessionStorage keys ARE copied by `window.open`). Fix: CASE-20260710-72AF65.
- **Verify referenced DB columns against the LIVE schema — task-provided "SCHEMA FACTS" can be wrong (2026-07-10):** 6EC85C built `scripts/pipeline_backlog_report.py` faithfully to a task that asserted `pipeline_dispatch_log` has a `created_at` column; it does NOT (real timestamps: `queued_at` sparse, `dispatched_at` always-populated, `started_at`, `completed_at`), so Section 4 raises asyncpg `UndefinedColumnError` at runtime and crashes the report. Even when a task hands you "SCHEMA FACTS (do not guess others)", confirm every column exists (`information_schema.columns` or `\d <table>`) before writing SQL against it — a spec can be authored from a stale mental model. Fix: CASE-20260710-6EC85C-FIX.
- **Client/server feature split across two agents (s1 receiver / s2 sender) MUST pin the exact wire contract, or the halves silently don't compose (2026-07-10):** the agent-06 pickup-trigger feature was built as two cases — sender 7875EC POSTs `{DEPLOY_TRIGGER_URL}/trigger-deploy` with body `{case_id, branch}`; receiver 187629 only handles `POST /trigger-pickup` and requires body `{dispatch_log_id, task_file, server_ip, session}`. Path AND payload diverged (and neither matched the s1 "dispatch.sh for the dispatch_log_id" contract), so a live trigger would fall through to the status handler → 200 (FALSE success) and dispatch.sh never runs. When two agents build the two ends of one HTTP call, ONE side must own an explicit contract (path + method + headers + exact JSON keys) and the other implements against it verbatim; the builder AND the gate must grep both files and diff sender's URL/payload against receiver's route + required-fields validation before merge. Bonus: don't `asyncio.create_task()` a fire-and-forget coroutine without retaining the task ref (GC can cancel it). Fix: CASE-20260710-0798B7.
- **"Modify the intake logic to prevent X" means WIRE it into the live path — a new helper nothing calls is dead code, not a fix (2026-07-10):** D2DD45 was tasked to dedup auto-filed failure cases; it added `pipeline_automation.py::auto_file_case()` with a correct-looking 30-min-window signature check, but nothing calls it and `pipeline_automation.py` has no failure-intake entrypoint — so duplicate parents are still created (the 541957 storm stayed unfixed) and the case merely LOOKED done. Two rules: (1) a case that says "prevent/dedup/intercept at intake" is unmet until the ACTUAL insert path invokes your logic — grep for the real `INSERT INTO pipeline_cases` from automated alerts and route it through your function; adding an orphan helper the dependent test can call directly is a false green. (2) Dedup by SELECT-then-INSERT with no DB-level guard still races: two near-simultaneous auto-files (the exact storm: same failure twice in ~45s) both miss the SELECT and both insert. Use a partial unique index / advisory lock / `ON CONFLICT` on the signature+window so concurrent dupes are impossible, not merely narrowed. Rejected at gate; fix: CASE-20260710-D2DDFX.
- **Side-effects on a state transition must fire off the rows your UPDATE actually flipped (RETURNING), not the pre-UPDATE candidate SELECT (2026-07-10):** 0FB344 added Slack "case unblocked" notifications by looping over the pre-UPDATE `release`/`toRelease` candidate list in `run_dep_release_for_org` + `on_child_done`. But the Inngest dep-check (`dependencyCheck.ts`, cron every 5 min AND `pipeline/case.status.changed` event) races those backend paths on the SAME `awaiting_deps→plan_approved` transition — both can SELECT the case as a candidate before either commits, so both notify → duplicate ops-channel spam. When N independent paths can perform the same idempotent transition, do the side-effect off `UPDATE ... WHERE status='<old>' ... RETURNING case_id` and act only on the returned rows — only the one path whose UPDATE actually flipped the row wins, giving exactly-once even under full concurrency. Fix: CASE-20260710-FB3F01.
- **Rebase-staleness on hot files: notifications/side-effects belong wherever master CURRENTLY handles that transition, not where it did when you branched (2026-07-10):** 0FB344 put its Slack send inside the `dependencyCheck.ts` DB `step.run`, but master ac1b9d29 had already moved released-case handling into a post-step `for (const caseId of counts.releasedIds)` loop (request-dispatch + metadata stamp) — structural conflict at the gate, merge aborted. Before adding a hook to a pipeline/orchestration file, `git log -p` the target function on current master first; if the flow was recently restructured, attach to the NEW seam. Superseded by CASE-20260710-FB3F01.
- **`get_control_plane_db()` returns a `ControlPlaneDB`, NOT an asyncpg pool — call raw queries through `.pool` (2026-07-10):** `ControlPlaneDB` (`backend/app/database/control_plane.py`) exposes only a `pool` property plus named CRUD helpers; it has NO `fetchval`/`execute`/`fetch`/`fetchrow`/`fetch_all` methods and no `__getattr__` proxy, so `db.fetchval(...)` raises `AttributeError` at runtime and 500s the endpoint. Use `db.pool.fetchval/execute/fetchrow/fetch`. Extra trap: asyncpg.Pool has NO `fetch_all` method — the multi-row call is `db.pool.fetch(...)` (returns list of Records). Grep every `db.<method>(` in a router and confirm each resolves to a real asyncpg.Pool method before merge. (cobrowse.py write path fixed in CASE-20260710-0C818B; read endpoints in CASE-20260710-CB1FIX.)
- **Worker-side severity enums are UPPERCASE; backend `severity_map` keys are lowercase — normalize at the boundary (2026-07-10):** agent-worker `BugFinding.severity` is `'CRITICAL'|'HIGH'|'MEDIUM'|'LOW'`, but `cobrowse.py::save_bug_report_internal` maps via `{"critical":"P1","high":"P2","medium":"P3","low":"P4"}.get(body.severity, "P2")` — sending uppercase silently falls through to the `"P2"` default AND stores the uppercase string in `bug_reports.severity`. When one side of a wire enum is cased differently from the other's lookup table, `.toLowerCase()` (or match the map keys) at the send site. Fixed in CASE-20260710-0C818B (`snapshot-test.ts`).
- **A new X-Node-Secret / internal endpoint is UNREACHABLE until its path is registered in `TenantAuthMiddleware` — handler-level auth never runs otherwise (2026-07-10):** C9FE02 built `POST /internal/pipeline/auto-file` (router prefix `/internal/pipeline`) with a correct in-handler `X-Node-Secret` fail-closed check, but `backend/app/middleware/tenant_auth.py` exempts the REVERSED prefix `/pipeline/internal/` plus a fixed `NODE_SECRET_EXEMPT_ROUTES` list — neither matches `/internal/pipeline/*`. Automated callers send only `X-Node-Secret` (no Bearer JWT), so the middleware 401s them at the Bearer fallthrough BEFORE the handler runs → the whole feature is dead. When adding any machine/internal endpoint: (1) grep `tenant_auth.py` and confirm the exact path is covered by `PUBLIC_PATHS`, a prefix check at the `call_next` line, `PUBLIC_POST_PATHS`, or `NODE_SECRET_EXEMPT_ROUTES` — a fail-closed handler check is NOT enough on its own; (2) prefer reusing an already-exempt prefix (here `/pipeline/internal/`, which the codebase already exposes) over inventing a new top-level path that needs a fresh middleware edit; (3) mind prefix ORDER — `/internal/pipeline` ≠ `/pipeline/internal`. Rejected at gate; fix: CASE-20260710-AF9C01.
- **A "flag for owner review, do NOT run in prod" migration must NOT land in the auto-applied dir; and never DROP a column still referenced by master code (2026-07-11):** CASE-20260711-234F76 was tasked to author a cobrowse_session_id DROP migration for OWNER review only (do not execute). Its branch put v042_drop_cobrowse_session_id.sql under backend/db/migrations/control_plane/ — but backend/app/database/migration_runner.py auto-applies EVERY vNNN_*.sql there at each boot, so merging = auto-DROP on next deploy, the exact opposite of "owner review only". Worse, the "no code writes this column" premise was FALSE: ai_support.py (INSERT+RETURNING+read), cobrowse.py, and support.py still write/read cobrowse_session_id, so the drop would 500 live support flows. Two rules: (1) if a migration is meant to be human-gated, it CANNOT go in an auto-run migrations dir — confirm where the runner picks files up before choosing a path; (2) a DROP COLUMN is safe only after every master reader/writer of that column is removed and shipped first — grep the whole backend for the column name and prove zero references before authoring the drop. Rejected at gate; fix: CASE-20260711-63241C.
- **Changing an INSERT/model/function signature means updating its tests in the SAME branch — a signature change without test updates leaves master's suite red, and the py_compile-only merge gate won't catch it (2026-07-11):** master commit a64ffd12 removed `cobrowse_session_id` from `ai_support.py` (CreateSessionRequest model + the `ai_support_sessions` INSERT, dropping it from 7 positional params to 6 so `openreplay_session_id` moved from `$4`/`args[3]` to `$3`/`args[2]`) but touched no test files, so 4 tests in `test_openreplay_feature_gating.py` / `test_ai_support_openreplay_plumbing.py` (asserting `"cb-123" in insert_args`, `insert_args[3] is None`, fake-pool `openreplay=args[3]`) began failing on master and stayed red because nothing runs pytest at merge. When you alter a SQL column list, a Pydantic model, or any positional-arg contract, grep the tests for the old shape (column name, positional index, fake-pool arg map) and fix them in the same branch; the reviewer only runs `python -m compileall` + `tsc`, so a red pytest ships silently. Also: `git diff A...B` (three-dot) diffs vs the merge base and HIDES changes that landed on master after you branched — a stale branch can look net-new while actually regressing already-landed work; confirm with two-dot `git diff origin/master..branch`. Found by review gate 2026-07-11; fix: CASE-20260711-AISTST.
- **Postgres multi-line string concatenation: the `E` escape prefix goes ONLY on the FIRST literal of the group (2026-07-11):** `scripts/seed_openreplay_notices.sql` prefixed every continuation line (`E'a\n'` newline `E'b'`) — that is a syntax error, so the merged "run once" seed could never run and shipped broken. Adjacent literals separated by a newline concatenate, but only the first may carry `E`; continuation lines are bare `'...'` (their `\n` still escape via the group's leading `E`). Any seed/utility SQL delivered in a branch must actually be EXECUTED (against prod for idempotent seeds, or at least `db.py -f` syntax-run) before sign-off — "merged" ≠ "runs". Hand-fixed 92ba89cd.
- **Adding a module-level import to a router breaks every test that stubs that dependency — update the stubs in the SAME branch (2026-07-12):** de11fd40 added `File`/`UploadFile` (fastapi), `RedirectResponse` (fastapi.responses) and `Field` (pydantic) to `claude_app.py`, but `tests/providers/test_anthropic_provider.py` builds in-memory stub modules for fastapi/pydantic that listed only the OLD names — collection died with `ImportError: cannot import name 'File' from 'fastapi' (unknown location)` and EVERY backend-touching CI run failed for 3 days, silently blocking Railway backend auto-deploys (deploy gates on CI; docs-only commits skip backend tests, so CI "looked" intermittently green). ~8 test files stub fastapi this way (`test_billing_webhooks.py` etc.). When you add ANY module-level import to a router/service, grep tests/ for `_stub_module(` + the module name and extend every stub that shadows it; `(unknown location)` in an ImportError = a bare `types.ModuleType` stub, not a broken install. Hand-fixed e5b33818.
- **NEVER run DDL on a tenant pool at request time — RLS-hardened tenants connect as `app_user`, which has no CREATE on schema public, and Postgres checks that privilege BEFORE the `IF NOT EXISTS` shortcut (2026-07-11):** legacy `_ensure_*` helpers in `apps.py`/`api_keys.py`/`rainbow.py`/`documents.py` ran `CREATE TABLE IF NOT EXISTS`/`ALTER TABLE` on `request.state.tenant_pool`; once a tenant moved to the hardened role every such endpoint 500ed with `InsufficientPrivilegeError` even though the tables already existed — GET /apps died and platform home showed "No apps yet" (no app launchable). Tenant schema belongs in provisioning (`provision_shard.py`/`backfill_rls_roles.py`), never in routers; if you must keep legacy ensure-DDL, route it through `tenant_db.ensure_tenant_ddl()` (swallows only the privilege denial, returns False — don't latch "initialised" flags on False). Emergency fix 51edf492; durable cleanup: CASE-20260712-TENDDL.
- **Renaming/deleting a module means fixing every `mock.patch("old.module.path...")` STRING target too — `compileall`/`tsc` never see inside patch strings, and stale targets raise `AttributeError` only at test run (2026-07-12):** CASE-20260710-3E9B12 renamed `backend/app/providers/cobrowse_provider.py` -> `assist_provider.py` (`CobrowseProvider`->`AssistProvider`, `get_cobrowse_provider`->`get_assist_provider`) and correctly updated its own test file, but left `tests/test_ai_support_openreplay_plumbing.py` and `tests/test_openreplay_feature_gating.py` patching `"app.providers.cobrowse_provider.get_cobrowse_provider"`. `unittest.mock.patch` resolves that dotted string lazily at `__enter__`, so the now-deleted module import blows up with `AttributeError: module 'app.providers' has no attribute 'cobrowse_provider'` — 5 tests failed. A grep of `import` statements misses these because the old name lives inside a patch string, not an import. After ANY module/symbol rename, grep the WHOLE tree (not just imports) for the old dotted path in string literals — `grep -rn "old_module_name" backend` including tests — and confirm zero hits before sign-off; the review gate's `compileall`+`tsc` gate cannot catch a patch-string typo. The case's own "grep-verify no stale references" constraint would have caught it. Rejected at gate; fix: CASE-20260712-ASSPRV.
- **A branch that adds a REQUIRED field to a shared TS interface breaks every sibling branch that CONSTRUCTS that type — both pass `tsc` alone, only the COMBINATION fails (2026-07-12):** CASE-20260712-695BAD added `assigned_team_id: string | null` and `location_visibility: LocationVisibility` as non-optional fields to the ranch-plan `Task` interface (`frontend/src/pages/ranch-plan-types.ts`). CASE-20260712-E5E41B (built in parallel against the OLD type) constructs 5 `DEMO_TASKS` object literals of type `Task`; once 695BAD merged, those literals failed `npx tsc --noEmit` with TS2739 (missing the two new required props). Each branch was individually green, so neither agent nor per-branch review caught it — the review gate only saw it at the post-merge combined tsc and had to merge 695BAD solo + route E5E41B's fixtures back (CASE-20260712-E5FX1B). Mitigations: (a) COALESCE a type-defining case with its type-consuming siblings onto ONE branch so the combined tsc runs before sign-off; (b) prefer adding new interface fields as OPTIONAL (`?:`) when existing code/fixtures construct the type, tightening to required only once all constructors are updated; (c) at the gate, always run the FULL combined-tree tsc after merging sibling frontend branches, never trust per-branch green.
- **Parallel branches that each rewrite the SAME control-flow function go stale the moment master refactors that function's structure — merging them then means hand-weaving two incompatible rewrites, which the gate must refuse (2026-07-12):** three cases targeted `agents/team-run.sh` at once. Master merged CASE-...-F27E64 (commit 3508fd78) which replaced the flat `_run_attempt 1/2/3` escalation ladder with `_run_rung_with_retries <rung>` (per-rung retry cap + circuit-breaker). Two OTHER in-flight branches — CASE-20260712-D41872 (prompt-cache prefix invariant) and CASE-20260712-A39F15 (bounded retry context) — had both been cut from the OLD ladder and each rewrote the same message-assembly flow; after the refactor landed, both referenced now-deleted symbols (`_run_attempt`, the "Attempt 1/2/3" sections) and conflicted structurally with master AND with each other. The gate merged neither and filed ONE combined rebase case (CASE-20260712-B39D41) carrying both intents forward onto the current `_run_rung_with_retries` structure. Lessons: (a) COALESCE cases that touch the same file's core control flow onto ONE sequential branch — never dispatch them as independent parallel branches; the last ones to land are unmergeable. (b) At the gate, before merging any branch that rewrites a function, grep master for the symbol names the branch's diff references (`_run_attempt` etc.); if master renamed/restructured them, the branch is stale → file a rebase case, do NOT hand-weave. (c) Same stale-base trap hit `agents/config.sh` in the same batch: CASE-...-4E28E5 was cut before master swapped team2 `TEAM_MODEL_STRONG` deepseek-chat->grok-4.5, so its merge tried to regress that line — resolve by keeping master's current value and taking ONLY the branch's net-new addition (team3).
- **`re.escape()` in Python 3.7+ does NOT escape `:` — code that strips a leading `\:` from an escaped pattern silently never matches, quietly breaking `:param` route matching (2026-07-13):** the SCTX screen-context resolver's `_pattern_to_regex` (`backend/app/services/screen_context.py`) did `re.sub(r"\\:([A-Za-z_]\w*)", r"[^/]+", re.escape(pattern))` to turn `/users/:id` into `/users/[^/]+`. Since 3.7 `re.escape` only escapes regex metacharacters and `:` is NOT one, the escaped string is `/users/:id` (no backslash), so the `\\:` sub matched nothing → the pattern stayed literal `^/users/:id$` and matched ONLY the literal string `/users/:id`, never `/users/42`. Exact-match tier still worked, so the bug hid until a unit test exercised the parameterised tier. Match the raw token `re.sub(r":([A-Za-z_]\w*)", ...)` (colon is literal in the escaped output), or don't pre-escape. Surfaced+fixed by CASE-20260713-10C11D. Rule: when regex-transforming an `re.escape()`d string, remember which chars 3.7+ leaves unescaped (`: / - space , = ! " % ' / < > @ ` _ backtick ~`) — don't assume a `\<char>` will be present.
- **A feature split across cases must have exactly ONE schema migration — sibling API/frontend cases must depend on it, never ship their own copy of the same tables; duplicate migrations collide at RUNTIME by filename-sort order and crash boot, and git cannot see it (2026-07-13):** the Object Glossary feature arrived as 4+ cases. CASE-20260713-A9D6F7 shipped the canonical `v044_glossary_schema.sql` (the one the already-merged `scripts/seed_object_glossary.py` depends on: `glossary_objects(explanation, synonyms text[], md_refs)` + seeded default libraries). But CASE-20260713-C3E1FD (the search API) ALSO shipped `v044_object_glossary.sql` that `CREATE TABLE`s the SAME three tables, and CASE-20260713-E535B8 shipped a THIRD, incompatible `v042_object_glossary.sql` (`canonical_name/category/metadata`, single `synonym` col). Two runtime traps the merge gate's `compileall`/`tsc` never catch: (a) the runner applies files in sorted filename order, so `v044_glossary_schema.sql` (g) creates the tables, then `v044_object_glossary.sql` (o) — using bare `CREATE TABLE` with NO `IF NOT EXISTS` — errors "relation already exists" and ABORTS the migration runner → every deploy boot-fails; (b) `v042_*` sorts BEFORE `v044_*`, so the incompatible E535B8 schema would create the tables with the WRONG shape first, poisoning the canonical migration's `CREATE TABLE IF NOT EXISTS` (no-ops on the bad tables) AND breaking the seed script + default-library INSERTs. Git shows NO conflict (different filenames, disjoint files) — the gate must reason about the *combined* migration set. Rule: at the gate, when >1 branch in a batch adds control_plane migrations, grep every new `*.sql` for `CREATE TABLE` and check for the same table name across files; merge only the ONE canonical schema, and file rebase cases for the siblings to (i) DELETE their duplicate schema migration and (ii) move any app-registration/data INSERTs into a NEW `vNNN` file. Every schema migration must use `CREATE TABLE IF NOT EXISTS`. Review gate 2026-07-13: merged A9D6F7, filed CASE-20260713-C31FDF (rebase C3E1FD), superseded E535B8.
- **Migration files must NOT contain explicit `BEGIN;`/`COMMIT;` — the runner already wraps each file in its own `conn.transaction()`, so an inner `COMMIT` commits early and splits the DDL from the ledger INSERT into separate commits (2026-07-13):** CASE-20260713-A9D6F7's `v044_glossary_schema.sql` wrapped its whole body in `BEGIN;...COMMIT;`. asyncpg tolerates the nested `BEGIN` (warns "already a transaction in progress", no-op) and the inner `COMMIT` closes the runner's managed transaction, then the `INSERT INTO schema_migrations` ledger row runs in autocommit as a SEPARATE commit — so on partial failure the DDL can commit without a ledger row (idempotent `IF NOT EXISTS` saved it here, but it's fragile). `backend/db/migrations/README.md` states each file "must run inside a single transaction" — the runner provides it; NO other `vNNN_*.sql` uses BEGIN/COMMIT. Rule: at the gate, grep new migration files for `^\s*(BEGIN|COMMIT|START TRANSACTION)\b` and reject/flag; the transaction is the runner's job. (Merged as-is 2026-07-13 because idempotent + verified non-fatal, but never author it this way.)
- **Filing a fix case (review gate or ad-hoc) = set the reporter in BOTH the `reporter` column AND `case_data.reporter` — the POLSUB policy guard only reads `case_data->>'reporter'` (2026-07-13):** batch5 filed P1 CASE-20260713-B84124 with only the column set to `review-gate`; the async `pipeline/policy.complete` callback then yanked it from `submitted` back to `awaiting_human` within ~2 min of EVERY resubmit (guard at `backend/app/routers/pipeline.py` ~371 saw no JSON key → not gate-exempt), stranding the P1 during a redeploy freeze with no hold row to signal it. Siblings filed WITH the JSON key (C31FDF, B39D41) were untouched. Recovery: `jsonb_set(case_data,'{reporter}','"review-gate"')` + flip to `submitted`. Durable fix (guard checks column OR JSON key): CASE-20260713-D6F50F.
- **A "failed"/never-merged case can still have its migration LIVE in prod, poisoning a later canonical migration into a boot-abort — dry-run every pending `vNNN` against the live DB before merging a branch that depends on it (2026-07-13):** confirmed the theoretical poisoning above actually happened. CASE-20260713-E535B8 was marked `failed` and its `v042_object_glossary.sql` never merged to master — BUT that run had already applied the migration directly to the prod control-plane DB, leaving `glossary_libraries/glossary_objects/glossary_synonym_overlays` with the incompatible shape (`slug/canonical_name/category`, NO `is_default/org_id/created_by`). The canonical `v044_glossary_schema.sql` (merged, still PENDING — ledger tops out at v043) then can't reconcile: its `CREATE TABLE IF NOT EXISTS` no-ops over the poisoned tables, and the next statement `CREATE UNIQUE INDEX ... WHERE is_default = true` errors `column "is_default" does not exist` → the single-txn file aborts → `migration_runner.run_migrations()` raises → BOOT ABORTS on the next redeploy. `compileall`/`tsc` and even `git log` show nothing; the live DB state is the only witness. Gate rule: (1) `git status` of a case (failed/superseded) does NOT mean its DDL is absent from prod — a failed run may have applied it. (2) Before merging any branch whose code references columns from a not-yet-applied migration, DRY-RUN that migration against the live control-plane DB inside `BEGIN; ...; ROLLBACK;` and confirm it applies. (3) Fix a poisoned pending migration by making it self-heal — prepend a guarded `DROP TABLE ... CASCADE` of the legacy tables (only when they lack the canonical column) BEFORE the CREATEs; it's editable while unapplied (not in the ledger, so the checksum guard is inert). Review gate 2026-07-13 batch5: held CASE-20260713-09879A (code clean, blocked on live schema), filed P1 CASE-20260713-B84124, DID NOT push (a redeploy would brick boot).
- **Dry-run the WHOLE pending migration file end-to-end, not just the one statement you suspect — a single file can hide MULTIPLE independent boot-abort defects (2026-07-13):** batch5 diagnosed the `v044_glossary_schema.sql` boot landmine by dry-running ONLY the suspect `CREATE UNIQUE INDEX ... WHERE is_default=true` line in isolation, confirmed it errored, and filed the fix — but never ran the rest of the file. Batch6, after prepending the guarded self-heal DROP, dry-ran the ENTIRE file (`BEGIN; <full file minus its own BEGIN/COMMIT>; ROLLBACK;`) against live prod and hit a SECOND, unrelated abort three statements later: `CREATE INDEX ... USING GIN (array_to_string(synonyms,' ') gin_trgm_ops)` → `ERROR: functions in index expression must be marked IMMUTABLE` (`array_to_string(anyarray,text)` is STABLE, not IMMUTABLE). Fixing only landmine #1 would have shipped a still-boot-bricking migration. Gate rule: to validate a migration, pipe the whole file (stripped of any inner BEGIN/COMMIT) into one `BEGIN; ...; ROLLBACK;` with `-v ON_ERROR_STOP=1` against the live DB and confirm it reaches ROLLBACK with zero ERROR lines — never spot-check a single statement. Two valid fixes for the non-IMMUTABLE expression-index case: (a) wrap in an `IMMUTABLE` SQL function the query also calls (`CREATE FUNCTION glossary_synonyms_text(text[]) ... IMMUTABLE ... array_to_string($1,' ')`), or (b) DEFER the index to the case that adds the query (correct without it on a small/empty table). Review gate 2026-07-13 batch6: the parallel-built fix CASE-20260713-B84124 chose (b) and was adopted over the gate's own (a).
- **When the schema a feature-cluster depends on ALREADY landed on master (a prior batch merged the canonical `vNNN`), sibling API/frontend branches forked BEFORE it still carry their OWN copy of that migration — the 3-dot diff shows it as "added", but merging it OVERWRITES the applied canonical file with a different body → checksum-mismatch BOOT ABORT; AND their code assumes THEIR migration's shape, not the deployed one (2026-07-13):** review-gate batch7 — master already had canonical `backend/db/migrations/control_plane/v045_voice_calls.sql` (PK `call_sid`, NO `id` column; the live table matches). CASE-20260713-44E02E and -53DD04 each re-added `v045_voice_calls.sql` at the SAME canonical path with a DIFFERENT body (`id UUID PRIMARY KEY`, `call_sid` merely UNIQUE). `migration_runner.run_migrations` records a sha256 per file and, when a ledger filename's checksum no longer matches, RAISES → boot aborts on the next redeploy. Worse, both branches' CODE assumed their own shape: 44E02E's `_CONTACT_SELECT` used `vc.id AS vc_id` and 53DD04's `_find_known_number` ran `SELECT id FROM voice_calls` — but the deployed table has no `id` column, so 44E02E 500s the LIVE authed `GET /contact` and 53DD04 500s every outbound call once provisioned. `git` shows both files as clean adds; only a byte-compare vs master's current file + a column check against the live DB reveal it. Gate rule: (1) for any migration a branch "adds" whose filename ALREADY exists on master, `diff <(git show origin/case-X:PATH) <(git show origin/master:PATH)` — if it differs, DROP the branch copy (never overwrite an applied migration; the canonical is authoritative) and re-verify the branch code against the LIVE column list (`information_schema.columns`), not the branch's own migration. (2) A voice/PK table using `call_sid` as PRIMARY KEY has NO surrogate `id` — grep merged endpoint code for `voice_calls` `id` references. Both branches rejected; fixes filed CASE-20260713-44E0FX + -53DDFX (drop the dup migration, use `call_sid`/`SELECT 1`, and FOLD -53DDFX's `/voice/outbound` into the existing `voice.py` under the shared router — 128C90 already created that file).
- **An agent told to "MODIFY endpoint X" from a stale base often RE-CREATES the whole router file instead — merging it silently REGRESSES the version a prior case already landed (different impl + different response shape) and re-adds a duplicate `main.py` mount (2026-07-13):** CASE-20260713-A18BBF was scoped to add a `contact_card` to the existing `GET /products/presentation`, but it forked before CASE-20260713-7E36FE landed `backend/app/routers/products.py` on master and shipped a full-file re-creation: a different category source (`request.state.lead_category`, never set by middleware, vs master's `_extract_category` JWT decode) and a different `_AGENTS_OVERVIEW` structure, plus a duplicate `from app.routers.products import ...` + `include_router` in `main.py` (already present on master). `tsc`/`compileall` pass; the regression only shows by reading master's current file. Gate rule: when a branch's diff shows a NEW file (`new file mode`) for a path that ALREADY exists on master, treat it as a stale-base re-creation, NOT an add — reject and file a rebase case that MODIFIES master's current file (here: keep `_extract_category` + `_AGENTS_OVERVIEW`, add only `contact_card` from `TWILIO_FROM_NUMBER`/`VOICE_CONTACT_NAME`/`VOICE_CONTACT_TITLE` env vars; do not touch `main.py`). Fix filed CASE-20260713-A18BFX.
- **A static/vanilla form control's submitted `value` must be the backend's exact enum SLUG, not the human-readable display label — the backend does a case-sensitive membership check with NO normalization, so a display-string value = HTTP 422 on every submit and a silently 100%-non-functional form (2026-07-13):** review-gate 2026-07-13 CASE-20260713-7CCF6C built `docs/landing-register/register.html` with four business-category radios whose `value` attrs were the display labels (`Manufacturing`, `Agriculture — Row Crops`, `Re-wholesale`, `Government Agency`). But `backend/app/routers/registration.py` accepts only `VALID_CATEGORIES = {"manufacturing","agriculture_row_crops","re_wholesale","government"}` and rejects anything else with `422 invalid category` (no `.lower()`, no label→slug map). Every submission failed; the 201-success swap never fired. `compileall`/`tsc` are blind to it (static docs file) and even reading the HTML looks fine in isolation — the defect only surfaces by cross-checking the form's `value` set against the backend enum. Gate rule: for any form/select/radio that POSTs to a backend endpoint, grep the target router for the field's allow-list (`VALID_*`/`Enum`/`in {...}`/Pydantic `Literal`) and confirm each control `value` is a member EXACTLY (case + punctuation); keep the label in the visible `<span>`/`<option>` text, put the slug in `value`. Verdict FIX (form's core function 100% broken; not merged — static docs, no gate impact). Fix filed CASE-20260713-7CCFIX.
- **NEVER hardcode a real secret as an `||` fallback in a test spec — even in a fully-mocked test, a production secret literal committed to git is a permanent leak requiring rotation, not just a code fix (2026-07-13):** review-gate 2026-07-13 CASE-20260713-776BBF (products QA Playwright spec) set `const JWT_SECRET = process.env.JWT_IMPERSONATION_SECRET || 'df4cd8ce…aa61b'` — and that literal, byte-for-byte, EQUALS the real `grotap/prd` Doppler `JWT_IMPERSONATION_SECRET` (verified by hashing both). The spec mocks ALL backend APIs so the JWT is never actually verified (the secret is functionally dead weight), which is exactly why the fallback looked harmless to the author — but the value is now in git history forever and can forge impersonation tokens against any env still using it. It was NOT a new leak: 5 pre-existing master specs (`agent-store`, `agent-team-dashboard`, `brand-lock`, `platform-brand`, `suggest-submit-50`) already carry the identical literal — a systemic pattern. Correct fallbacks already in-repo: `ag-grid-license-smoke.spec.ts` throws if the env var is absent; `test_auth_brand_filter.py` uses a dummy `"test-secret"`. Gate action: merged the test-only branch (156 committed e2e PNGs already establish that screenshot convention; backend filtering test already lived on master as `test_products_presentation.py`) but stripped the literal to a `'test-secret'` dummy in the merge (fix commit c0848167), filed P1 CASE-20260713-SECFIX1 to sweep the 5 master files, and flagged that the code fix does NOT remediate the historical exposure — the owner must ROTATE the secret in Doppler prd+dev and refresh every Railway copy. Author rule: a test JWT signed with a secret the backend never checks (mocked APIs) needs only a dummy `'test-secret'`; a test that hits a real backend needs the env var with a throw-if-absent guard — NEVER embed the literal. Gate rule: grep any e2e/test branch for hex-looking `|| '…'` fallbacks and byte-compare against live Doppler secrets before merge.
- **App-registration migration: `apps.status` is CHECK-constrained to `('voting','building','beta','live','deprecated')` — `'active'` is NOT valid and aborts the migration → boot abort on next redeploy (2026-07-13):** review-gate 2026-07-13 CASE-20260713-C31FDF shipped `v045_object_glossary_app.sql` with `INSERT INTO apps (... status, app_status) VALUES (... 'active','active')`. The live `apps_status_check` (`CHECK (status = ANY (ARRAY['voting','building','beta','live','deprecated']))`) rejects `'active'`; the single-txn migration file errors and `migration_runner` raises → boot aborts. `compileall`/`tsc` are blind (it's SQL). Every existing app-registration migration (`v014_claude_app`, `v037_claude_code_app`, `v039_scan_m_app`) uses `status='beta', app_status='beta'` — that is the convention. Gate rule: for any `INSERT INTO apps`, confirm `status` ∈ the CHECK allow-list (grep `pg_constraint` for `apps_status_check`, or copy a sibling app-reg migration) — default `'beta'`; `category` is likewise constrained (`Finance/Legal/HR/Operations/Platform/Infrastructure/Internal/ai-knowledge`). Gate-fixed `active`→`beta` in the merge commit and shipped.
- **A "delete the duplicate migration" fix case can be pointed at the CANONICAL file: if the fix branch's base already carries the single good copy (the dup only ever existed on the rejected parent branch, never on this lineage), the agent deletes the real source-of-truth — removing the table's only creator, so fresh provisioning silently loses it (2026-07-13):** CASE-20260713-53DDFX was told "DELETE the re-added `v045_voice_calls.sql` (canonical already applied/live)" — correct advice for the parent 53DD04 branch that shipped a divergent copy, but this FIX branch forked from a base that already had the canonical `v045_voice_calls.sql` (byte-identical to master's applied file) and NO second copy. The agent `git rm`'d it anyway; merging as-is would delete master's sole `CREATE TABLE voice_calls` (existing prod DB unaffected since it's already applied, but any fresh control-plane provision loses the table). Gate rule before honoring a "delete the dup" instruction: confirm a dup actually EXISTS on this branch — `git cat-file -e base:PATH` and byte-compare vs `origin/master:PATH`; only delete when the branch's copy DIFFERS from the applied canonical. If the branch's file == master's applied file, there is no dup — keep it. Gate-fixed by restoring master's `v045_voice_calls.sql` in the merge; kept only the branch's `voice.py`/`tenant_auth.py`/test additions.
- **One case's branch can bundle a DIFFERENT case's work (scope bleed) — merge each case to its own scope, not the whole commit: co-merging duplicates the other case's files and collides when that case lands on its own (2026-07-13):** CASE-20260713-53DDFX (voice `/outbound`) was built as a single commit `feat(voice+products): ... (CASE-53DDFX + A18BFX)` that ALSO re-implemented A18BFX's `products.py contact_card` (a different, cleaner version than A18BFX's own branch). While reviewing, the orchestrator merged A18BFX's dedicated branch to master. Merging 53DDFX whole would have re-conflicted `products.py`/`test_products_presentation.py` and landed a competing contact_card impl. Gate rule: when a branch touches files outside its case's stated scope AND another queued/merged case owns those files, do a `--no-commit` merge and resolve the out-of-scope files to `HEAD` (`git checkout HEAD -- <path>`), keeping only the files the case actually owns; note the scope correction in the merge message.
- **A refactor that renames a shell function/var OR changes a log/output string MUST update its companion test assertions in the SAME branch — a passing `tsc`/`compileall` gate is blind to bash test breakage, so the stale test only surfaces when someone runs it (2026-07-13):** review-gate 2026-07-13 CASE-20260713-B9FFCA generalized `_team2_daily_cap_ok`→`_team_daily_cap_ok <team>` in `agents/dispatch.sh` and changed the fallback log from `TEAM2_DAILY_CAP=10` to `TEAM_DAILY_CAP[team2]=10` and removed the `TEAM2_DAILY_CAP` env override — but left `agents/tests/test_dispatch_team_routing.sh` T11 asserting the old string and the old env override, silently breaking the suite (no CI workflow runs it, so it fails only for whoever runs it manually). Author/gate rule: when a diff touches a code path with an obvious companion test (grep the tests dir for the renamed symbol / the old output string), the test MUST change in lockstep; a diff that renames the thing under test but doesn't touch the test file is a defect signal, not a clean change.
- **Before rewriting a SHARED control-flow gate, check whether master already generalized the SAME flow via a peer commit that landed after your branch was cut — two parallel solutions to one gate collide as a STRUCTURAL merge conflict, and yours may be the redundant/inferior one (2026-07-13):** B9FFCA rewrote dispatch.sh's team-routing gate as `[[ "$DISPATCH_TEAM" != "team1" ]]`, but master commit 514db03d had already generalized the identical gate via `get_team_runtime "$DISPATCH_TEAM" == "agents/team-run.sh"` (config-driven, strictly better — it honors `TEAM_RUNTIME_SCRIPT`). Merge hit a structural conflict; the branch's routing half was fully superseded while only its per-team-cap half retained value. Gate rule: don't hand-weave — abort, and file a rebase case that keeps ONLY the branch's non-superseded delta re-applied onto master's current structure (here: drop the `!= team1` gate, re-apply just the `TEAM_DAILY_CAP` per-team cap onto master's `_team2_daily_cap_ok`/`get_team_runtime` code, + fix the companion test). Fix filed CASE-20260713-B9FFFX.
- **A docs/smoke-test file that shows example request bodies or asserts response codes must match the REAL handler contract — a wrong example produces false failures for whoever runs it (the deploy looks broken when it isn't) (2026-07-14):** review-gate 2026-07-14 CASE-20260713-1D7D28 shipped `docs/landing-register/README.md` with a smoke curl using `"category":"info"` (not in the registration handler's `VALID_CATEGORIES` = {manufacturing, agriculture_row_crops, re_wholesale, government} → real response is 422, not the asserted 201), and claimed the honeypot path returns **200**. FastAPI applies the route decorator's `status_code=201` to EVERY return unless the handler overrides it with a Response object; the honeypot's bare `return {"ok": True}` therefore returns **201** too — the only observable tell is the missing `id`, not a different status code. Author/gate rule: when a doc asserts an enum value or an HTTP status, grep the handler (the enum set, the `@router.post(..., status_code=N)` decorator) and confirm — don't trust the doc's own numbers. Gate-fixed the category slug and the honeypot status in the merge commit; shipped.
- **Decomposing a NEW feature across parallel agents WITHOUT a ratified schema/service contract makes each agent invent its own incompatible schema, router, and service — the pieces then cannot be integrated and the whole cluster stalls at the gate (2026-07-14):** review-gate 2026-07-14 found the "Free Stream" feature split into ~8 parallel cases where THREE (CASE-20260714-{60EB18,6825BE,1B3898}) each created `migrations/apps/free-stream/v001_free_stream_core.sql` with mutually-incompatible column contracts (`entity_id/entity_type/event_type/payload` vs `id/source_system/event_action/external_ref/status_modifier/current_classification`), TWO created `free_stream.py` routers, TWO created `free_stream_ingest.py`, and TWO created `FreeStreamPage.tsx` — none mergeable together. Worse, a dependent TAP (CASE-20260714-D62068, Stripe billing → Customer events) was merged FIRST (by the orchestrator) while its `app.services.free_stream_ingest` module was still unmerged: master now imports a module that doesn't exist. It didn't brick prod only because the tap is `asyncio.create_task(...)` fire-and-forget with a bare `try/except` around a lazy import — so the ImportError is swallowed and every event is silently dropped (feature 100% inert, invisible to `compileall`/`tsc`). Rules: (1) a feature decomposition MUST ratify ONE canonical schema + service-signature contract BEFORE parallel dispatch — the schema/ingest-owner case defines it, dependents import it; don't let N agents each scaffold their own `v001`. (2) Merge dependency order is schema→service→tap→frontend; NEVER merge a tap/consumer before the service module it imports exists on master (grep the branch for `import ...<new_module>` and confirm the module is live or merged in the same batch). (3) At the gate, the tell that a "foundation" case is really the canonical one: an already-merged consumer's call signatures match it exactly (D62068's `ingest_event(source_system=,event_action=,external_ref=)` + `set/clear_entity_status_modifier` pinned 1B3898 as canonical over the thinner 60EB18/6825BE). When a cluster has incompatible parallel foundations, don't hand-weave — file ONE integration/rebase case naming the canonical and the outliers-to-rebase (filed CASE-20260714-FS0INT), and hold every cluster branch.
- **`CREATE POLICY` has NO `IF NOT EXISTS` form in Postgres — an RLS migration using `CREATE POLICY IF NOT EXISTS ...` is a hard syntax error that aborts the whole migration file (2026-07-14):** CASE-20260714-6825BE's `v001_free_stream_core.sql` used `CREATE POLICY IF NOT EXISTS tenant_isolation ON ...`. Postgres (through 16) rejects `IF NOT EXISTS` on `CREATE POLICY` (unlike CREATE TABLE/INDEX/SCHEMA), so the file errors on first parse and the migration_runner boot-applies nothing. `compileall`/`tsc` are blind (SQL). Idempotent-policy pattern that works (used by 1B3898): wrap in `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE schemaname=... AND tablename=... AND policyname=...) THEN CREATE POLICY ...; END IF; END $$;`. Gate rule: grep any new migration for `CREATE POLICY IF NOT EXISTS` (and `CREATE TRIGGER IF NOT EXISTS`, also invalid) — both must use the `pg_policies`/`DROP TRIGGER IF EXISTS`+`CREATE TRIGGER` guard idioms instead.
- **When a feature's tables live in a NAMED Postgres schema (e.g. `free_stream.*`), EVERY SQL reference must be schema-qualified — tenant/app connections do NOT set `search_path` to that schema, so an unqualified `FROM unified_stream_events` 500s with "relation does not exist" on every call (2026-07-15):** review-gate 2026-07-15 CASE-20260715-924BF5 (Free Stream action endpoints) wrote all three endpoints' SQL with bare table names (`unified_stream_events`, `stream_entities`, `stream_companies`, `stream_action_templates`) while the canonical schema (`migrations/apps/free-stream/v001_free_stream_core.sql` + `free_stream_ingest.py`, both qualifying as `free_stream.<table>`) creates them under the `free_stream` schema. `py_compile`/`tsc` are blind (SQL in strings); the failure only appears at request time. Author/gate rule: when a branch adds an app-schema feature, grep its SQL for the table names and confirm each is `<schema>.<table>`-qualified (match the schema-owner migration/service); the app pattern is qualify-everywhere, not per-connection `SET search_path`. Fix filed CASE-20260715-924BFX (also folds the endpoints into the EXISTING 0A5FE7 router — see next lesson).
- **A dep-only branch that adds a package must first verify the requested package+version EXISTS — an agent handed an impossible spec will silently substitute a wrong version and mask the resulting peer-dep mismatch with an npm `overrides` hack instead of surfacing the false premise (2026-07-15):** review-gate 2026-07-15 CASE-20260715-FD5BAF was told "add `@mantine/rte@6`, no tiptap" — but `@mantine/rte` has NO v6 (terminal 5.10.5, a Mantine-v5 Quill editor; v6 replaced it with the tiptap-based `@mantine/tiptap`, so the literal spec is unsatisfiable). The agent installed `@mantine/rte@5.10.5` against the repo's `@mantine/core@^6.0.22` and added an `overrides` block forcing the v5 editor's peers up to v6 — a v5-on-v6 render-crash landmine that `tsc` misses because nothing imports it yet. Author rule: `npm view <pkg> versions` before adding; if the requested version/package doesn't exist or its peer range excludes the repo's installed major, STOP and report the contradiction — never paper over a peer mismatch with `overrides`. Gate rule: on a dep-add branch, check the added version's peer ranges against the root's installed majors and reject `overrides` that force a cross-major peer. Filed CASE-20260715-RTEFIX (needs owner product decision: `@mantine/tiptap` vs drop).
- **Within a coalesced cluster, two agents can pick DIVERGENT data models for the SAME feature (a new dedicated table vs. columns bolted onto an existing table) — merge the branch that carries the actual consumer/worker, reject the branch whose table nothing reads, and confirm any dark-launch enable command UPDATEs the exact table/column the worker gates on (2026-07-15):** review-gate 2026-07-15 batch7, Free Stream Gmail sync (FSA-E) split into CASE-20260714-6541F8 (the worker `gmail_sync.py`) and CASE-20260714-41B930 (setup hold + dark-launch docs). 6541F8 gated each account on `loop_engine.connected_accounts.gmail_sync_enabled` (columns added by `v006_gmail_sync_cursor.sql`, reusing loop-engine's existing OAuth tokens); 41B930 instead created a brand-new `free_stream.stream_gmail_accounts` table with its own `sync_enabled`, and its operator hold said `UPDATE free_stream.stream_gmail_accounts SET sync_enabled=true` — which the worker NEVER reads, so flipping it does nothing and the feature can never be un-darked via the documented path (orphan table + inert dark-launch). `compileall`/`tsc` are blind (the table exists, the SQL is valid — it's just the wrong table). Gate rule: for a dark-launch/feature-flag branch, grep the actual consumer for the flag it reads (`SELECT ... WHERE <flag>`) and confirm the enable command targets THAT table+column; when two cluster branches model the same state differently, merge the one with the live consumer and file a fix case to rebuild the docs/hold against it (merged 6541F8, closed 41B930 superseded, filed CASE-20260715-FSDLFX).
- **A React component test MUST drive controlled inputs through React's own event path — raw `el.dispatchEvent(new Event('change'/'input'))` after setting `el.value` does NOT fire React's `onChange`, so state never updates and every downstream assertion fails; and the `[attr!="x"]` attribute selector is jQuery-only, not valid CSS, so `querySelector(All)` throws a SyntaxError in jsdom (2026-07-15):** review-gate 2026-07-15 batch8 CASE-20260714-659A0E shipped `PromptBar.test.tsx` with 14/26 tests FAILING for exactly these two reasons — React 18 tracks an input's value via its own prototype descriptor, so a native event dispatched without the native value setter is seen as a no-op change; and `button[aria-label!="Undo last draft"]` threw `Invalid selector`. `tsc`/`compileall` are BLIND to both (types are fine, selectors are strings) — the failures only appear when vitest actually runs, which the review-gate's mandated gates do NOT include. Gate rule: for any branch whose PURPOSE is tests (or that adds a `*.test.tsx`), actually RUN `npx vitest run <file>` before merging — a green tsc is not evidence the tests pass. Author rule: set controlled-input values via `Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype,'value').set.call(el,v)` then dispatch `input`, or use `@testing-library/react` `fireEvent.change`/`userEvent`; never use `!=`/`^=`(non-standard) jQuery selector extensions in `querySelectorAll`. A test file that fails is a defect, not a "small latent" merge-with-fix — reject it. Fix filed CASE-20260715-FSTFIX.
- **A per-app schema feature is NOT live when its code merges — it is live when every tenant DB has (a) ALL its migration files applied and (b) the app_user GRANTS block; and an ORDER BY on a CTE's outer SELECT cannot use the inner alias (2026-07-15):** owner "cannot open Free Stream" — every `/free-stream/*` endpoint 500'd for ~12s while `/health` returned 200 (it only probes schema existence). Three stacked defects: (1) `v001_free_stream_core.sql` had NO grants — RLS-hardened tenants connect as `app_user`, which got `permission denied for schema free_stream` on every SELECT; the loop-engine `v001` grants DO-block (`GRANT USAGE ON SCHEMA` + `GRANT ... ON ALL TABLES` + `ALTER DEFAULT PRIVILEGES`, guarded for missing role) is MANDATORY in every new app-schema migration — fixed via `v003_app_user_grants.sql`. (2) `v002_free_stream_settings.sql` merged but was never APPLIED to tenant DBs, so the router's per-request runtime DDL crashed on `ALTER TABLE free_stream.stream_settings ENABLE ROW LEVEL SECURITY` — `ensure_tenant_ddl` swallows ONLY InsufficientPrivilege; UndefinedTable propagates as a 500 on EVERY endpoint that calls `_ensure_schema`. Runtime DDL is not a fallback on hardened tenants (app_user can't CREATE): each migration file needs an explicit apply-to-all-tenants step (scripts/apply_free_stream_v001.py pattern, owner URL) + verify `SELECT` AS APP_USER post-apply. The ~12s/request was ~40 runtime-DDL statements each round-tripping to a privilege denial — the latch never sets on False. (3) `list_events_stacked` ordered `SELECT * FROM base` (CTE) by `e.occurred_at` — the inner alias is out of scope in the outer query (`missing FROM-clause entry for table "e"`, BOTH sort modes dead); order CTEs by bare output columns. Verify-serving rule: after merging an app's backend, hit its live endpoints with a real tenant JWT (impersonation recipe) — a 200 `/health` is NOT evidence the app works.
- **A full-component REWRITE branched before a peer commit added a small mount/wiring into that same component will structurally conflict and, if force-resolved toward the branch, silently DROP the peer's just-landed integration — abort and rebase, never take-theirs (2026-07-15):** review-gate 2026-07-15 batch8 CASE-20260715-B7D70C rewrote `StreamWorkspace.tsx` into the full workspace but was cut from `daeddea1`, BEFORE master `5540959d` replaced the FSA-D seam with `<PromptBar event={event} />`. The branch has ZERO PromptBar reference; a `-X theirs` or blind take-branch merge would delete master's mount, orphaning `PromptBar.tsx` and defeating its own test case. Gate tell: `git show <branch>:<file> | grep <symbol-master-just-added>` returns nothing → the branch predates the wiring. Rule: file a rebase case that re-applies the branch's rewrite onto current master AND re-inserts the peer's mount in the right pane; hold the branch. Filed CASE-20260715-WSRBFX.
- **An OPERATOR RUNBOOK or SETUP-HOLD case documents actions a human will literally copy-paste during an incident/setup — every path, port, URL, systemd directive, and rm-vs-mv MUST be grepped against the real script/service/router before merge; a "plausible-sounding" instruction is a defect, not docs polish (2026-07-15):** review-gate 2026-07-15 batch13 rejected TWO such cases in one batch. CASE-20260715-31654C (`scripts/free_stream_gmail_hold.sql`) told a human to register GCP redirect URI `https://<domain>/api/loop-engine/oauth/gmail/callback`, but the backend builds `{api_base_url}/loop-engine/oauth/google/callback` (`loop_engine.py:617 _make_redirect_uri`; router mounted with NO `/api` prefix at `main.py` `include_router(loop_engine_router)`; segment is `google` not `gmail`) → copying it fails GCP OAuth with `redirect_uri_mismatch`. CASE-20260715-E5CD3A (JUMPBOX §8.6 wedged-session runbook) said the loop is caused by `Restart=on-failure` (real unit is `Restart=always`+`RestartSec=5`, and on-failure wouldn't even restart a clean exit — self-contradictory), attributed the rapid-exit self-heal to "Claude Code v2.1.200+" (it's OUR `claude-remote-wrapper.sh` §4, fires at ≥2 exits under `RAPID_EXIT_SECS`=30s), and told operators to `rm -rf ~/.claude/projects/<proj>/.claude/resume-state/` — a path that DOESN'T EXIST (real dir is the single `~/.claude/projects/-home-<user>-workspace-grotap`, and the wrapper deliberately `mv`s it to a backup, NEVER `rm`s). `compileall`/`tsc`/`vitest` are all blind to prose. Gate rule: for any docs/hold/runbook branch, extract each concrete claim (redirect URI, port, `Restart=`, state path, rm/mv, HTTP status) and grep the authoritative source file to confirm — reject on any mismatch, file a fix case with the corrected facts + the source line proving it. The rest of a wrong doc can be perfectly good (31654C's `human_holds` INSERT, ON CONFLICT index match, and enable-SQL were all correct) — one wrong operational line still blocks, because that's the line a human runs. Fixes filed CASE-20260715-FSGHFX, CASE-20260715-JBXRUN.
- **Canonical on-box Claude Code jumpbox host is `claudecode.grotap.com` (status agent at loopback `127.0.0.1:8791`, Caddy-proxied at `:443`) — NOT `claudecode.grotap.ai`; the `.ai` strings that appear in a few docstrings (`status_agent_provider.py`, `deploy_trigger.py`, and the JUMPBOX doc's §3.5 Caddy config) are STALE `e.g.` examples, not the configured value (2026-07-15):** review-gate rejected CASE-20260715-6310F4 — a good Layer1/Layer2 restructure of `docs/CLAUDE_CODE_JUMPBOX.md` that put host `claudecode.grotap.ai` in the Layer 2 heading + host table, under a big "⚠ NEVER MIX LAYERS" warning. Source of truth is `scripts/claudecode/provision.sh` (`CLAUDECODE_DOMAIN="claudecode.grotap.com"`, Caddy `claudecode.grotap.com -> 127.0.0.1:8791`), `backend/app/routers/claude_code.py:361` default `"claudecode.grotap.com"`, migration `v049__update_claude_code_slots_server_host.sql`, and `scripts/seat_template_cases.py:103` — all `.com`; public DNS `claudecode.grotap.com -> 178.156.209.112`. The case spec itself even said `.com`. Rule: any jumpbox host/port/auth claim → grep `provision.sh` + `claude_code.py` before trusting; treat `grotap.ai` in a jumpbox context as a defect. Fix filed CASE-20260715-JBXTLD.
- **A refactor that "single-sources" a config file by narrowing a full-file stamp to a single-key subset SILENTLY DROPS every other key the old writer produced — unless another mechanism supplies them; check what the ABSENT-file path writes, not just the re-run/convergence path (2026-07-15):** review-gate 2026-07-15 batch19 rejected CASE-20260715-BC9F58. It replaced `apply-seat.sh` [6/8] config-bridge with a "convergence" block that, when `~/.claude/settings.json` is ABSENT, stamps only `{extraKnownMarketplaces}`, AND deleted the `seat-template.json` `per_user_files` `stamp_if_absent` entry that previously stamped the FULL `agents-tree/.claude/settings.json` (`_warning`,`hooks`,`enableAllProjectMcpServers`,`extraKnownMarketplaces`,`enabledPlugins`). Result: every FRESH seat loses `hooks.SessionStart` (the seat bootstrap hook wired via `.claude/settings.json` per `session-start-hook.sh`), `enabledPlugins` (codex/code-simplifier/ui-ux-pro-max), and `enableAllProjectMcpServers`. Existing seats are masked (their extra keys trip the "user-customized -> skip" branch), so the regression is invisible on the machine you test and only bites the NEXT provisioned seat. `bash -n`/grep-asserts all PASS (syntax fine, forbidden strings absent). Gate rule: when a branch removes a stamp/copy of a config file and replaces it with a subset-writer, diff the OLD source file's keys vs. what the NEW absent-file path writes; if the new path emits fewer keys, grep for another writer that supplies the rest — if none, it's a data-loss regression, reject and file a full-file-stamp fix. The case spec itself was written against a non-existent code shape (referenced `provision.sh` `__SETTINGS_EOF__` heredoc + lines 296-318 that don't exist; the bridge lived in `apply-seat.sh`) — a spec that misdescribes the target file is a strong tell to re-derive scope from the real code before trusting the implementation. Fix filed CASE-20260715-BC9FIX.
- **A lookup-by-external_ref call (update_live_state / any "find the row I ingested earlier" helper) MUST use the EXACT external_ref string the ingest site wrote — grep the producer for its literal ref; a plausible-but-invented prefix silently no-ops because the helper returns False on no-match and every downstream state transition dies quietly (2026-07-15):** review-gate 2026-07-15 batch24 rejected CASE-20260715-E2C59B. Its `voice.py` dial-result/voicemail-complete handlers called `update_live_state(external_ref=f"ring-{call_sid}", source_system="Voice", ...)` for all three transitions (answered/missed/voicemail), but the ringing tap `_fire_voice_ringing_tap` (voice.py ~line 249, already on master) ingests that event with `external_ref=f"call-{call_sid}"`. `update_live_state` matches on `(tenant_id, source_system, external_ref)`, finds NO row with the `ring-` prefix, returns False, and no-ops — so ringing cards never resolve via Twilio webhooks and the 25s reaper reaps even ANSWERED calls to 'missed' (wrong data). `compileall`/`tsc` are BLIND — the string is valid, the function exists, the signature matches; only the value is wrong. The branch also added a duplicate module constant `_FREE_STREAM_VOICE_TENANT` when `_FREE_STREAM_LEADS_TENANT` (same env var, same value) already existed. Author rule: before writing an `external_ref=` on a lookup/update call, grep the ingest site for the literal ref it produces and copy it — never guess a prefix. Gate rule: for any branch calling update_live_state / a by-external_ref lookup, `grep -n 'external_ref=' ` the producer and the consumer and confirm the prefixes match byte-for-byte; a mismatch is a feature-breaking defect, not a small latent one — reject. Fix filed CASE-20260715-E2CFIX.
- **A helper that POSTs an HI hold / notification MUST use the platform's REAL hold endpoint (`POST https://api.grotap.com/human-intervention/`, in PUBLIC_PATHS so NO auth) — an agent will otherwise invent a plausible internal route and mint an unnecessary auth token, and because such helpers are wrapped in a non-fatal try/except the wrong URL fails silently, so no hold is ever filed (2026-07-15):** review-gate 2026-07-15 merged loadtest LT-A skeleton (CASE-20260715-1EB877) + LT-E cleanup_mail (CASE-20260715-DB1D05); DB1D05's `_file_hi_hold()` in `loadtest/cleanup_mail.py` POSTed to `https://api.grotap.com/internal/holds` with a Bearer HS256 token (jose-minted from `JWT_IMPERSONATION_SECRET`) — but `/internal/holds` does NOT exist (grep `backend/app/routers/` — the only hold route is `human_intervention.py` `prefix="/human-intervention"`, allow-listed in `tenant_auth.py:107` PUBLIC_PATHS = no auth). The auth-failure remediation still WORKS (prints the checklist, exits nonzero), so it merged with a fix case (CASE-20260715-DB1DFX) rather than being rejected — the broken part is only the automated hold POST, which fails into its own `except` and prints "POST failed (non-fatal)". Author rule: to file a hold from a script, POST unauthenticated JSON `{task_id, category, priority, description}` to `https://api.grotap.com/human-intervention/` (curl/urllib both fine, no token) — never invent an `/internal/*` route or attach a JWT. Gate rule: grep any new hold/notify helper for its POST URL and confirm it's `/human-intervention/`; a non-fatal try/except around a wrong URL is a silent-no-op defect (merge-with-fix if the primary path still works, reject if the hold is the only signal path). Also: sibling coalesced cases that each scaffold a shared package file (both LT-A and LT-E created `loadtest/__init__.py`) produce an add/add conflict at merge — keep the skeleton-owner's version, take only the module file from the consumer branch.
- **Decomposing a BRAND-NEW feature that introduces a shared schema/router/file into parallel cases dispatched off a base that lacks it makes every slice independently CREATE that same file+tables — a guaranteed structural collision; land the shared foundation FIRST (or coalesce onto one branch), never fan out slices that each `CREATE TABLE`/create-the-same-router in isolation (2026-07-15):** review-gate 2026-07-15 batch29, the agents.grotap.ai per-company agent-service feature was split into CASE-20260715-0AE58E (admin Companies grid + tenant-service PUT), CASE-20260715-8C3DCC (pool CRUD/clone API), CASE-20260715-8FB536 (customer read-only view). All three were built off a master with NO `agent_pools`/`agent_pool_teams`/`tenant_agent_service` tables and NO `backend/app/routers/admin_agent_pools.py`, so each branch created its own `v051_agent_pools*.sql` (0AE58E and 8C3DCC even used the SAME filename `v051_agent_pools.sql`) AND 0AE58E+8C3DCC both created the SAME file `admin_agent_pools.py` with different content. Worse, the three schemas DISAGREED: 8C3DCC used `agent_pool_teams` PK `team_id` + `capacity INT` + `daily_cap` + status CHECK `active/inactive`, while 0AE58E/8FB536 used PK `id` + `capacity JSONB` + status `active/provisioning/suspended/deleted`. Because every migration is `CREATE TABLE IF NOT EXISTS`, whichever lands first SILENTLY wins and the loser's router then references columns that don't exist (a boot/500 landmine that no `compileall`/`tsc` catches). Gate disposition that worked: identify the SUPERSET/canonical branch (8FB536's `v051_agent_pools_service.sql` created all three tables with the richer schema + the customer view + graceful `{configured:false}` endpoint), MERGE it to establish canonical tables on master, then REJECT the two colliding admin branches and file ONE reconciliation case (CASE-20260715-A91FC0) that rebuilds a single `admin_agent_pools.py` UNIONing both routers on top of the now-present schema (rewriting the outlier 8C3DCC to canonical column names). Never hand-weave two full-file rewrites of the same router in the gate. Author/orchestrator rule: when a feature needs a new shared migration or a new shared router file, dispatch that foundation as its own case that lands FIRST, then fan out consumers off the updated master — or coalesce the whole feature onto one branch. Reviewer tell: two `case-*` branches both `create mode 100644` the same path, or two `v0NN_*.sql` files creating the same table with differing column definitions → structural collision, pick the superset, reject the rest, file one reconciliation case.
- **NEVER edit an already-shipped/applied `vNNN_*.sql` control-plane migration to "fix" its seed data — the boot runner (`migration_runner.py`) checksums every applied file against the `schema_migrations` ledger and HARD-RAISES on mismatch ("An applied migration file was edited after it shipped"), aborting boot for the whole fleet; the fix for wrong seed data is a NEW `vNNN+1` file that `UPDATE`s the existing rows (+ an idempotent `INSERT ... ON CONFLICT DO NOTHING` for fresh envs), leaving the shipped file byte-identical (2026-07-15):** review-gate 2026-07-15 batch31 took CASE-20260715-F5DE03 (seed pool1 `agent_pool_teams.model_config` from `agents/config.sh` TEAM_MODEL_ENV). The branch did it BOTH ways: it (a) edited the canonical, already-merged+applied `v051_agent_pools_service.sql` (d6654980) in place to rewrite the seed keys `primary/strong/endpoint` → `provider/model/model_strong/budgets/base_url_env`, AND (b) added a correct new `v052_agent_pool_team_model_config.sql` that `UPDATE`s the five pool1 rows to the right structure and re-`INSERT`s for fresh envs. Part (b) alone fully satisfies the task (runner applies v051→v052 in sorted order, so fresh boots get old keys then v052 corrects them; existing prod rows get UPDATEd). Part (a) is a guaranteed boot landmine: v051 is in the ledger, so on next deploy its recomputed checksum ≠ ledger checksum → RuntimeError → boot abort. `compileall`/`tsc` are BLIND (the SQL is valid; the damage is a runtime checksum mismatch). Gate disposition that worked: MERGE ONLY the new v052 file (`git checkout <branch> -- backend/db/migrations/control_plane/v052_*.sql`), DROP the v051 edit entirely (confirm `git diff origin/master -- v051_*.sql` is empty), commit just the new file. Author rule: to correct seeded/reference data, add the next `vNNN` file with an UPDATE + idempotent INSERT — never touch a file that already has a `schema_migrations` row. Gate rule: for any migration branch, run `git diff origin/master -- backend/db/migrations/control_plane/` and if it MODIFIES an existing (already-on-master) `vNNN` file rather than only ADDING new ones, treat the modification as a boot landmine — strip it, keep only the added files. NOTE: v052 number was also pre-claimed by fix case CASE-20260715-AFARMFX (agent_farms.tenant_id nullable+CHECK, still awaiting_human/unbuilt at merge time) — since this model_config seed took v052 on master first, AFARMFX must rebuild as v053.
- **The control-plane asyncpg pool registers NO JSONB type codec (grep `set_type_codec` across `backend/` = zero hits; `control_plane.py connect()` calls `asyncpg.create_pool()` with no `init=`/codec) — so a JSONB column READS BACK AS `str` and PASSING A python `dict`/`list` to a jsonb param RAISES `DataError: expected str, got dict/list` (with OR without a `::jsonb` cast). Every jsonb WRITE must be `json.dumps(value)` bound to a `$n::jsonb` placeholder; every jsonb READ must `json.loads(row[col])` (guard `isinstance(str)`). `compileall`/`tsc` are BLIND to this — it's a 100% runtime 500 on the write path and a malformed-string body on the read path (2026-07-15):** review-gate 2026-07-15 merged the agent-pools router (CASE-20260715-44330C, `admin_agent_pools.py`) WITH a fix case (CASE-20260715-AAPJSN) because two of its endpoints hit exactly this: `create_team()`/`update_team()` pass `body.model_config_data` and `body.capacity` (dicts) straight into the `agent_pool_teams` jsonb columns → guaranteed 500 on every call (even the default `{}`); and `_team_row()` returns `row["model_config"]`/`row["capacity"]` verbatim → responses emit JSON *strings* instead of objects. Tell in the SAME file that it's fixable, not fatal: the `tenant_agent_service` upsert in that router DOES it right (`json.dumps(enabled_ids)` + `$4::jsonb`, and `_service_row` does `json.loads` when `isinstance(str)`), and `agent_team_provisioner._clone_pool_for_tenant` (CASE-20260715-CBA9E0, merged same batch) is also correct (`json.dumps(...) + $5::jsonb`, `json.loads` on read). Disposition: merged because the defect neither corrupts data nor breaks auth and the surface is net-new admin-only with no FE consumer yet — foundation value + unblocks the feature > two dark broken endpoints. The IDENTICAL bug is pre-existing on master in `admin_agent_team_templates.py create_template()` (passes `body.agent_seed` list raw into the jsonb `agent_seed` column → 500) — folded into the same fix case. Author rule: any `INSERT/UPDATE ... <jsonb_col> = $n` where the bound value is a python dict/list → wrap `json.dumps()` and cast `$n::jsonb`; any serializer reading a jsonb column → `json.loads` guarded by `isinstance(str)`. Gate rule: for any control-plane branch, grep the diff for dict/list values bound to jsonb params without `json.dumps` (and serializers returning a jsonb column without `json.loads`) — it's invisible to compile/type gates and only shows at runtime. This pool has no codec; do NOT assume asyncpg auto-serializes JSON.
- **A frontend that consumes a LIST endpoint and expects each row to carry a nested child array (pool.teams, order.items, …) MUST verify the LIST serializer actually embeds that array — many routers nest children ONLY in the per-id DETAIL endpoint, so the list returns `undefined` for that field and the page silently breaks (feature never renders) or CRASHES (`undefined.length`/`.filter` in a cell/render path) (2026-07-15):** review-gate 2026-07-15 rejected CASE-20260715-41F88A (frontend `AgentServicesPage.tsx` + route wiring for @grotap.com users). The page called only `GET /admin/agent-pools` and treated every pool as `{...,teams: AgentPoolTeam[]}`, but the backend list serializer `_pool_row` (`backend/app/routers/admin_agent_pools.py`) omits `teams` entirely — teams are exposed ONLY by the separate `GET /admin/agent-pools/{pool_id}/teams` detail endpoint, which the page never calls. Result: (1) the EditDrawer team-toggle section (`poolTeams = activePool?.teams ?? []`, gated behind `poolTeams.length > 0`) NEVER renders → the core per-team enablement feature is dead; (2) `poolTeamsByIds()` does `return pool.teams`(undefined)/`pool.teams.filter(...)` and the grid "Enabled Teams" cell renderer then reads `teams.length` → runtime TypeError for any row with a pool assigned — exactly the empty/first-assignment path the case explicitly required to work. `tsc` is BLIND (the type ASSERTS `teams: AgentPoolTeam[]` so `pool.teams` is well-typed at compile; the field is just never populated at runtime). All schema columns + the PUT/GET endpoint/column names (`caps` JSONB, `enabled_team_ids` JSONB, `id` PK) were otherwise correct. Disposition: FIX not merge — the primary feature is non-functional AND there's a reachable crash (not a small latent defect, though it neither corrupts data nor breaks auth). Fix filed CASE-20260715-AGSFIX (scope: nest `teams[]` in the `/admin/agent-pools` LIST via `_team_row`; add frontend `pool.teams ?? []` guard; verify menu-tile grotap gating). Author rule: when a page maps over a list and reads `row.<childArray>`, open the LIST endpoint's serializer and confirm it embeds that array — do not assume list rows carry the same nested shape as the detail endpoint. Gate rule: for any FE branch consuming a list endpoint, grep the backend list serializer for each nested field the FE dereferences; a field present only in the detail serializer is a feature-dead + crash defect invisible to tsc.
- **A `WHERE col = $1::uuid` (or any `text→uuid`/`::int`/`::timestamptz` cast) applied to a value taken from a loosely-typed TEXT column will HARD-CRASH the request with asyncpg `InvalidTextRepresentationError`/`DataError` (a subclass of `PostgresError`, NOT `RuntimeError`/`ValueError`) the moment the column holds a non-conforming value — and `pipeline_cases.tenant_id` is exactly such a column: `NOT NULL text` where ~98/1961 rows hold ORG-ID strings (`org_01KJ…`), not UUIDs. Validate before casting (`uuid.UUID(v)` try-parse / regex) and fall back, and if you wrap the call in `except`, catch the RIGHT class — `except RuntimeError` will NOT catch a cast failure (2026-07-15):** review-gate 2026-07-15 rejected CASE-20260715-4C38F7 (harden run_env minting in `agent_dispatch.py`). Its revised `_fetch_shared_farm_status()` took a tenant-scoped path whenever `tenant_id` is truthy (`WHERE t.tenant_id = $1::uuid`, passing `pipeline_cases.tenant_id`); but that column is `NOT NULL text` so the tenant path is ALWAYS taken (the org-scoped fallback + multi-tenant collision `RuntimeError` fence is dead code), and for the ~5% of rows whose tenant_id is an org-id string the `$1::uuid` cast raises `InvalidTextRepresentationError`. `dispatch_case` wrapped the call in `except RuntimeError` ONLY — so the PostgresError propagates UNCAUGHT and BREAKS DISPATCH for every non-UUID-tenant case. `compileall`/`tsc` are BLIND (valid SQL, correct signature; the value is wrong at runtime for 98 real rows). Verify with `SELECT count(*) FILTER (WHERE col !~* '^[0-9a-f]{8}-...') FROM t` before trusting a text column is uuid-clean. Fix filed CASE-20260715-4C3FIX (validate tenant_id is a UUID before the tenant-scoped lookup; else fall back to org-scoped; keep the collision fence reachable). Author rule: never cast a text column to a strict type in a WHERE/param without validating the value first; asyncpg cast failures are `PostgresError`, not `RuntimeError`. Gate rule: for any branch adding a `::uuid`/`::int`/`::timestamptz` cast on a param sourced from a text column, `SELECT count(*) FILTER (WHERE NOT <matches format>)` on the real table — nonzero = a runtime-crash landmine invisible to compile/type gates.
- **An open-team (team2/3/4/5) run's SUCCESS must land the case at `change_review` for the agent-06 gate — NEVER directly `done`; and the per-attempt gate must structurally validate the diff (touches a task-named file, adds no unnamed files) BEFORE any LLM scoring — a model can "pass" by committing its own narration (2026-07-15):** first team2 production pilot CASE-20260715-66DC98 (single-file fix to `scripts/db.py`) escalated rung 1 gemma4:31b (4 gate-fails) → rung 2 grok-4.5, which committed ONE junk file literally named `Reporting progress, then applying the stream reconfigure.` containing a `report-progress.sh` shell line the model narrated instead of executing — `scripts/db.py` untouched. The internal gate scored it ok=true (llm_usage_log `fleet-team2` 23:59:48Z), team-run.sh reported success, and the case was marked `done` while branch `case-CASE-20260715-66DC98` (0f65f481) was never merged: silent work-loss + false done, invisible to every compile gate. Rules: (1) open-team success paths route the case to `change_review` (or the awaiting_review dispatch-row park) — `done` is reserved for merged work; (2) gate an attempt with `git diff --name-only` vs the task's named files: no named file touched → fail; files added that the spec never names → fail; (3) a filename that reads like a progress sentence is a narration-committed tell. Fix filed CASE-20260716-1A27FE; pilot branch routed to the real gate for rejection.
- **A FRONTEND-only branch (especially a REBASE of an older UI onto refactored master) must be validated against the actual RESPONSE SHAPE and PATH-PARAM IDENTITY of every endpoint it consumes on master — not merely that the route EXISTS. An endpoint that returns `string[]` when the FE types it as `object[]`, or a route keyed on `contact_submission_id` when the FE sends `call_sid`, compiles and type-checks cleanly yet crashes at runtime (render throw / 500 / 404). `tsc` is BLIND: the FE's own `interface`/generic ASSERTS the shape the code wants, so `a.email`/`row.field` is well-typed even though the server never sends it (2026-07-17):** review-gate 2026-07-17 verdicted CASE-20260715-7FCFIX (rebase of 7FCD71 voice-lead assignment UI onto refactored CallsPage) as FIX — but the orchestrator had already merged it to master. Three runtime defects invisible to compile/type gates: (1) `GET /voice/admins` (voice.py `list_voice_admins`) returns `sorted(emails)` = a JSON array of STRINGS, but the FE types it `VoiceAdmin[]`={email,first_name,last_name} and renders `a.first_name ?? a.email.split('@')[0]` → each `a` is a string so `a.email` is undefined → `undefined.split` THROWS during render whenever ≥1 admin exists → the Calls console white-screens; (2) FE PATCHes `/voice/leads/${call.call_sid}/assign` but the route is `/voice/leads/{contact_submission_id}/assign` doing `WHERE id=$1::uuid` — a Twilio call_sid ("CA…") is not a uuid → uncaught PostgresError 500 (and the `/calls` list serializer never even returns the submission id the FE would need); (3) the assigned-admin badge never shows because `list_voice_calls` omits `assigned_admin_email`. Fix filed CASE-20260717-7FCFX2 (P1 — it is LIVE). Author rule: before a FE branch dereferences `resp.<field>` or interpolates an id into a route path, open the CONSUMED endpoint's serializer/handler on master and confirm (a) the JSON shape matches the FE interface field-by-field, and (b) the path param is the SAME identifier the FE holds (call_sid ≠ submission_id ≠ uuid). Gate rule: for any frontend-only/rebase branch, for each `api.get/post/patch(<path>)` grep the backend route — verify the response shape against the FE interface and that any `{id}` path segment is the identifier the FE actually sends; a shape/identity mismatch is a runtime crash, not a latent nit. (Related: the existing "frontend that consumes a LIST endpoint expecting a nested child array" and "text→uuid cast" lessons — same blind spot, different surface.)
- **Fanning out a multi-SCREEN app (Quote Machine: catalog CRUD + quotes admin + work orders + builder wizard + product matrix + public configure) into parallel case-branches off ONE base makes sibling slices independently (a) build a RIVAL copy of the same net-new router file, (b) each edit the SAME shared frontend files (`api/quotes.ts`, `types/quote.ts`, `App.tsx`, and the same stub page), and (c) call CREATE/support endpoints that no slice actually ships — every one is a merge-time collision or a runtime dead-end invisible to `tsc`; even when the shared SCHEMA already landed, the ROUTER + shared-FE files are a second shared foundation that must be single-sourced (2026-07-17):** review-gate 2026-07-17 processed a 6-case Quote Machine coalesced cluster (parent CASE-20260717-524B03) dispatched as 6 separate branches off `3d13f0a8`. Collisions found: CASE-20260717-F052B5 AND -20DAEB BOTH `create mode 100644 backend/app/routers/product_catalog.py` with DIFFERENT shapes (F052B5 = canonical CRUD spec w/ POST+brand_type filter+status='active'+`_is_grotap_user`; 20DAEB = its own GET/PATCH w/ richer row `pricing_display`/`is_quote_on_request`/`active`/`service_level` that ONLY its ProductMatrixPage consumes); CASE-20260717-DBA395's `quotes.py` and -0CBD74's `quotes_public.py` BOTH register `GET /quotes/public/{token}` (same `APIRouter(prefix="/quotes")`, divergent shapes — cents/int vs dollars/float+mode+model_options) → ambiguous double route; -842F35 and -DBA395 both rewrite the same `QuoteMachineQuotesPage.tsx` stub; -20DAEB's `App.tsx` HIJACKS the `/quote-machine` dashboard route (replaces `<QuoteMachineDashboardPage/>`); -842F35's `QuoteBuilderWizard.tsx` calls `POST /quotes`, `GET /quotes/model-options`, `POST /quotes/{id}/send-configure-link` that NO branch ships (DBA395 has only list/detail/patch/send). Disposition that worked: MERGE the cleanly-separable, schema-verified backends + their own-file pages ({F052B5 canonical catalog, 9A73C7 work_orders, DBA395 admin quotes+public viewer+v057}), union-resolving only `main.py` (all add `include_router` at one anchor) and `api/quotes.ts` (HEAD public accept/decline + DBA395 admin fns are ADDITIVE — keep both). REJECT/supersede the 3 colliding branches with FIX cases (842FX2 build the wizard's missing endpoints + rebase page; 0CBFX2 dedupe the public route to one owner + reapply configure FE; 20DFX2 drop rival router, rebuild ProductMatrixPage against canonical router + add a dedicated /quote-machine/products route, restore dashboard). Verify every backend router's columns against the LIVE schema first (`information_schema.columns` for product_catalog/quotes/quote_line_items/work_orders/agent_team_templates) — the F052B5 spec's column names (`setup_fee_cents`/`monthly_cents`/`brand_type` on product_catalog) were WRONG; the agent correctly used the real 14-col schema, so trust the code-vs-schema check over the spec prose. Author/orchestrator rule: a multi-screen app is TWO shared foundations (schema AND the shared router/api/types/App.tsx surface) — land both on one branch (or a foundation-first branch) before fanning out page slices; never dispatch sibling pages that each create-the-same-router or edit the same `api.ts`/`types.ts`/`App.tsx`, and never dispatch a page whose CREATE/support endpoints aren't already on master. Gate tell: two `case-*` branches both `create mode 100644` the same router path, or two routers sharing an `APIRouter(prefix=...)` + same sub-path, or a page calling `api.post(<path>)` whose route isn't on master → structural collision, merge the superset/canonical + own-file pages, FIX the rest. (Reuses the batch29 "land shared foundation first" + the "FE consumes endpoint not on master" lessons; new angle = the shared FE files + rival-router copies are the collision even after the schema exists.)
- **A webhook/event-handler idempotency claim keyed on `WHERE <col> IS NULL` (or `<col> = <expected>`) is DEAD if an EARLIER step in the same flow already populated `<col>` — the guard never matches, so the "first event" branch (mark paid / trigger fulfilment) NEVER runs. Key the idempotency claim on the STATE TRANSITION you are guarding, not on a side-channel column that another step writes (2026-07-17):** review-gate 2026-07-17 merged CASE-20260717-845475 (QM-B quote engine backend) WITH fix case CASE-20260717-QMBFIX. The `accept_public_quote()` endpoint creates the Stripe checkout session and immediately persists `UPDATE quotes SET stripe_checkout_session_id=session.id`. The billing webhook's `kind=="quote"` branch then tried to claim-and-fulfil with `UPDATE quotes SET status='paid', stripe_checkout_session_id=$1 WHERE quote_id=$2 AND stripe_checkout_session_id IS NULL RETURNING quote_id` — but accept already set that column, so `WHERE ... IS NULL` matches ZERO rows, `updated` is None, the handler logs "duplicate — skipped", and `fulfil()` (work_order + pipeline case + human_holds + provisioning) is NEVER called for any paid quote. `compileall` is BLIND (valid SQL); only end-to-end (accept → simulated checkout.session.completed) exposes it, and the branch's own unittest stubbed a quote with a NULL session id — i.e. it tested a state the real flow never produces. Merged anyway (app internal/dark: QM-C/QM-D UIs unshipped → no live trigger; no bad data; ~3-line fix). Correct pattern: claim on the transition — `WHERE quote_id=$2::uuid AND status <> 'paid' RETURNING quote_id` — first event flips accepted→paid + fires fulfil, duplicates find status already 'paid' and no-op; keep a second idempotency backstop in fulfil() itself (it already guarded on work_order existence). Author rule: an idempotency guard must test the very state you flip (status), never a column a prior step in the same happy path also writes. Gate rule: for any webhook/retry-safe handler, trace whether the column in its `WHERE ... IS NULL`/`= expected` claim is written EARLIER in the same flow — if so the guard is dead and the fulfilment branch never runs; and distrust a unittest that seeds a state (NULL session id) the production path can't reach.
- **A standalone column-RENAME migration dropped into `backend/db/migrations/control_plane/` is a BOOT-TIME landmine, not a deferred/manual change: the migration runner (`backend/app/database/migration_runner.py`) globs `control_plane/*.sql` and AUTO-APPLIES every pending file on the NEXT deploy's startup (advisory-locked). So renaming `<table>.<col>` WITHOUT renaming every code reference in the SAME branch crashes the live handler with asyncpg `UndefinedColumnError` ("column <old> does not exist") the instant the app boots — even though nobody ran `db.py` by hand (2026-07-17):** review-gate 2026-07-17 verdicted CASE-20260717-C52B61 as FIX (not merged). Its `v059_quotes_tenant_id_rename.sql` did an idempotent `ALTER TABLE quotes RENAME COLUMN org_id TO tenant_id` (and same on work_orders) with a correct DO-block guard — but master's Quote Machine backend still references `org_id` in 24 places (`quotes.py` `SELECT q.org_id`/`INSERT (quote_number, org_id,...)`/`LEFT JOIN tenants t ON t.tenant_id = q.org_id`; `quote_engine.py` `quote_row.get("org_id")`, work_orders INSERT). v056 created both tables WITH `org_id`, so the code was correct pre-rename; the migration flips the schema out from under it and boot auto-applies it → every quote/work-order query 500s. `compileall`/`tsc` are BLIND (valid SQL, valid Python — the column name is only wrong against the post-migration DB). Fix filed CASE-20260717-C52FIX (couple the rename with renaming all 24 org_id refs to tenant_id in ONE branch, or abandon the rename — the column works as org_id). Author rule: a column rename is a coordinated refactor, never a standalone migration — grep `git grep -n '<oldcol>'` across routers/services/tests and rename every site in the same branch, or don't rename. Gate rule: for ANY branch that RENAMEs/DROPs a column (or changes its type) in a control_plane migration, remember the file auto-applies at boot — grep master for the old column name; ANY surviving reference = a guaranteed live crash on next deploy → FIX, do not merge. (Extends the "referenced column must exist on master schema" rule to its inverse: a migration that removes/renames a column the merged code still uses is just as fatal.)
- **Generalizing a literal-keyed branch means fixing EVERY comparison site of that literal — and a "generalized" gate is only proven by a test that iterates the WHOLE registry (2026-07-17):** the TEAM2-DISPATCH generalization (FC0208 child) claimed to kill the `team2||team3` literal in `agents/dispatch.sh`, but both decision sites still compared `_TEAM_RUNTIME_SCRIPT == "agents/team-run.sh"` — the pool/cap/model-env gate (~:150) AND the runner-script write (~:182). Result: the first-ever team5 dispatch (TEAM_RUNNER=`agents/codex-run.sh`, case 628778) silently skipped team routing, ran the team1 CLAUDE runner on a legacy caller-passed box (grotap-dev-agent), and logged `fleet` instead of `fleet-team5` telemetry — the "pilot" never exercised Team 5 at all (fix case CASE-20260717-22A75E). Rules: (1) when a task says "generalize the X literal", `grep -n` for EVERY comparison of X in the file and enumerate the sites in the deliverable; (2) any routing gate keyed on a registry value (TEAM_RUNNER etc.) needs a test that loops over ALL registry keys and asserts the gate engages for each — a test pinned to the two teams that existed at write time rots the moment team N+1 is added; (3) silent fall-through to a default runner is the failure mode to design against: an unrecognized runtime script should be a loud error or explicit logged fallback, never the implicit else-branch.
- **Feeding a case to an open team (team2/3/4/5) means setting `case_data.team` — the `pipeline_cases.tags` column is INERT to routing (2026-07-17):** TEAM2-PILOT-4 (CASE-20260716-DBUTF8) was "tagged" via `tags=ARRAY['team2']` + plan_approved; it dispatched to team1/agent-03 and merged (33aa3175) with zero fleet-team2 telemetry — Team 2 still unexercised after 4 pilots. The routing chain is: `case_data.team` → `agent_dispatch.py _build_task_fields` → `team: "<id>"` YAML frontmatter in the task file → `dispatch.sh TASK_TEAM` gate. Nothing in that chain reads `tags`. Rule: to feed a team, `UPDATE pipeline_cases SET case_data = jsonb_set(case_data,'{team}','"team2"')` (or file the case with `team` in case_data) + plan_approved; verify the dispatch echoed `Team routing: team2` and llm_usage_log shows `fleet-team2` rows before calling the lane exercised.
- **A verify-and-merge (gate-shaped) case going `done` does NOT mean the TARGET branch merged — the orchestrator merges the CASE'S OWN branch, so a D6848A-style "verification doc + hand off to gate" branch merges cleanly while the coordinated payload branch (case-F5CFF1, migration+code rename) stays unmerged and the whole chain silently loses its deliverable (2026-07-17):** CASE-20260717-D6848A (verify + merge gate for the v059 org_id→tenant_id rename) pushed only `docs/CASE-…-verification.md`, was orchestrator-merged and stamped done; `v059_quotes_tenant_id_rename.sql` and the 24 code renames were still absent from master until a human gate-keeper merged case-F5CFF1 by hand (f45b6fd6). Rules: (1) a gate-shaped case's deliverable is the MERGE OF THE NAMED TARGET BRANCH — its success check is `git log origin/master` containing the payload commits, not its own branch merging; (2) after any chain whose last case is verify/merge-shaped goes done, grep master for the chain's actual files before rolling up; (3) when decomposing, put the target-branch name in the merge case's body AND make its no-commit success path assert the target merge happened. (Extends the FXGATE no-commit-success-path + 66DC98 narration-commit lessons.)
- **A fallback/default helper's sentinel value is for PERSISTING a safe default, NOT for gating a hard rejection — coupling `if result == FALLBACK: raise HTTPException(422)` to a derive-a-default helper turns valid inputs into errors and is scope creep the spec never asked for (2026-07-18):** review-gate 2026-07-18 merged CASE-20260718-CD6506 (case_title.derive_case_title helper + stamp every INSERT INTO pipeline_cases) WITH fix case CASE-20260718-CD6FIX. The helper's raw_input branch correctly SKIPS markdown headers/horizontal-rules/blanks, and the spec explicitly wanted the final fallback string ("Untitled case — needs description") PERSISTED ("empty input -> fallback"). But `submit_case` in `backend/app/routers/pipeline.py` added, unrequested, `if case_data["title"] == FALLBACK_TITLE: raise HTTPException(422)` — so a legitimate submission whose raw_input is header-only (e.g. `raw_input="## Fix login bug"`) derives FALLBACK and gets 422 on the platform's PRIMARY case-intake endpoint (POST /pipeline/cases), rejecting a previously-valid filing. The branch's own test `test_only_headers_and_blanks_falls_through` proves header-only input falls through to FALLBACK; `compileall`/`tsc` are blind (valid code, contract change only). Merged (no data corruption, no auth break, feature otherwise correct + 42 tests). Author rule: a "derive a default / guaranteed non-empty value" helper's job is to always RETURN something safe to store — never wire its sentinel to a raise; if a real emptiness guard is wanted, gate it on the raw INPUT being empty/whitespace (`not body.raw_input.strip()`), not on the derived-title sentinel (which also fires for header-only prose). Gate rule: when a title/slug/default-derivation change adds a rejection path, check whether the sentinel can be reached by legitimate input the spec intended to ACCEPT — if so it's a regression, FIX (merge only if it doesn't corrupt data / break auth).
- **A background loop/scheduler module is NOT shipped until it is registered in `backend/app/services/background_loops.py` (`start_leader_locked_loops`) — a docstring saying "registered in background_loops.py" is a claim, not wiring (2026-07-18):** case 0F7343 delivered `routing_drift.py` + `routing_drift_scheduler.py` with green module tests and went `done`, but `start_routing_drift_scheduler` was never imported/started in background_loops.py, so the routing-drift watchdog was dead code in prod (fix case CASE-20260718-7E9223). Rules: (1) any deliverable whose behavior is "runs periodically in prod" must include the registration edit in the SAME branch and a registry-iterating test asserting the loop is in the registered set; (2) gate reviewers of scheduler/loop cases must grep the startup wiring site (background_loops.py / lifespan), not just the new module; (3) acceptance for such cases = the boot log line on deploy, not pytest alone.
