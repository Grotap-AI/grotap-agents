---
id: "10012"
title: "Bug: Platform — [ScanTap Mobile FOUNDATION] Create the Expo app (new repo grotap-scantap-mobile)"
complexity: complex
priority: high
branch: "case-CASE-202"
case_id: "CASE-20260619-968BC2"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [ScanTap Mobile FOUNDATION] Create the Expo app (new repo grotap-scantap-mobile)

## Context
No additional context provided.

## Requirements
[ScanTap Mobile FOUNDATION] Create the Expo app (new repo grotap-scantap-mobile) — scaffold + Scan screen + local storage + cloud sync. Reader is SIMULATED in this case (case E adds real CS463 USB/BLE).

REPO: create Grotap-AI/grotap-scantap-mobile (gh repo create under the Grotap-AI org). If repo creation is unavailable to you, scaffold under platform/mobile/scantap/ instead and clearly note that in your progress report so it can be split into its own repo later.

STACK: Expo (managed → custom dev client, because case E needs a USB native module). TypeScript. expo-router. The app and the web app share the SAME Neon cloud DB via the platform backend API — do NOT connect to Neon directly from the device; sync through the existing endpoint POST https://api.grotap.com/rfid/scans (and api-staging for testing). Auth: reuse the platform auth (WorkOS) — implement a login screen that obtains a bearer token usable against the API; for now a token paste / test-session flow is acceptable, structured so real WorkOS login drops in.

DELIVERABLES:
1) Project scaffold: Expo + TS + expo-router + EAS config (eas.json) for Android (and iOS placeholder). README with run/build steps. .gitignore. Doppler-free (mobile config via app.config + EAS secrets).
2) Navigation (3 menus per spec): "Start Scanning New Batch", "Setup" (stub in this case — case E fills it), "Send Scans to the Cloud".
3) Start Scanning New Batch screen:
   - Prompts "Start scanning now?" — no tags captured until the user confirms.
   - Live list of tags as they come in (from a SIMULATED reader service that emits fake EPCs on an interval — define a ReaderService interface with a simulator impl so case E can swap in USB/BLE).
   - Live summary: total tags, total unique product IDs, total locations, current GPS (expo-location), distance traveled, elapsed stopwatch, tags/minute.
   - "Capture Scans to Tablet" → persist the batch to local SQLite.
4) Local persistence: SQLite (expo-sqlite) schema for batches + tag records (epc, product_id, geo_lat, geo_lng, signal_strength, scanned_at).
5) Device behaviors: keep screen awake during scanning (expo-keep-awake); audio beep on each tag read (expo-av).
6) Send Scans to the Cloud screen: list local batches, check them off, "Send to Cloud" → POST /rfid/scans (map to ScanSessionCreate: scanned_by, device_name, vendor='CSL CS463', scan_date, records:[{rfid_id, data}]) and mark synced locally.

ACCEPTANCE: app builds (expo prebuild / eas build --profile preview for Android succeeds OR documented blocker); typecheck passes; the simulated end-to-end loop works: start → simulated scans → capture → send to cloud → appears as a new batch in the ScanTap web app. Branch/target: this is a new repo, use its default branch; tag the first working build. Keep secrets out of the repo.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-968BC2" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
