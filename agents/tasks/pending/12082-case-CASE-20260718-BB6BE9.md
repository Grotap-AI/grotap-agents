---
id: "12082"
title: "Chore: Platform — Note which service runs the auto-feed stamping in the auto-feed docs section.  C"
complexity: medium
priority: high
branch: "case-CASE-20260718-BB6BE9"
case_id: "CASE-20260718-BB6BE9"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
---

# Task: Chore: Platform — Note which service runs the auto-feed stamping in the auto-feed docs section.  C

## Context
No additional context provided.

## Requirements
Note which service runs the auto-feed stamping in the auto-feed docs section.

CONTEXT: docs/05-agents/open-model-teams.md's "Auto-feed: how teams receive work" section
does not say which process performs the stamping. Per docs/WORKER_SERVICE.md the singleton
assign loop runs on the dedicated backend-worker service (advisory-lock leader), so both of
that service's flags matter when enabling or debugging auto-feed.

SCOPE (docs/05-agents/open-model-teams.md ONLY — pure append of ONE short paragraph or
bullet at the end of the auto-feed section): state that the assign loop (and therefore the
stamping) executes on the backend-worker service (see docs/WORKER_SERVICE.md), so the
process-level activation flag must be present on that service.

ACCEPTANCE: one appended paragraph/bullet; existing lines byte-identical; no other files.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260718-BB6BE9" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
