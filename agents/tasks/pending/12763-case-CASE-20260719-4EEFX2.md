---
id: "12763"
title: "Bug: Platform — FIX for CASE-20260719-4EEFIX (OpenReplay summarizer dark loop). Dark-by-default"
complexity: medium
priority: high
branch: "case-CASE-20260719-4EEFX2"
case_id: "CASE-20260719-4EEFX2"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
team_fallback: true
---

# Task: Bug: Platform — FIX for CASE-20260719-4EEFIX (OpenReplay summarizer dark loop). Dark-by-default

## Context
No additional context provided.

## Requirements
FIX for CASE-20260719-4EEFIX (OpenReplay summarizer dark loop). Dark-by-default gating is CORRECT (OPENREPLAY_SUMMARY_ENABLED off by default, deferred import, no module-level raise) but the job body calls a nonexistent provider method. DEFECTS: (1) backend/app/tasks/openreplay_summaries.py:303 calls openreplay_provider.session_search(...) — that method does NOT exist (provider has search_sessions / list_sessions / _post_session_search); raises AttributeError the moment OPENREPLAY_SUMMARY_ENABLED is flipped on. Replace with openreplay_provider.list_sessions(filters=[], sort=..., order=..., limit=..., start_ts=..., end_ts=...) — its signature matches the kwargs already passed. (2) Coupled envelope bug ~line 316: reads result.get("sessions") but list_sessions returns {"data":{"sessions":[...],"total":N}} -> always empty; use ((result or {}).get("data") or {}).get("sessions") or []. Minor: user_id (session userId) is stored into the user_email column — semantic mislabel, rename or document. Branch is STALE-BASED — rebuild on current master bb3474e7. Loop must stay dark.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-4EEFX2" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
