---
id: "11526"
title: "Chore: Platform — TEAM3-PILOT-2 — document the open-model agent teams (team2/team3) in docs/05-age"
complexity: medium
priority: normal
branch: "case-CASE-20260712-AD23EF"
case_id: "CASE-20260712-AD23EF"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team3"
---

# Task: Chore: Platform — TEAM3-PILOT-2 — document the open-model agent teams (team2/team3) in docs/05-age

## Context
No additional context provided.

## Requirements
TEAM3-PILOT-2 — document the open-model agent teams (team2/team3) in
docs/05-agents.

CONTEXT: Team 2 (qwen/deepseek via aider, agent-20/21) and Team 3 (Grok 4.5 via
OpenRouter, agent-30/31) are live in agents/config.sh but docs/05-agents has no page for
them; the only sources are config.sh itself and agents/SERVERS.md.

SCOPE (grotap-platform repo, docs/ ONLY):
1. Add docs/05-agents/open-model-teams.md covering, factually from the repo (read
   agents/config.sh, agents/team-run.sh header, agents/SERVERS.md — do NOT invent):
   - the team registry model (TEAM_IDS/TEAM_POOL/TEAM_RUNNER/TEAM_MODEL_ENV in
     agents/config.sh), DEFAULT_TEAM=team1 unchanged;
   - team2 and team3 pools (box names + IPs from SERVERS.md), model ladders
     (TEAM_MODEL -> TEAM_MODEL_STRONG -> Claude with TEAM_CLAUDE_BUDGET_USD cap);
   - how a case routes to a team (case_data.team read at dispatch, team-tagged cases
     dispatch solo, telemetry service tag fleet-<team> in llm_usage_log).
2. Add one line linking the new page from the docs/05-agents index page (or
   docs/00-INDEX.md if 05-agents has no index of its own).

ACCEPTANCE: new page + one index line; every fact traceable to config.sh / SERVERS.md /
team-run.sh; no changes outside docs/.


REVIEW FEEDBACK (gate reject 2026-07-12, attempt 2 — fix ALL THREE):
1. Your previous branch case-CASE-20260712-AD23EF described agent-30 as "GEX131 hardware" and agent-31 as "llm-gpu-02 hardware". WRONG: both are cpx22 cloud instances (see SERVERS.md); GEX131/llm-gpu-02 is a separate GPU server that shares the team2-llm-lan network. Say "cpx22" and do not attribute GPU hardware to these boxes.
2. You omitted acceptance item 2: add ONE line linking the new page from the docs/05-agents index (or docs/00-INDEX.md if 05-agents has no index).
3. Start from current master; do not reuse the old branch content.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260712-AD23EF" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
