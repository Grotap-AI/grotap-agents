---
id: "12662"
title: "Bug: Platform — Fix CASE-20260719-F399AE usage-telemetry change: built on a stale base, it CLOBB"
complexity: medium
priority: high
branch: "case-CASE-20260719-F399FX"
case_id: "CASE-20260719-F399FX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team3"
team_fallback: true
---

# Task: Bug: Platform — Fix CASE-20260719-F399AE usage-telemetry change: built on a stale base, it CLOBB

## Context
Source: review-gate-20260719-batch7

## Requirements
Fix CASE-20260719-F399AE usage-telemetry change: built on a stale base, it CLOBBERS master meta. In backend/app/providers/model_router.py _record_usage, master sets payload["meta"] = {"cache_creation_input_tokens": usage.get("cache_creation_input_tokens", 0)} then payload["cache_discount"] = usage["cache_discount"] at TOP level. The telemetry ingest (backend/app/routers/telemetry.py record_llm_usage / LLMUsageBody) has NO cache_discount field, so a top-level cache_discount is silently dropped; only meta (JSONB) and cost_usd persist. F399AE intent (persist cache_discount under meta) is right, but its patch REPLACES the whole meta dict with {"cache_discount": ...}, deleting cache_creation_input_tokens. FIX: FOLD cache_discount into the existing meta dict, e.g. if "cache_discount" in usage: payload["meta"]["cache_discount"] = usage["cache_discount"] (after meta dict created). Keep F399AE good tests (cached_input_tokens parsing, Lane C isolation, cache_discount parse) but update test_record_usage_puts_cache_discount_in_meta to assert BOTH meta.cache_discount AND meta.cache_creation_input_tokens survive. Files: backend/app/providers/model_router.py, backend/tests/providers/test_openai_compatible_provider.py. py_compile + run the test module.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-F399FX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
