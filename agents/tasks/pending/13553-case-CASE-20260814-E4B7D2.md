---
id: "13553"
title: "Bug: Platform — FOUND BY THE REVIEW GATE 2026-08-14 (batch-3). Not caused by any branch this ru"
complexity: medium
priority: high
branch: "case-CASE-20260814-E4B7D2"
case_id: "CASE-20260814-E4B7D2"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "33936817-b1ac-470d-bbf9-4349df216355"
team: "team2"
---

# Task: Bug: Platform — FOUND BY THE REVIEW GATE 2026-08-14 (batch-3). Not caused by any branch this ru

## Context
No additional context provided.

## Requirements

FOUND BY THE REVIEW GATE 2026-08-14 (batch-3). Not caused by any branch this run merged —
reproduced on a pristine checkout of origin/master.

FAILING TEST (1 of 10 in the file; the other 9 pass):
  backend/tests/test_gdrive_archive_hook.py::FetchGdriveArchiveHookTests::
    test_resolve_file_id_error_in_archive_does_not_break_content
  AssertionError: 'Document body text' not found in ''
  at backend/tests/test_gdrive_archive_hook.py:90

REPRO:
  git checkout origin/master && python3 -m pytest backend/tests/test_gdrive_archive_hook.py -q
  -> 1 failed, 9 passed

CAUSE: CASE-20260814-D31A1A (merged in ee0f7197) implemented step 4 of the CASE-20260814-3BD940
brief — "Hoisting resolve_file_id(url) out of the read so file_id is available on both the success
and the error path" — in backend/app/routers/claude_app.py::_fetch_gdrive_content. resolve_file_id()
is now called BEFORE svc.read_file_content(), and its ValueError is handled by
`except ValueError: continue`, which skips the URL entirely. The test patches resolve_file_id to
raise unconditionally and asserts that content is STILL returned, which was true when the call sat
inside the archive step and only that step failed.

DECIDE WHICH SIDE IS RIGHT, then make master green — do not just delete the test:
 (a) If skipping the URL is the intended behaviour (a URL whose file ID cannot be extracted is not
     a real Drive link, and there is nothing to fetch), then the test's premise is stale: rewrite it
     to assert that an unresolvable URL is skipped and yields no inline block, and rename it so the
     name no longer claims content is preserved.
 (b) If content must survive a resolve failure — note the test name says the ARCHIVE step failing
     must not break content, which is a real and separate guarantee — then the hoist went too far:
     keep resolve_file_id() hoisted for the audit/error path but make only the ARCHIVE call site
     tolerant, so a failure there cannot suppress content that was fetched successfully.
Read the 3BD940 brief step 4 and the D31A1A diff before choosing; (a) looks likelier but the guarantee
in (b) is worth preserving explicitly either way.

GATE: python3 -m pytest backend/tests/test_gdrive_archive_hook.py backend/tests/test_google_drive_service.py
must be fully green (currently 1 failed / 9 passed and 27 passed respectively).


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260814-E4B7D2" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
