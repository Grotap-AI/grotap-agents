---
id: "11461"
title: "Bug: Platform — TEAM2-PILOT-2: port k/M-suffixed aider token parsing to agents/pilot-run.sh  CON"
complexity: medium
priority: normal
branch: "case-CASE-20260711-DBB803"
case_id: "CASE-20260711-DBB803"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
---

# Task: Bug: Platform — TEAM2-PILOT-2: port k/M-suffixed aider token parsing to agents/pilot-run.sh  CON

## Context
No additional context provided.

## Requirements
TEAM2-PILOT-2: port k/M-suffixed aider token parsing to agents/pilot-run.sh

CONTEXT: agents/team-run.sh was fixed (master commit 6a0518f3) to parse aider's
compact token counts ("2.0k sent", "1.2M sent") via a _tok_to_int helper.
agents/pilot-run.sh — the original runner team-run.sh was generalized from —
still has the OLD parser (lines ~232-237: `grep -oE '[0-9,]+ sent'` etc.), so
k/M-suffixed counts parse as empty there.

TASK (single file, nothing else):
1. In agents/pilot-run.sh, port the same fix: add a _tok_to_int helper matching
   the one now in agents/team-run.sh (plain integers with commas, decimals with
   case-insensitive k suffix -> x1000, M suffix -> x1000000, rounded to integer)
   and use it for INPUT_TOKENS / OUTPUT_TOKENS extraction, widening the greps to
   accept the suffixed forms. Mirror team-run.sh's implementation as closely as
   pilot-run.sh's structure allows. COST_USD parsing must remain unchanged.
2. Verify before committing: `bash -n agents/pilot-run.sh` exits 0.

GUARDRAILS:
- Touch ONLY agents/pilot-run.sh.
- Do NOT change its telemetry payload shape, exit codes, or any env/secret handling.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260711-DBB803" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
