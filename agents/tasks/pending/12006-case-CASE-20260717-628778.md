---
id: "12006"
title: "Chore: Platform — TEAM5-PILOT-1 — document the Codex CLI runner (Team 5) in docs/05-agents.  CONTE"
complexity: medium
priority: high
branch: "case-CASE-20260717-628778"
case_id: "CASE-20260717-628778"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team5"
---

# Task: Chore: Platform — TEAM5-PILOT-1 — document the Codex CLI runner (Team 5) in docs/05-agents.  CONTE

## Context
No additional context provided.

## Requirements
TEAM5-PILOT-1 — document the Codex CLI runner (Team 5) in docs/05-agents.

CONTEXT: agents/codex-run.sh (Team 5, merged 2026-07-13 case 8B9BFC) is the full agentic
Codex CLI runner sharing agent-40/41 with Team 4; it is undocumented.

SCOPE (grotap-platform repo, docs/ ONLY):
1. Add docs/05-agents/codex-runner.md covering, factually from the repo (read
   agents/codex-run.sh and agents/config.sh — do NOT invent):
   - what it runs (codex exec headless) and how it differs from team-run.sh/aider;
   - auth (OpenRouter via OPEN_MODEL_API_KEY — there is no OPENAI_API_KEY in Doppler);
   - the result-JSON contract, telemetry service tag fleet-team5 in llm_usage_log,
     budget per run, and TEAM_DAILY_CAP[team5];
   - which boxes it runs on (shared with team4, from config.sh/SERVERS.md).
2. Add one line linking the new page from docs/05-agents/index.md (append at the END of
   the page list to minimize merge conflicts).

ACCEPTANCE: new page + one index line; every fact traceable to codex-run.sh / config.sh /
SERVERS.md; no changes outside docs/.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260717-628778" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
