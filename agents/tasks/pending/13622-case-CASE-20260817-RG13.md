---
id: "13622"
title: "Bug: Platform — Branch case-CASE-20260817-A90A43 was REJECTED by the review gate on 2026-08-17."
complexity: medium
priority: high
branch: "case-CASE-20260817-RG13"
case_id: "CASE-20260817-RG13"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "59652d7e-1874-42c9-96be-c09201f9b23e"
team: "team3"
---

# Task: Bug: Platform — Branch case-CASE-20260817-A90A43 was REJECTED by the review gate on 2026-08-17.

## Context
No additional context provided.

## Requirements
Branch case-CASE-20260817-A90A43 was REJECTED by the review gate on 2026-08-17. Delete
origin/case-CASE-20260817-A90A43 before redispatch. The branch adds one file,
docs/gate-reports/CASE-20260817-A90A43.md, and no code.

REASON 1 (the report's central claim is false). The report states printer_status_decoded is
"confirmed in every layer" and "flows end-to-end from agent decode (s1) -> DB read (s3) -> API
response (s3) -> frontend render". It is not, and it does not. The write path does not exist:
backend/app/routers/print_cloud_agent.py names the column in the UPDATE at line 739, but binds it
at line 753 to a literal
    None,  # printer_status_decoded — not captured by this legacy endpoint
and JobStateRequest (line 367) has no field for it, so an agent cannot supply one either. Verified
with `grep -rn "printer_status_decoded" backend/app/routers/print_cloud_agent.py`: the only two
hits are the SQL placeholder and the hardcoded None. The column is therefore NULL for every job
ever recorded, and PrintCloudJobsPage.tsx renders it behind `{printer_status_decoded && ...}`, so
users always see "No additional details." The report mistook "the column name appears at line 739"
for "a value is written there".

REASON 2 (a receipt does not belong in the repo). Per CLAUDE.md (owner directive 2026-08-13), a run
summary is a log, not an artifact — receipts go to stdout or a report table. docs/gate-reports/ as
a committed per-case verification log is meta work that produces no customer-visible change, and a
stale one is worse than none: this file would sit in the tree asserting a working feature that is
not wired, and the next agent to touch print-cloud would trust it.

FIX SCOPE: there is nothing to re-run — the verification task itself was mis-specified. The real
work is CASE-20260817-RG10, which extends JobStateRequest and _AGENT_UPDATABLE_STATES so the agent
can actually post printer_status_response / printer_status_decoded, and binds them in
update_job_state instead of the hardcoded None. Re-verify the end-to-end flow ONLY after RG10
lands, and prove it with an integration test that posts a state with a decoded status and asserts
the column is non-NULL on read back — not by grepping for the column name. Do not re-create
docs/gate-reports/.

FILES: backend/app/routers/print_cloud_agent.py, backend/tests/test_print_cloud_integration.py
RELATED: CASE-20260817-RG10, CASE-20260817-RG04, CASE-20260817-RG11

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260817-RG13" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
