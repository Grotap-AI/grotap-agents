---
id: "12376"
title: "Bug: frontend/SupportPage — Rebuild the Console export-video work from case CASE-20260719-D9D2B1 (branch ori"
complexity: medium
priority: high
branch: "case-CASE-20260719-D9D2FX"
case_id: "CASE-20260719-D9D2FX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team5"
---

# Task: Bug: frontend/SupportPage — Rebuild the Console export-video work from case CASE-20260719-D9D2B1 (branch ori

## Context
Source: review-gate
Component: frontend/SupportPage

## Requirements
Rebuild the Console export-video work from case CASE-20260719-D9D2B1 (branch origin/case-CASE-20260719-D9D2B1). That branch is BLOCKED at review — do NOT merge it as-is. The "Export video" button in frontend/src/pages/SupportPage.tsx is wholly non-functional against the CURRENT backend contract in backend/app/routers/support.py (routes support_openreplay_export_video_create @ POST /openreplay/sessions/{session_id}/export-video, and support_openreplay_export_video_status @ GET /openreplay/sessions/{session_id}/export-video). Both return _serialize(row) of an openreplay_video_exports row.

FOUR CONTRACT MISMATCHES to fix in SupportPage.tsx:
1. POST response field: backend returns keys id, session_id, status, r2_url, error, timestamps — there is NO job_id. Frontend reads res.data.job_id (undefined). Use res.data.id (though it is not even needed — see #2).
2. Poll path: backend GET status route is SESSION-keyed with NO job_id path param: GET /support/openreplay/sessions/{sessionID}/export-video. Frontend polls .../export-video/${jobId} (extra segment) => 404 on every poll, spinner never resolves. There is exactly ONE export per session (UNIQUE index on session_id), so the poll loop needs no jobId at all — poll the session-keyed route until terminal.
3. Status values: backend CHECK constraint (v061_openreplay_video_exports.sql) is status IN ('pending','processing','done','failed'). Frontend union/spinner uses 'queued'|'running'. Update the ExportVideoJob status type to 'pending'|'processing'|'done'|'failed'; show the spinner while status is 'pending' or 'processing'; treat 'done'/'failed' as terminal.
4. Download URL field: backend returns r2_url (not url). Frontend reads job.url so the Download link never renders even when done. Read r2_url and render the download link from it.

Note: POST returns 202 and, for a re-request (ON CONFLICT DO NOTHING then SELECT), may already be terminal ('done' with r2_url populated) — the handler already returns early on terminal initStatus, keep that, using the corrected field names.

Keep the rest (button disable-while-active, ExportVideoStatus component, spinner keyframes, e2e spec) — just align field names / path / status enum to the backend. Update frontend/e2e/ai-support.spec.ts mocks to the corrected shape.

Gates: cd frontend && npx tsc --noEmit must pass.

Files: frontend/src/pages/SupportPage.tsx, frontend/e2e/ai-support.spec.ts. Contract reference: backend/app/routers/support.py (~lines 1030-1115), backend/db/migrations/control_plane/v061_openreplay_video_exports.sql.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-D9D2FX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
