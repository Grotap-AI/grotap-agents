---
id: "12678"
title: "Bug: Platform — FIX for CASE-20260719-436070 (auto-block security email) — small latent perf def"
complexity: medium
priority: high
branch: "case-CASE-20260719-436BFX"
case_id: "CASE-20260719-436BFX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "6722241d-f716-4053-bbef-44cca7d1bbc7"
team: "team5"
---

# Task: Bug: Platform — FIX for CASE-20260719-436070 (auto-block security email) — small latent perf def

## Context
No additional context provided.

## Requirements
FIX for CASE-20260719-436070 (auto-block security email) — small latent perf defect filed at merge; the branch WAS MERGED (html-escaping is correct and shipped). Defect: in backend/app/services/strike_engine.py auto_block() section 7 (Platform-error email notification), the call `_provider.send(to=..., subject=..., html=...)` is SYNCHRONOUS inside the async auto_block coroutine, blocking the event loop on the SMTP/provider round-trip (auto_block runs in the abuse-defense request path). FIX: wrap that send in `await asyncio.to_thread(_provider.send, to=..., subject=..., html=...)` (asyncio already imported) so the blocking send is offloaded, keeping the existing try/except non-fatal wrapper. This also fulfills the intent of the superseded/skipped CASE-20260719-4AE0CC (offload EmailProvider.send to asyncio.to_thread). Note: 4AE0CC was SKIPPED (structural conflict with 436070 + it interpolated org_name/value/top_paths into email HTML WITHOUT html.escape = injection); its only remaining valuable goal (the to_thread offload) is covered here. If a separate CUSTOMER-facing org-admin notification is still wanted, file that as a new feature case. Rebuild from current master.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-436BFX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
