---
id: "10020"
title: "Bug: Platform — [ScanTap] Backend APIs — Print Tags (stub), AI Compare to Inventory, Customers +"
complexity: complex
priority: high
branch: "case-CASE-20260619-7DBF19"
case_id: "CASE-20260619-7DBF19"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [ScanTap] Backend APIs — Print Tags (stub), AI Compare to Inventory, Customers +

## Context
No additional context provided.

## Requirements
[ScanTap] Backend APIs — Print Tags (stub), AI Compare to Inventory, Customers + Orders/Invoices (SBI stub), Locations/geo, product aggregation.

DEPENDS ON case A (schema v002 + rebrand). If the v002 tables are not yet on `staging`, coordinate/rebase onto A's branch — do not duplicate the migration.

CONTEXT: extend platform/backend/app/routers/rfid_pipe.py (or add platform/backend/app/routers/scantap.py mounted in main.py — prefer a new module to reduce merge conflicts, importing the same `_get_tenant` helper). New external calls (Anthropic) MUST go through a providers/ wrapper (see platform/backend/app/providers/ — there is an Anthropic provider; reuse it). SBI is STUBBED: build a thin `services/sbi_stub.py` interface (get_vouchers, get_work_orders, get_customers, get_inventory, create_order, create_invoice) returning seeded/sample data, so the real SBI connector can drop in later.

DELIVERABLES (all tenant-scoped via _get_tenant, audit-logged via audit_log_event):
1) Print Tags:
   - POST /rfid/print/from-source  body {source:'voucher'|'work_order', source_ref} → reads SBI stub, creates scantap_print_jobs + scantap_print_tags rows with generated EPCs (96-bit EPC hex; deterministic generator util). Returns job + tags.
   - POST /rfid/print/manual  body {items:[{product_id, qty}]} → generates that many tags per item.
   - GET  /rfid/print/jobs and GET /rfid/print/jobs/{id}/tags ; POST /rfid/print/jobs/{id}/label-preview → returns a stub label payload (JSON/SVG) — NO physical printer.
2) Compare to my Inventory (AI):
   - POST /rfid/scans/{session_id}/compare?level=batch|product|tag → aggregates the scan vs scantap_inventory, then calls the Anthropic provider to produce: (a) summary reports (overage/shortage/dud tags/location mismatches), (b) a suggested set of inventory updates to apply. Return structured JSON {reports:[...], suggested_updates:[...]}. Keep the prompt + provider call inside a services/ module.
3) Customers + Orders/Invoices:
   - GET /rfid/customers (from scantap_customers; POST /rfid/customers/sync pulls from SBI stub into the table).
   - POST /rfid/scans/{session_id}/create-order and /create-invoice → only valid if the session has customer_id; calls SBI stub create_order/create_invoice; records result.
4) Locations/geo:
   - CRUD GET/POST /rfid/locations ; POST /rfid/scans/{session_id}/reconcile-locations → assigns each record.location_id by nearest scantap_locations geo within radius (haversine in Python).
5) Product aggregation + session counts:
   - GET /rfid/scans/{session_id}/by-product → groups records by product_id with all detail columns (description,size,category,type,item_class,income/cost/asset_account,catalog_id,customer_name,qty).
   - On session sync/update, maintain unique_products and unique_locations counts on rfid_scan_sessions.

ACCEPTANCE: py_compile passes; new endpoints documented in the router docstring; Anthropic + SBI access only via wrapper/stub modules (Rule: no 3rd-party calls outside providers/); JSONB written with json.dumps (asyncpg has no JSONB codec); never use .get() on asyncpg Records. RELEASE PATH: branch off `staging` and target `staging` (NOT master). This is a platform/framework-touching change set; validate on staging.grotap.com / api-staging.grotap.com before any promotion. Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. All secrets are in Doppler — never touch GitHub secrets.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-7DBF19" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
