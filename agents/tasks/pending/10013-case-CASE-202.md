---
id: "10013"
title: "Bug: Platform — [Human Intervention] App will not open — users cannot see the grid, approve, or"
complexity: complex
priority: high
branch: "case-CASE-202"
case_id: "CASE-20260619-3B53F1"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [Human Intervention] App will not open — users cannot see the grid, approve, or

## Context
No additional context provided.

## Requirements
[Human Intervention] App will not open — users cannot see the grid, approve, or answer queued clarification questions. Diagnose with Playwright and fix.

IMPACT: HIGH. Human Intervention is how humans unblock agents AND answer pipeline clarification questions (category='clarification') and approvals (contact-form leads + pipeline decomposition). While it is down, those queues back up.

WHAT USERS NEED (acceptance): open the app from the platform, see the grid/list of pending holds, Claim, Resolve, Dismiss, and for clarification holds type an answer and "Submit Answer" (which routes back to the originating pipeline case). The History view must also load.

DIAGNOSTIC LEADS (already gathered — verify, don't assume):
- Route IS registered: frontend/src/App.tsx lines ~824-835 (/human-intervention and /human-intervention/history → HumanInterventionPage, behind PrivateRoute).
- Page: frontend/src/pages/HumanInterventionPage.tsx — uses the OLD AppSidebar pattern (not AppFrame). It polls GET /human-intervention/?status=...&limit=50 and GET /human-intervention/stats every 5s; on failure it sets "Failed to load holds" (so a pure API failure would NOT cause a blank/won't-open — suspect a crash or launch/routing/registration issue instead).
- Backend: backend/app/routers/human_intervention.py — list/stats/patch/create; uses get_control_plane_db() (control-plane DB, NOT tenant-scoped), _ensure_table() creates human_holds lazily.
- Registration: seed_apps.sql + seed_brands_apps.sql include human-intervention; check the apps catalog launch path (slug → route) — confirm the catalog tile actually navigates to /human-intervention and the slug mapping is correct (there is a history of non-obvious slug→path mappings).

LIKELY ROOT CAUSES TO CHECK IN ORDER (use Playwright on the live app + console/network logs; self-test by minting a test session — see backend/app/routers/testing.py /testing/agent-token and /auth/test-session):
1) App launch / catalog: does clicking the tile navigate at all? Is the app subscribed + brand-assigned for the logged-in user? Is the slug→route mapping correct?
2) Runtime crash / white screen: open /human-intervention directly and read the browser console. Check for a render crash (e.g. a null field — though categories/priorities are guarded), an auth redirect loop, or a PUBLIC_PATHS/middleware issue.
3) API: confirm GET /human-intervention/ and /stats return 200 with a valid session; if 500, check human_holds table + _ensure_table on the control-plane DB.

DELIVERABLES: identify the actual root cause (state it in the progress report with evidence), fix it, and PROVE it with Playwright (screenshot the working grid + a successful Resolve and a clarification Submit Answer). Add/extend an e2e spec under frontend/e2e covering: app opens, grid renders, resolve a hold, submit a clarification answer.

ACCEPTANCE: app opens reliably, grid + History render, Claim/Resolve/Dismiss/Submit Answer all work, clarification answers route back to the pipeline case, Playwright proof attached, tsc --noEmit + py_compile pass. RELEASE PATH: branch off `staging` and target `staging` (NOT master). This is a platform/framework-touching change set; validate on staging.grotap.com / api-staging.grotap.com before any promotion. Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. All secrets are in Doppler — never touch GitHub secrets.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-3B53F1" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
