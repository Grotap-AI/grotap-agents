---
id: "10011"
title: "Bug: Platform — [ScanTap FOUNDATION] Evolve the existing `rfid-pipe` app into "ScanTap" — schema"
complexity: complex
priority: high
branch: "case-CASE-202"
case_id: "CASE-20260619-99E80C"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [ScanTap FOUNDATION] Evolve the existing `rfid-pipe` app into "ScanTap" — schema

## Context
No additional context provided.

## Requirements
[ScanTap FOUNDATION] Evolve the existing `rfid-pipe` app into "ScanTap" — schema + rebrand + nav.

GOAL: ScanTap is the existing `rfid-pipe` app grown into a fuller RFID inventory product. Do NOT create a new app/slug — extend `rfid-pipe` in place. This case lays the foundation that cases B (backend) and C (frontend) build on, so keep it tight and mergeable.

CONTEXT — files that already exist:
- Schema: platform/migrations/apps/rfid-pipe/v001_initial.sql (schema `rfid_pipe`: rfid_batch_templates, rfid_scan_sessions, rfid_scan_records, ignored_rfid_tags, rfid_audit_log — all RLS tenant-isolated).
- Backend router: platform/backend/app/routers/rfid_pipe.py
- App registration: platform/backend/migrations/seed_apps.sql + seed_brands_apps.sql
- Nav: platform/frontend/src/config/appMenuConfig.ts ; routes: platform/frontend/src/App.tsx

DELIVERABLES:
1) New tenant migration platform/migrations/apps/rfid-pipe/v002_scantap.sql (idempotent, IF NOT EXISTS, RLS like v001) adding to schema `rfid_pipe`:
   - scantap_customers(customer_id uuid pk, tenant_id, external_id text, name text, email, phone, created_at) — SBI-synced customers (stub source).
   - scantap_locations(location_id uuid pk, tenant_id, name text, geo_lat double precision, geo_lng double precision, geo_radius_m numeric, created_at).
   - scantap_inventory(inventory_id uuid pk, tenant_id, product_id text, description, size, category, type, item_class, income_account, cost_account, asset_account, catalog_id, location_id uuid, qty_on_hand integer, ready_date date, updated_at) — local mirror of SBI live inventory (stub).
   - scantap_print_jobs(job_id uuid pk, tenant_id, source text check in ('voucher','manual','work_order'), source_ref text, status text default 'queued', created_by, created_at) and scantap_print_tags(tag_id uuid pk, tenant_id, job_id uuid, epc text, product_id text, data jsonb default '{}', printed boolean default false, created_at).
   - ALTER rfid_scan_sessions ADD COLUMN IF NOT EXISTS customer_id uuid, customer_name text, location_id uuid, unique_products integer default 0, unique_locations integer default 0.
   - ALTER rfid_scan_records ADD COLUMN IF NOT EXISTS product_id text, geo_lat double precision, geo_lng double precision, location_id uuid, signal_strength integer.
   Add tenant indexes + RLS policies mirroring v001. Do not break existing rows.
2) Rebrand to ScanTap (display only — keep slug `rfid-pipe`, schema `rfid_pipe`, routes, subscriptions unchanged to avoid breakage):
   - In seed_apps.sql update the rfid-pipe row: name='ScanTap', description to match an RFID inventory mgmt app, has_mobile=true. Keep slug='rfid-pipe', status='beta'.
   - Update appMenuConfig.ts rfid-pipe entry: display name "ScanTap"; add nav items for the new screens that cases C will fill: existing (Dashboard, Scans, Templates) + new "Print Tags" (/rfid-pipe/print) and "Inventory" (/rfid-pipe/inventory). Keep paths under the existing /rfid-pipe base.
   - Update RfidPipeAppSidebar.tsx appName to "ScanTap" (and icon if appropriate). Do NOT restructure into AppFrame in this case — keep the existing sidebar pattern to stay mergeable; AppFrame migration is out of scope.
3) Register the v002 migration in the app's migrations list the same way v001 is registered (see platform/backend/migrations/phase5_app_schema_migrations.sql pattern for rfid-pipe) so new/existing tenants get it applied. Apply it to the grotap tenant DB (proud-union-74070434) as part of validation.

ACCEPTANCE:
- tsc --noEmit passes; backend py_compile passes; migration applies cleanly to a fresh schema AND on top of existing v001 data.
- App still loads as before, now titled "ScanTap", with the two new (empty-for-now) nav entries routed (a stub page is fine; cases C fills them).
- No changes to slug/schema names. RELEASE PATH: branch off `staging` and target `staging` (NOT master). This is a platform/framework-touching change set; validate on staging.grotap.com / api-staging.grotap.com before any promotion. Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. All secrets are in Doppler — never touch GitHub secrets.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-99E80C" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
