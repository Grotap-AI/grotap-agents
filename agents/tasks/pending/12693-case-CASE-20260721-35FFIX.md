---
id: "12693"
title: "Bug: Platform — Fix unreachable except clause in backend/app/services/agent_team_provisioner.py"
complexity: medium
priority: high
branch: "case-CASE-20260721-35FFIX"
case_id: "CASE-20260721-35FFIX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "24ccfbf8-7c1f-4bd7-ac2b-ca5adb0c9ef4"
team: "team5"
---

# Task: Bug: Platform — Fix unreachable except clause in backend/app/services/agent_team_provisioner.py

## Context
No additional context provided.

## Requirements
Fix unreachable except clause in backend/app/services/agent_team_provisioner.py Step 1 BYO-credential resolution (latent defect merged from CASE-20260721-35F273). PermissionError is a SUBCLASS of OSError, and the preceding clause `except (asyncio.TimeoutError, TimeoutError, OSError, ConnectionError) as exc:` matches it first — so the dedicated `except PermissionError as exc:` block (Freestream needs_human=True alert + _file_hold 'byo-creds-permission-error') is DEAD CODE and never executes. Permission errors are silently handled as transient (needs_human=False, no HI hold), contradicting the case requirement to route permission issues to HI. Fix: move the `except PermissionError as exc:` clause ABOVE the OSError/timeout tuple clause so it matches first. (ConnectionError in the tuple is also redundant — subclass of OSError — optional cleanup.) Verify with py_compile. Scope: that single try/except block only.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260721-35FFIX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
