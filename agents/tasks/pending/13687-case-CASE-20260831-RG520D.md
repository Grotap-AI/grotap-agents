---
id: "13687"
title: "Bug: Platform — Follow-up to CASE-20260831-520DF2 (suppress rename suggestion on pre-v017 tenant"
complexity: medium
priority: high
branch: "case-CASE-20260831-RG520D"
case_id: "CASE-20260831-RG520D"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "c2511a03-c50b-438e-8ace-40031ed71591"
team: "team2"
---

# Task: Bug: Platform — Follow-up to CASE-20260831-520DF2 (suppress rename suggestion on pre-v017 tenant

## Context
No additional context provided.

## Requirements
Follow-up to CASE-20260831-520DF2 (suppress rename suggestion on pre-v017 tenants),
MERGED by the review gate on 2026-08-31 at 9acd542c. The PRODUCTION FIX IS CORRECT and shipped —
do not change backend/app/routers/print_cloud.py. The defect is in the test that is supposed to
pin it, and in a trap in the fixture that will keep producing tests like it.

--- THE FINDING (measured, not inferred)

backend/tests/test_print_cloud_integration.py ::
TestRenamePreV017Schema::test_rename_suggestion_none_pre_v017 passes whether or not the fix is
present. The review gate verified this by reverting exactly one expression on the merged tree —

    rename_suggestion=_rename_suggestion(r, caps) if has_rename else None
  ->  rename_suggestion=_rename_suggestion(r, caps)

— and re-running the class: 4 passed. The regression the case exists to prevent is not covered.

CAUSE: the test seeds the printer with `created_at=self.state.now()`, and the suite's fake clock is
a FIXED point in the past:

    backend/tests/test_print_cloud_integration.py:49
    _BASE_TS = datetime(2026, 7, 6, 12, 0, 0, tzinfo=timezone.utc)

_rename_suggestion() compares created_at against WALL-CLOCK now
(print_cloud.py::_now_utc, window _RENAME_SUGGESTION_WINDOW = 7 days). _BASE_TS is 56 days before
today and grows one day staler every day, so the freshness gate returns None before the has_rename
gate is ever reached. The sibling class already knew this — TestRenameSuggestion._seed_discovered
carries the comment "The monotonic fake clock (_BASE_TS) is weeks in the past, which would
otherwise read every fixture as 'old' and suppress the suggestion" and deliberately overrides
created_at with `datetime.now(timezone.utc)`. The new test did not.

--- REQUIRED

1. Give the test teeth. In test_rename_suggestion_none_pre_v017, seed created_at/updated_at with a
   real-now timestamp (`datetime.now(timezone.utc)`), the same way
   TestRenameSuggestion._seed_discovered does. Then PROVE it: temporarily revert the
   `if has_rename else None` expression, confirm the test FAILS, restore it, confirm it passes.
   A regression test that has not been observed to fail is not a regression test. Say in the case
   notes that you did this.

2. Close the trap for the next author, don't just patch this one test. Pick ONE of these and apply
   it across the file:
     (a) make the fake clock relative — `_BASE_TS = datetime.now(timezone.utc) - timedelta(...)`,
         or expose a `_fresh_ts()` helper that _printer_row defaults created_at to; or
     (b) make the window injectable so the test controls it rather than the wall clock — e.g.
         _rename_suggestion takes a `now` parameter defaulting to _now_utc().
   (a) is the smaller change and keeps production untouched; prefer it unless it breaks the tests
   that rely on _BASE_TS being a stable, printable constant (grep _BASE_TS first — several
   timestamp-formatting assertions may hard-code it, and those must keep their fixed value).

3. Sweep for the same vacuity. Grep the Print Cloud suites for any other assertion that depends on
   a comparison between a fixture timestamp and wall-clock `now()`, and confirm each one still
   fails when its production guard is removed. `_rename_suggestion`'s 7-day window is the only
   Python-side wall-clock window in print_cloud.py today; `_COMPUTER_STALE_AFTER` ('15 minutes') is
   evaluated in SQL by the fake pool, so check how that one is faked before assuming it is sound.

FILES: backend/tests/test_print_cloud_integration.py (and only that file if option (a) is taken).

DO NOT: change _rename_suggestion's gates, the 7-day window, or the has_rename plumbing added by
CASE-20260831-520DF2. They are correct. This case is about the test suite only.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260831-RG520D" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
