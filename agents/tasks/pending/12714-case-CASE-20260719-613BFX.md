---
id: "12714"
title: "Bug: Platform — FIX for CASE-20260719-6138DF (delete-on-complete checkpoint cleanup). Reviewed,"
complexity: medium
priority: high
branch: "case-CASE-20260719-613BFX"
case_id: "CASE-20260719-613BFX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "8815ccfd-e496-40e4-b748-2a0cee6e99a3"
team: "team5"
---

# Task: Bug: Platform — FIX for CASE-20260719-6138DF (delete-on-complete checkpoint cleanup). Reviewed,

## Context
No additional context provided.

## Requirements
FIX for CASE-20260719-6138DF (delete-on-complete checkpoint cleanup). Reviewed, MERGE-BLOCKED, one defect. The case's own critical requirement is that cleanup be NON-FATAL, but the checkpoint delete is NOT wrapped: deleteCheckpointThreadLogic and both call sites have no try/catch, and the delete runs inside step.run("cleanup-checkpoint", ...) in an Inngest function with retries:2. A transient Neon error therefore retries twice then marks the run FAILED even though the session already completed and was durably persisted in the prior step — producing spurious run-failure alerts for successful sessions. FIX (small, 2 sites): wrap the delete call inside each step.run callback in try/catch that logs a warning and swallows the error, in agent-worker/src/functions/run-erp-agent.ts (~line 165-167, uses session_id) and agent-worker/src/functions/resume-erp-agent.ts (~line 87-89, uses thread_id). No schema/dependency/type changes. Also convert the two 'guard' tests in checkpoint-cleanup.test.ts that simulate the if-condition inline to exercise the real functions if practical. Rebuild from current master.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-613BFX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
