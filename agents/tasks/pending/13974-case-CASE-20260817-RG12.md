---
id: "13974"
title: "Bug: Platform — Branch case-CASE-20260817-E44FBF was REJECTED by the review gate on 2026-08-17."
complexity: medium
priority: high
branch: "case-CASE-20260817-RG12"
case_id: "CASE-20260817-RG12"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "8642c37b-d281-498b-89c4-25753c19161a"
team: "team3"
---

# Task: Bug: Platform — Branch case-CASE-20260817-E44FBF was REJECTED by the review gate on 2026-08-17.

## Context
No additional context provided.

## Requirements
Branch case-CASE-20260817-E44FBF was REJECTED by the review gate on 2026-08-17. Delete origin/case-CASE-20260817-E44FBF before redispatch.

SUPERSEDED BY A MERGE. E44FBF's premise — "keep _parse_status_response returning str, add a
separate _is_error_status helper" — was overtaken by CASE-20260817-3F32C1, which the gate MERGED
earlier the same day (commit c06af971). On current master _parse_status_response already returns
(is_error: bool, reason: str) in both agent/print-cloud-agent/print_cloud_agent.py:240 and
backend/app/static/print_cloud_agent.py:338. Landing E44FBF would give the codebase TWO independent
error-decision paths over the same bytes, and they DISAGREE:
  - merged 3F32C1 (ZPL): is_error = parts[1] != "0" or bool(int(parts[4], 16))   # paper-out, flags
  - E44FBF     (ZPL): is_error = parts[2] == "paused" or int(parts[4], 16) != 0
A second source of truth for "is this printer broken" is how false error states get shipped.

DEFECT (independent of the conflict — this check can never fire). E44FBF's ZPL branch tests
    if len(parts) > 2 and parts[2] == "paused":
Field 2 of the ZPL ~HS block-1 response is a single flag DIGIT ("0" or "1"), never the literal
string "paused". The comparison is dead code, so a genuinely paused printer is reported healthy.
The merged decoder has the same field correct (it compares against "0").

DEAD CODE. _is_error_status has zero callers on the branch — verified with
`git grep -n "_is_error_status"`, which returns only the two definitions. It adds a second full
decode of raw (re-splitting and re-parsing bytes _parse_status_response already parsed) for a value
nothing consumes.

FIX SCOPE: do NOT re-add a separate helper. Everything of value here is already covered by
CASE-20260817-RG11, which owns verifying the ZPL ~HS field offsets in the merged tuple decoder and
wiring its currently-unused is_error through _query_printer_status to the call site. Fold the one
real insight from this branch — that TPCL error bits are {0x02,0x04,0x08,0x10,0x20,0x40} and 0x01
(printing) is NOT an error — into RG11; the merged decoder already implements exactly that mask.
Close this case as duplicate once RG11 lands.

FILES: agent/print-cloud-agent/print_cloud_agent.py, backend/app/static/print_cloud_agent.py
RELATED: CASE-20260817-3F32C1 (merged), CASE-20260817-RG11, CASE-20260817-RG10

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260817-RG12" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
