---
id: "12004"
title: "Chore: Platform — TEAM4-PILOT-2 — GLOBAL.md lesson: MODEL_ROUTING_JSON edits must merge, never rep"
complexity: medium
priority: high
branch: "case-CASE-20260717-FC656D"
case_id: "CASE-20260717-FC656D"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team4"
---

# Task: Chore: Platform — TEAM4-PILOT-2 — GLOBAL.md lesson: MODEL_ROUTING_JSON edits must merge, never rep

## Context
No additional context provided.

## Requirements
TEAM4-PILOT-2 — GLOBAL.md lesson: MODEL_ROUTING_JSON edits must merge, never replace.

CONTEXT: 2026-07-09 incident — when the triage/diagnose Lane C entries were added to
MODEL_ROUTING_JSON, the whole value was REPLACED instead of merged, silently dropping the
four approved Lane A entries (compliance-check, learning-extract, finalize,
knowledge-select). Those tasks fell back to Claude at full price and the regression ran
8 days undetected because fallback-to-Claude is the designed safe behavior (nothing errors).

SCOPE (grotap-platform repo, agents/GLOBAL.md ONLY — single file):
Add ONE compact lesson block (3-6 lines, matching the existing lesson-block style) stating:
MODEL_ROUTING_JSON is a shared routing table across services — any edit MUST read the
current value and merge keys, never write a fresh object; after editing, diff the key set
before/after and verify no previously-routed task key disappeared (a dropped key silently
reverts that task to Claude with no error signal). Place it near the other routing/open-lane
lessons (qwen 2026-07-06 incident block).

ACCEPTANCE: one new lesson block in agents/GLOBAL.md, no other files touched, no existing
lesson blocks modified or reordered. Plain markdown.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260717-FC656D" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
