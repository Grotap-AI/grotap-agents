---
id: "13287"
title: "Bug: Platform — Updater execution: detached spawn, loop guard, state persistence  Implement drai"
complexity: medium
priority: high
branch: "case-CASE-20260726-762412"
case_id: "CASE-20260726-762412"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "0dead8bd-24e8-468c-a9d8-49a028e295db"
---

# Task: Bug: Platform — Updater execution: detached spawn, loop guard, state persistence  Implement drai

## Context
No additional context provided.

## Requirements
Updater execution: detached spawn, loop guard, state persistence

Implement drain-on-idle wait (use existing Worker paused/idle state). Spawn installer fully detached (DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP | CREATE_BREAKAWAY_FROM_JOB, no inherited handles, no wait). Pass /VERYSILENT /NORESTART /SUPPRESSMSGBOXES /LOG=<agent-logs-dir>. Do NOT pass /KEY=. Persist %ProgramData%\GrotapPrintCloud\update\state.json with target_version, attempts, last_attempt_at, last_error. Stop after 3 failed attempts on same target; reset attempts on target change. Clear downloaded installer after successful update. Update worker status to 'installing' during spawn.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260726-762412" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
