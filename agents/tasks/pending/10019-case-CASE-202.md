---
id: "10019"
title: "Bug: Platform — [Platform/Ops] Onboard Manorview Farms as a tenant through the fixed provisionin"
complexity: complex
priority: high
branch: "case-CASE-20260620-249223"
case_id: "CASE-20260620-249223"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [Platform/Ops] Onboard Manorview Farms as a tenant through the fixed provisionin

## Context
No additional context provided.

## Requirements
[Platform/Ops] Onboard Manorview Farms as a tenant through the fixed provisioning flow + load 514 locations + create its user.

DEPENDS ON: case H for the Locations UI to view the result (can verify via API if H not merged yet). The provisioning + locations schema fixes are ALREADY live on master.

GOAL: stand up "Manorview Farms" as its own Neon tenant (database-per-tenant) using the now-working automated flow, seed its 514 inventory locations, and create its login user.

STEPS:
1) Create a WorkOS organization "Manorview Farms" (or reuse if exists), then POST /tenants/provision for that org (provisions a new Neon project + base tenant_template schema + control-plane tenants row). Record the new tenant_id + neon_project_id.
2) Subscribe the Manorview tenant to the `rfid-pipe` (ScanTap) app via the app_registry subscribe endpoint. Confirm `app/subscribed` fired and the appSchemaProvision worker applied BOTH migrations into the new tenant DB: check control-plane app_schema_migrations has rfid-pipe v001_initial.sql AND v002_locations.sql = 'applied', and that rfid_pipe.locations exists with the PostGIS boundary column + trigger in the new tenant DB.
3) Load locations: run migrations/apps/rfid-pipe/seed_manorview_locations.sql (514 rows, generated by scripts/scantap_load_locations.py) against the Manorview tenant DB:
   psql "$MANORVIEW_DB_URL" -v tenant_id="<manorview tenant_id>" -f migrations/apps/rfid-pipe/seed_manorview_locations.sql
   Verify count = 514 and geo_points are empty (boundary NULL until captured).
4) Create user info+Manorviewuser1@grotap.com tied to the Manorview org: insert an approved_users row (status='approved') AND send a WorkOS invitation (POST /tenants/members/invite or the approve-user path). Password is set by the user at first login — do NOT set a password. This is NOT a @grotap.com super-admin; it must be scoped to the Manorview tenant only.
5) Verify end-to-end: the user can log in, lands in the Manorview tenant, opens ScanTap, and the Locations screen shows all 514 Manorview locations (Location Name / Zone / Site populated, geo points blank).

ACCEPTANCE: new Manorview Neon tenant live + registered; rfid_pipe schema incl v002 provisioned automatically (proves the subscribe->provision fix); 514 locations loaded and visible; the user can log in and is correctly tenant-scoped (cannot see other tenants). Report the tenant_id, neon_project_id, and a screenshot of the Locations grid. RELEASE PATH: platform/framework-touching — branch off `staging` and target `staging`; validate on api-staging.grotap.com before promotion to master/prod.

PRE-READ before starting: C:\1Claude\ScanTap-ARCHITECTURE-RECOMMENDATIONS.md (what we have / what's best / what to do) and C:\1Claude\ScanTap-BUILD-PLAN.md. Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. All secrets are in Doppler — never touch GitHub secrets. Through-vendor-wrapper rule: no 3rd-party calls outside providers/. asyncpg: json.dumps() for JSONB, never .get() on Records.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260620-249223" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
