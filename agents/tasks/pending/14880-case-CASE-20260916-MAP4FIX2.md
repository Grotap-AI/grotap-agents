---
id: "14880"
title: "Bug: Platform — backend/app/routers/scantap_share.py _mint_scoped_bearer() falls back to the kno"
complexity: medium
model_tier: medium
priority: high
branch: "case-CASE-20260916-MAP4FIX2"
case_id: "CASE-20260916-MAP4FIX2"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "0b950148-fae1-41e3-b63a-8c175f9a2f75"
team: "team2"
---

# Task: Bug: Platform — backend/app/routers/scantap_share.py _mint_scoped_bearer() falls back to the kno

## Context
No additional context provided.

## Requirements
backend/app/routers/scantap_share.py _mint_scoped_bearer() falls back to the known string dev-secret-replace-in-doppler when SCOPED_BEARER_SECRET is not set (line near the end of the file: secret = _SCOPED_BEARER_SECRET or dev-secret-replace-in-doppler). While no middleware currently validates stk_ tokens, when MAP-6 adds that validation a missing env var will silently make all tokens forgeable. Fix: raise a RuntimeError at startup (in the module-level _SCOPED_BEARER_SECRET assignment block) if the value is empty, or at minimum log a warning at import time. Also add SCOPED_BEARER_SECRET to Doppler grotap prd+dev with a strong random value before the MAP-6 bearer validation ships. File: backend/app/routers/scantap_share.py.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260916-MAP4FIX2" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
