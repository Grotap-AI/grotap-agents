---
id: "11456"
title: "Bug: Platform — Implement concurrency test for exact-duplicate auto-file dedup  Write a test in"
complexity: medium
priority: high
branch: "case-CASE-20260711-A077A6"
case_id: "CASE-20260711-A077A6"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
---

# Task: Bug: Platform — Implement concurrency test for exact-duplicate auto-file dedup  Write a test in

## Context
No additional context provided.

## Requirements
Implement concurrency test for exact-duplicate auto-file dedup

Write a test in backend/tests that simulates two near-simultaneous calls to pipeline_file_case (or auto_file_case directly) with the same normalized error signature (touch-surface + time-window overlap). Verify that the DB UNIQUE index or ON CONFLICT clause produces exactly ONE parent case record and that the second call returns is_new=False. Test both raw-description variations (as in the 541957 case) to confirm semantic/normalized dedup works.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260711-A077A6" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
