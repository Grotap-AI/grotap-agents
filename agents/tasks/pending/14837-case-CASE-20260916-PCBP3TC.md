---
id: "14837"
title: "Bug: Platform — In scantap_printing.py print_reconciliation_list (new PCBP-3 endpoint), the GROU"
complexity: medium
priority: high
branch: "case-CASE-20260916-PCBP3TC"
case_id: "CASE-20260916-PCBP3TC"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "bf434cd6-4eef-43d6-b158-999e20b9648d"
team: "team5"
---

# Task: Bug: Platform — In scantap_printing.py print_reconciliation_list (new PCBP-3 endpoint), the GROU

## Context
No additional context provided.

## Requirements
In scantap_printing.py print_reconciliation_list (new PCBP-3 endpoint), the GROUP BY query has AND t.printed in the WHERE clause. Only printed tags are joined, so COUNT(t.tag_id) AS tag_count always equals COUNT(t.tag_id) FILTER (WHERE t.printed) AS printed_count. Fix: remove AND t.printed from the WHERE clause and add HAVING COUNT(t.tag_id) FILTER (WHERE t.printed) > 0. Start from origin/master fresh worktree.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260916-PCBP3TC" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
