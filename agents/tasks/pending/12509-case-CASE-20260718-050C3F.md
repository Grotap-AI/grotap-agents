---
id: "12509"
title: "Bug: Platform — MAT-FIX1: /managed-agent-teams Teams-by-Type table shows invented Agent Count an"
complexity: medium
priority: high
branch: "case-CASE-20260718-050C3F"
case_id: "CASE-20260718-050C3F"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
team_fallback: true
---

# Task: Bug: Platform — MAT-FIX1: /managed-agent-teams Teams-by-Type table shows invented Agent Count an

## Context
No additional context provided.

## Requirements
MAT-FIX1: /managed-agent-teams Teams-by-Type table shows invented Agent Count and Location values — replace with the spec data

CONTEXT: MAT-A (CASE-20260718-436C9E) shipped
frontend/src/pages/ManagedAgentTeamsPage.tsx. Verified on prod 2026-07-18:
the "Teams by Type" table shows Agent Counts 12/8/6/10/4 and Location badges
"AWS us-east-1 / Multi-region / xAI Cloud / Azure eastus / GitHub Actions".
NONE of those values were in the case spec — they were invented. The fleet
runs on Hetzner cloud servers in Ashburn/Hillsboro/Falkenstein (the roster
tables lower on the SAME page already say this correctly), so the page
currently contradicts itself and misstates the product. The count column
header also reads "AGENTS" where the spec said "Agent Count".

TASK (single file: ManagedAgentTeamsPage.tsx; content-only fix, no redesign):
1. Column header "AGENTS" -> "Agent Count".
2. Agent Count values MUST equal the number of roster rows shown for that
   team in the Agent Rosters section below: Claude Code 14, Open-Model
   Executors 2, Grok 2, GPT-5.6 2, Codex CLI 2.
3. Location values (replace the invented cloud badges):
   - Claude Code: "Ashburn, VA — CPX21 cloud servers (+ Hillsboro, OR — CPX31)"
   - Open-Model Executors: "Ashburn, VA — CPX21 cloud servers"
   - Grok: "Falkenstein, DE — CPX22 cloud servers (GPU-adjacent)"
   - GPT-5.6: "Ashburn, VA — CPX21 cloud servers"
   - Codex CLI: "Ashburn, VA — CPX21 cloud servers (shared with GPT-5.6)"
4. Verify no other invented facts remain in the Teams-by-Type section
   (descriptions must match the roster sections' story).

VERIFY BEFORE COMMIT: `npx tsc --noEmit` clean from frontend/; page renders
with the corrected values (Playwright screenshot); still no horizontal
scroll at 375px; still zero IP addresses in page text.

GUARDRAILS: touch ONLY ManagedAgentTeamsPage.tsx. Do not change layout,
styling, routes, or the roster tables (they are already correct). Every
fact on this page must come from THIS prompt — do not invent numbers,
locations, providers, or vendor names.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260718-050C3F" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
