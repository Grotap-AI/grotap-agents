---
id: "10022"
title: "Bug: Platform — [Loop Engine FOUNDATION] New app `loop-engine` — tenant migrations + app registr"
complexity: complex
priority: high
branch: "case-CASE-20260619-E80886"
case_id: "CASE-20260619-E80886"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [Loop Engine FOUNDATION] New app `loop-engine` — tenant migrations + app registr

## Context
No additional context provided.

## Requirements
[Loop Engine FOUNDATION] New app `loop-engine` — tenant migrations + app registry + nav + stub console.

GOAL: stand up the Loop Engine skeleton cases B-F build on. Loop Engine is a per-tenant automation console: a "loop" = a named, scheduled instance of a reusable "bundle" (the logic). Spec §1-4, §13.2.

DELIVERABLES:
1) Manifest ONLY at platform/apps/loop-engine/app.manifest.json (use spec §4 JSON: slug=loop-engine, name="Loop Engine", icon "🔁", category "Operations", workos_feature_id "loop-engine", status "building"). No standalone Vite project (app-template-guide.md).
2) Tenant migration platform/migrations/apps/loop-engine/v001_initial.sql — idempotent, IF NOT EXISTS, tenant_id UUID NOT NULL + RLS tenant_isolation + tenant index on EVERY table. Tables EXACTLY per spec §3 (loops, loop_runs) PLUS §13.2 connected_accounts and the loops.account_id column. connected_accounts stores ENCRYPTED tokens (enc_refresh_token/enc_access_token BYTEA). Register the migration like rfid-pipe v001 so new/existing tenants get it; apply to the grotap tenant DB (proud-union-74070434) as validation.
3) App registry: add the loop-engine row to control-plane (backend/migrations/control_plane_apps.sql pattern) + brand assignment so it shows in the catalog; create WorkOS Feature `loop-engine` via the app-registry/register path (app-template-guide.md step 4 + app-store-model.md).
4) Frontend wiring: nav entry in frontend/src/config/appMenuConfig.ts (display "Loop Engine"); route in frontend/src/App.tsx to a stub console page (PrivateRoute + AppShell). Stub is fine — case E fills it.

ACCEPTANCE: tsc --noEmit + py_compile pass; v001 applies cleanly to a fresh schema AND on top of existing data; app appears in catalog and opens to the stub. No business logic yet. RELEASE PATH: branch off `master` and target `master` (app-only fast path, NO staging — owner directive). Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. ALL secrets in Doppler (grotap/prd) — never touch GitHub secrets. AUTHORITATIVE SPEC: read docs/12-app-platform/loop-engine.md (grotap-agents repo) BEFORE starting — it has the data model, API, Inngest funcs, the Gmail bundle steps, and the §13 platform-build additions (generic `gmail-cleanup` slug, per-account OAuth, connected_accounts). Obey the 9 ABSOLUTE RULES in CLAUDE.md (Doppler, vendor wrapper, Neon DB-per-tenant, tenant RLS scoping, AppShell+Cobrowse, 4-reviewer). Inngest functions live in the TypeScript worker (Rule 2: agents=TS/JS); Python only fires events via providers/inngest_provider.py.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-E80886" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
