---
id: "10015"
title: "Bug: Platform — [Loop Engine] Per-account Google OAuth + `gmail_provider.ts` vendor wrapper (spe"
complexity: complex
priority: high
branch: "case-CASE-202"
case_id: "CASE-20260619-0751AA"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [Loop Engine] Per-account Google OAuth + `gmail_provider.ts` vendor wrapper (spe

## Context
No additional context provided.

## Requirements
[Loop Engine] Per-account Google OAuth + `gmail_provider.ts` vendor wrapper (spec §7, §13.2, §13.3).

DEPENDS ON case A (connected_accounts table). Read spec §7 + §13.2/§13.3.

CONTEXT: Inngest functions + their providers live in the TypeScript worker (the bundle executor in case C calls this provider). Vendor-wrapper Rule 3 — NO Gmail SDK call outside the wrapper. Secrets in Doppler (grotap dev+prd): GOOGLE_OAUTH_CLIENT_ID, GOOGLE_OAUTH_CLIENT_SECRET, LOOP_ENGINE_ENC_KEY. OAuth scope: https://www.googleapis.com/auth/gmail.modify ONLY (label + trash; NEVER mail.google.com/ full delete).

DELIVERABLES:
1) app/providers/gmail_provider.ts (in the TS Inngest worker) — methods the bundle needs: list_labels, ensure_label(name)->id, search_threads(query, pageToken), label(threadIds, addLabelIds), unlabel(threadIds, removeLabelIds) INCLUDING adding TRASH, get_profile. Refresh access token from the stored refresh token. Batch in <=1000-id chunks; backoff on 429.
2) OAuth connect flow on the FastAPI loop-engine router (Python): GET /loop-engine/oauth/google/start (returns Google consent URL; state = signed tenant+nonce) and GET /loop-engine/oauth/google/callback?code&state (exchange code, upsert connected_accounts with the refresh token ENCRYPTED via LOOP_ENGINE_ENC_KEY — Fernet/AES-GCM). NEVER store or log plaintext tokens.
3) GET /loop-engine/accounts — list connected mailboxes (email, status; never tokens).

ACCEPTANCE: worker typecheck + py_compile pass; OAuth round-trip works against a real Google account (document the consent step); token column is BYTEA ciphertext, not plaintext; provider unit-coverable via mock. JSONB via json.dumps; never .get() on asyncpg Records. RELEASE PATH: branch off `master` and target `master` (app-only fast path, NO staging — owner directive). Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. ALL secrets in Doppler (grotap/prd) — never touch GitHub secrets. AUTHORITATIVE SPEC: read docs/12-app-platform/loop-engine.md (grotap-agents repo) BEFORE starting — it has the data model, API, Inngest funcs, the Gmail bundle steps, and the §13 platform-build additions (generic `gmail-cleanup` slug, per-account OAuth, connected_accounts). Obey the 8 ABSOLUTE RULES in CLAUDE.md (Doppler, vendor wrapper, pooled tenancy + FORCE RLS scoping, AppShell+Cobrowse, 4-reviewer). Inngest functions live in the TypeScript worker (Rule 2: agents=TS/JS); Python only fires events via providers/inngest_provider.py.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-0751AA" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
