---
id: "15140"
title: "Bug: Platform — Fix photoCount state and camera capture UI in antenna-test.tsx  BLOCK-1: Fix pho"
complexity: medium
model_tier: medium
priority: high
branch: "case-CASE-20260924-290850"
case_id: "CASE-20260924-290850"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "004405ae-58ed-4de5-8e52-840d565354cf"
team: "team3"
---

# Task: Bug: Platform — Fix photoCount state and camera capture UI in antenna-test.tsx  BLOCK-1: Fix pho

## Context
No additional context provided.

## Requirements
Fix photoCount state and camera capture UI in antenna-test.tsx

BLOCK-1: Fix photoCount state that is never updated. Add camera button in Photos step that calls capturePhoto(), store result via addPhoto(), and update photoCount via setPhotoCount(photos.length) after each capture and on listPhotos() load. Remove dead addPhoto import and ensure capturePhoto is properly imported.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260924-290850" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
