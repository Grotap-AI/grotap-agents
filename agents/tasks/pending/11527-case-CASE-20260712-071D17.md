---
id: "11527"
title: "Chore: Platform — TEAM3-PILOT-4 — document the open-model sandbox verification gate in docs/05-age"
complexity: medium
priority: normal
branch: "case-CASE-20260712-071D17"
case_id: "CASE-20260712-071D17"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team3"
---

# Task: Chore: Platform — TEAM3-PILOT-4 — document the open-model sandbox verification gate in docs/05-age

## Context
No additional context provided.

## Requirements
TEAM3-PILOT-4 — document the open-model sandbox verification gate in
docs/05-agents.

CONTEXT: the Docker sandbox gate just merged (agents/sandbox/Dockerfile, the _sanitize_cmd
command allowlist in agents/team-run.sh, and docker provisioning in
agents/setup-team2-box.sh) and is undocumented. It applies to open-model teams
(team2/team3) only; team1 is unaffected.

SCOPE (grotap-platform repo, docs/ ONLY):
1. Add docs/05-agents/open-model-sandbox.md covering, factually from the repo (read
   agents/sandbox/Dockerfile, agents/team-run.sh, agents/setup-team2-box.sh — do NOT
   invent):
   - what the grotap-sandbox image contains and how it is built (setup-team2-box.sh);
   - when the sandbox is used (TEAM_SANDBOX env: default per team) and what runs inside
     it (verification gate: build/tsc/pytest/py_compile) vs on the host;
   - the command allowlist (_sanitize_cmd / CMD_ALLOWLIST / EXTRA_VERIFY_CMDS) and what
     happens when a command is rejected.
2. Add one line linking the new page from the docs/05-agents index page (or
   docs/00-INDEX.md if 05-agents has no index of its own).

ACCEPTANCE: new page + one index line; every fact traceable to the three source files;
no changes outside docs/.


RE-RUN NOTE (2026-07-12, attempt 2): previous run exhausted the model ladder without a passing commit (rung 1 grok-4.5 is region-blocked from this box — expect it to fail fast; the real attempts start at rung 2). Keep the page SHORT and strictly factual from the three source files; produce at least one commit.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260712-071D17" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
