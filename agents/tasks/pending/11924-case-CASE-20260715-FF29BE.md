---
id: "11924"
title: "Bug: Platform — TEAM2-PILOT-5: fix horizontal scroll at 375px mobile viewport on /claude-code  C"
complexity: medium
priority: high
branch: "case-CASE-20260715-FF29BE"
case_id: "CASE-20260715-FF29BE"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
---

# Task: Bug: Platform — TEAM2-PILOT-5: fix horizontal scroll at 375px mobile viewport on /claude-code  C

## Context
No additional context provided.

## Requirements
TEAM2-PILOT-5: fix horizontal scroll at 375px mobile viewport on /claude-code

CONTEXT: the Playwright spec frontend/e2e/claude-code-live.spec.ts test
"no horizontal scroll at 375px mobile viewport" FAILS against prod
(observed 2026-07-15): the /claude-code seat page overflows horizontally at
a 375px viewport. Every other test in that spec passes, so this is a CSS
containment defect on the seat page, not an auth/data problem.

TASK (frontend only, smallest possible diff):
1. Find the overflowing element(s) on the /claude-code page (seat card /
   config table / copy-command block are the likely culprits) and contain
   them at narrow widths — e.g. min-width: 0 on flex children, max-width:
   100% / overflow-x: auto on the code or table block. Match the repo's
   existing responsive patterns; do not redesign the page.
2. Verify before committing: from frontend/, the spec's 375px test passes:
   npx playwright test e2e/claude-code-live.spec.ts -g "375px"
   (run under doppler per repo convention), and `npx tsc --noEmit` is clean.

GUARDRAILS:
- Touch ONLY the component/style files needed for the /claude-code page fix.
- No dependency changes, no visual redesign, no changes to other pages.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260715-FF29BE" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
