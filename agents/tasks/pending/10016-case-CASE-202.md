---
id: "10016"
title: "Bug: Platform — [Loop Engine] Bundle framework + `gmail-cleanup` executor — the 15 cleanup steps"
complexity: complex
priority: high
branch: "case-CASE-202"
case_id: "CASE-20260619-D942FD"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [Loop Engine] Bundle framework + `gmail-cleanup` executor — the 15 cleanup steps

## Context
No additional context provided.

## Requirements
[Loop Engine] Bundle framework + `gmail-cleanup` executor — the 15 cleanup steps (spec §6, §7).

DEPENDS ON case A (schema). Develops against case B's gmail_provider.ts interface — if not merged, code to the documented signatures (spec §7) and rebase.

CONTEXT: TypeScript (Rule 2), in the Inngest worker. A bundle = reusable logic; the SAME `gmail-cleanup` code serves EVERY account — all account specifics (gmail_account, label IDs, sweep window) come from the loop's `config` JSONB (spec §7 config block, §13.1). Canonical rules: platform/migrations/apps/loop-engine/seeds/gmail-filters.xml (17 filters) — keep the executor in sync with it.

DELIVERABLES:
1) A Bundle interface (slug, version, config schema, run(ctx, config)->summary) + a registry keyed by bundle_slug + a runner. ctx provides the resolved connected account + a GmailProvider + logger.
2) bundles/gmail-cleanup implementing spec §7 steps 1-15 IN ORDER: Home Depot/UPS/promos/cold-sales -> TRASH; SignWell/QuickBooks(SIS vs vendor)/Intuit/vendor-receipts/insurance/DocuSign -> label+archive; Venmo; PDX Parent; Squarespace form subs (KEEP in inbox, spam subs TRASH); Horse Camp (subject:camp + Reolink); Kids Lessons (CONTENT-classify, exclude adult); Staffing (Craigslist/applicants); notification-noise -> Z Email Notices All; and the 90-DAY SWEEP that MUST check each thread's NEWEST message date (skip threads with any recent reply). Content-based steps (Kids Lessons, staffing nuance) use the Anthropic provider — via the existing wrapper only.
3) Return the spec §7 run-summary shape (trashed/vendor/staffing/kids_lessons/horse_camp/notices/archived_90d/pdx_parent/venmo/skipped_active). Include the honest note that Gmail API cannot truly unsubscribe (trash, not unsubscribe).

ACCEPTANCE: worker typecheck passes; gmail-cleanup runs end-to-end against a MOCKED GmailProvider producing a correct summary; ZERO hardcoded account/label values (all from config); a 2nd Gmail account cleanup needs only a new loop+config, no code change. RELEASE PATH: branch off `master` and target `master` (app-only fast path, NO staging — owner directive). Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. ALL secrets in Doppler (grotap/prd) — never touch GitHub secrets. AUTHORITATIVE SPEC: read docs/12-app-platform/loop-engine.md (grotap-agents repo) BEFORE starting — it has the data model, API, Inngest funcs, the Gmail bundle steps, and the §13 platform-build additions (generic `gmail-cleanup` slug, per-account OAuth, connected_accounts). Obey the 8 ABSOLUTE RULES in CLAUDE.md (Doppler, vendor wrapper, pooled tenancy + FORCE RLS scoping, AppShell+Cobrowse, 4-reviewer). Inngest functions live in the TypeScript worker (Rule 2: agents=TS/JS); Python only fires events via providers/inngest_provider.py.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260619-D942FD" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
