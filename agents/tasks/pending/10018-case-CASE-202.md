---
id: "10018"
title: "Bug: Platform — [ScanTap] Locations screen — backend CRUD + reconcile (PostGIS) + frontend grid/"
complexity: complex
priority: high
branch: "case-CASE-20260620-B1BA6F"
case_id: "CASE-20260620-B1BA6F"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
---

# Task: Bug: Platform — [ScanTap] Locations screen — backend CRUD + reconcile (PostGIS) + frontend grid/

## Context
No additional context provided.

## Requirements
[ScanTap] Locations screen — backend CRUD + reconcile (PostGIS) + frontend grid/map. SUPERSEDES the locations/geo bits of cases A & B.

CRITICAL COORDINATION (read first): the Locations table ALREADY EXISTS and is the source of truth — `rfid_pipe.locations` in migrations/apps/rfid-pipe/v002_locations.sql (already merged + registered in apps.migrations). Columns: business_name, location_name (=SBI strWarehouseID), description, zone (="Zone Name"=strDivision), site (="Site Name"=strSite), location_type, department, address/city/state/zip/county/country, account_cogs, account_sales, directions, notes, geo_points JSONB (<=25 editable vertices = the "GeoLocation Point 1..25" columns), boundary geography(Polygon,4326) DERIVED from geo_points by trigger locations_sync_boundary (GiST-indexed), sort_order, is_active.
- DO NOT create a `scantap_locations` table. DO NOT use Python haversine. Use PostGIS.
- If case A's v002_scantap.sql tries to add scantap_locations, drop that piece and use rfid_pipe.locations instead. Coordinate with the case A/B branches.

BACKEND (extend rfid_pipe.py, tenant-scoped via _get_tenant, audit-logged):
1) CRUD /rfid/locations: GET (list with filters zone/site/is_active + search), GET/{id}, POST, PATCH/{id}, DELETE/{id}. geo_points is the editable vertex list (<=25, the table CHECK enforces it); boundary is maintained by the trigger — never set it directly.
2) Excel/CSV export of the locations grid (reuse the CSV Blob pattern in RfidPipeScanDetailPage).
3) POST /rfid/scans/{session_id}/reconcile-locations: for each scan record that has geo_lat/geo_lng (columns added by case A's ALTER on rfid_scan_records — coordinate), set record.location_id by PostGIS: first ST_Contains(boundary, point); if none, nearest within a radius via ST_DWithin(boundary, point, :meters). Set a `needs_confirmation` boolean when the match is ambiguous (GPS is only ~5 m accurate — two fields <~10 m apart cannot be auto-disambiguated; flag, don't guess). Return per-record assignments + a summary.

FRONTEND (Locations page, route /rfid-pipe/locations — add to nav in appMenuConfig + App.tsx + RfidPipeAppSidebar; "ScanTap" branding):
4) Grid (Mantine React Table conventions): columns Location Name, Zone Name, Site Name, GeoLocation Point 1 .. GeoLocation Point 25 (rendered from geo_points[i] as "lat,lng" or blank), plus collapsible detail columns (Type, Department, Address, City, State, Zip, Cost Account, Sales Account, Directions, Notes, Active). Add / Edit / Delete. Excel/CSV export.
5) Edit drawer with a MAP to capture/drag up to 25 boundary points per location (writes geo_points; boundary auto-derives). Prefer Leaflet + OSM tiles (no API key); if a key is needed, read it from env, never hardcode. Show a realistic-accuracy note (~5 m) and let the user confirm/override an ambiguous reconcile.

ACCEPTANCE: tsc --noEmit + py_compile pass; the Locations page lists rows from rfid_pipe.locations; create/edit writes geo_points and the boundary polygon materializes (verify with ST_AsText); reconcile assigns location_id and flags ambiguous ones; MRT rules respected (no isLoading-with-data; nullable accessorFn uses `|| []`); dark design system. RELEASE PATH: app-scoped to rfid-pipe (its own pages + router) — fast path to master is OK per the staging-routing rule, since blast radius is the ScanTap app alone.

PRE-READ before starting: C:\1Claude\ScanTap-ARCHITECTURE-RECOMMENDATIONS.md (what we have / what's best / what to do) and C:\1Claude\ScanTap-BUILD-PLAN.md. Run `tsc --noEmit` (frontend) and `python -m py_compile` (backend) before pushing. All secrets are in Doppler — never touch GitHub secrets. Through-vendor-wrapper rule: no 3rd-party calls outside providers/. asyncpg: json.dumps() for JSONB, never .get() on Records.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260620-B1BA6F" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
