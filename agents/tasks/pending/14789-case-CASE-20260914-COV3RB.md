---
id: "14789"
title: "Bug: Platform — The COV-3 branch (case-CASE-20260914-0464C6) introduces a new class-based Covera"
complexity: medium
priority: high
branch: "case-CASE-20260914-COV3RB"
case_id: "CASE-20260914-COV3RB"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "96b63c53-60cc-4c23-bf65-15c290cfa31a"
team: "team3"
---

# Task: Bug: Platform — The COV-3 branch (case-CASE-20260914-0464C6) introduces a new class-based Covera

## Context
No additional context provided.

## Requirements
The COV-3 branch (case-CASE-20260914-0464C6) introduces a new class-based CoverageEngine in platform/mobile/scantap/services/coverage.ts (573 lines) that conflicts with a prior partial COV-3 agent run already on master (274 lines, pure-function API). The prior version is imported by coverage.test.ts with old types (EARTH_RADIUS_M, GpsPoint, Edge, Side). Fix: on a rebase of CASE-20260914-0464C6, replace coverage.ts with the new CoverageEngine version (exports: computeBearing, snapToEdge, determineTravelSide, computeLocationState, CoverageEngine class, loadCachedEdges, loadCoverageSpec) and update coverage.test.ts to test the new API. The geofence.ts helpers, database.ts tables, readerConfig.ts, and scan.tsx changes are already on master; only coverage.ts and its test file need reconciling. IMPORTANT: the existing coverage.test.ts tests the old pure-function API and must be rewritten or replaced to test the new CoverageEngine.

## Clarifications
- Q: Should the new CoverageEngine class maintain backward compatibility with any of the old pure-function exports (computeBearing, snapToEdge, etc.), or can they be fully replaced as standalone functions alongside the class?
  A: Keep ALL the old pure-function exports alongside the new CoverageEngine class. Specifically: computeBearing, snapToEdge, buildEdgeToSides, computeCoverage, summariseCoverage, and types Edge/EdgeToSides/GpsPoint/LocRings/Side must remain exported from coverage.ts. platform/mobile/scantap/services/__tests__/coverage.test.ts imports them directly (verified 2026-09-14) and must not break. The Side type 'a'|'b' is the COV-1 server-mirror contract (edge ordering by edge_id); the 'left'|'right' labels in CoverageEngine are for the GPS-snap direction engine. Both must coexist in the module. Approach: keep the pure-function block intact at the top of coverage.ts, then add the class-based CoverageEngine below it.
- Q: Are there any other files beyond geofence.ts, database.ts, readerConfig.ts, and scan.tsx that import from coverage.ts and would need updates after this change?
  A: Verified (2026-09-14) via grep of platform/mobile/scantap/: the only files that import from '../coverage' or '../../services/coverage' are: (1) platform/mobile/scantap/services/__tests__/coverage.test.ts — imports pure-function API (Edge, EdgeToSides, GpsPoint, LocRings, Side, buildEdgeToSides, computeCoverage, summariseCoverage). Keep these exports; this file does NOT need modification. (2) platform/mobile/scantap/app/(tabs)/scan.tsx — in the branch: imports CoverageEngine, loadCoverageSpec, loadCachedEdges. These are new exports that the rebase must add. No other mobile files import from coverage.ts.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260914-COV3RB" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
