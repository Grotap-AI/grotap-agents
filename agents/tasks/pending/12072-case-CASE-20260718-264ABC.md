---
id: "12072"
title: "Chore: Platform — TEAM5-PILOT-2 — append a "Verifying a task ran on the codex path" section to doc"
complexity: medium
priority: high
branch: "case-CASE-20260718-264ABC"
case_id: "CASE-20260718-264ABC"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team5"
---

# Task: Chore: Platform — TEAM5-PILOT-2 — append a "Verifying a task ran on the codex path" section to doc

## Context
No additional context provided.

## Requirements
TEAM5-PILOT-2 — append a "Verifying a task ran on the codex path" section to docs/05-agents/codex-runner.md.

CONTEXT: dispatch.sh originally gated BOTH the pool-routing block AND the runner-script write
on the literal string "agents/team-run.sh", so team5 (codex-run.sh) tasks silently fell
through to the team1 Claude runner (fixed by case CASE-20260717-22A75E via the
_is_team_runner_script wildcard matcher). codex-runner.md does not tell an operator how to
verify a team5 task actually ran on the codex path rather than silently falling through.

SCOPE (grotap-platform repo, docs/05-agents/codex-runner.md ONLY — pure append at end of
file, existing lines byte-identical):
Add one short section titled "Verifying a task ran on the codex path" stating, factually
from the repo (read agents/dispatch.sh — do NOT invent):
- dispatch.sh resolves the runtime via _is_team_runner_script, which matches runtime scripts
  ending in team-run.sh OR codex-run.sh (wildcard suffix match, not a literal comparison);
- a real team5 run logs "Team routing: team5" in the dispatch output and writes rows with
  service='fleet-team5' to llm_usage_log;
- a task whose telemetry shows service='fleet' (Claude runner) means team routing was
  bypassed and the run must not be counted as a team5 result.

ACCEPTANCE: exactly one appended section in codex-runner.md; every fact traceable to
agents/dispatch.sh; no other files changed.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260718-264ABC" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
