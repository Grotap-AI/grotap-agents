---
id: "13960"
title: "Bug: Agent Pipeline — List Option Narration Fails  if I do the list option on a request and then I do"
complexity: medium
priority: normal
branch: "case-6678d3b0-de35-4cff-84a0-289793c12677"
case_id: "6678d3b0-de35-4cff-84a0-289793c12677"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "62106438-4fe5-466e-817c-5234d2db9ed2"
team: "team4"
---

# Task: Bug: Agent Pipeline — List Option Narration Fails  if I do the list option on a request and then I do

## Context
List Option Narration Fails

if I do the list option on a request and then I do a second narration that narration never makes it to the case there's a bug there please review and fix

so if user clicks, click to submit a list of changes
Then the user does a 2nd narration
that narration never saves on the case, it has a bug
research and resolve

[Narration] if I do the list option on a request and then I do a second narration that narration never makes it to the case there's a bug there please review and fix
App: Agent Pipeline (agent-pipeline)
Area: Agent Pipeline
Source: feedback_toolkit
Component: Agent Pipeline

## Requirements
[agent-pipeline] List Option Narration Fails

if I do the list option on a request and then I do a second narration that narration never makes it to the case there's a bug there please review and fix

so if user clicks, click to submit a list of changes
Then the user does a 2nd narration
that narration never saves on the case, it has a bug
research and resolve

[Narration] if I do the list option on a request and then I do a second narration that narration never makes it to the case there's a bug there please review and fix

App: Agent Pipeline (agent-pipeline)
Brand: Grotap Apps
Area of app: Agent Pipeline
Route: /pipeline
Screen: Agent Pipeline
Session replay attached (rrweb, R2) (17003 ms): issue/0336e35d-1c29-4c77-b029-6453a8785d0c/replay.json.gz
Narration audio attached (R2): issue/0336e35d-1c29-4c77-b029-6453a8785d0c/narration.webm
Submitted by: info@grotap.com via feedback toolkit

## User Narration
if I do the list option on a request and then I do a second narration that narration never makes it to the case there's a bug there please review and fix

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "6678d3b0-de35-4cff-84a0-289793c12677" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
