---
id: "12659"
title: "Bug: Platform — Fix Test 5 (CallsPage assign menu) in frontend/e2e/calls-page-behavior-parity.sp"
complexity: medium
priority: high
branch: "case-CASE-20260717-B27A61"
case_id: "CASE-20260717-B27A61"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
---

# Task: Bug: Platform — Fix Test 5 (CallsPage assign menu) in frontend/e2e/calls-page-behavior-parity.sp

## Context
No additional context provided.

## Requirements
Fix Test 5 (CallsPage assign menu) in frontend/e2e/calls-page-behavior-parity.spec.ts so it passes against live CallsPage.tsx. Two parity gaps: (1) live CallsPage PATCHes `/voice/leads/${lead_submission_id}/assign` and disables the assign <select> when `!call.lead_submission_id`, but the test's CALLBACK_CALL seed has no lead_submission_id, so the select renders disabled and selectOption never fires. Add `lead_submission_id` (e.g. a seeded UUID) to CALLBACK_CALL. (2) The page.route stub targets `**/voice/leads/${CALL_SID}/assign` but live uses lead_submission_id in the path — retarget the route to the seeded lead_submission_id. Verify with RUN_CALLS_PARITY=1 npx playwright test e2e/calls-page-behavior-parity.spec.ts (Test 5 only). Tests 1-4 already pass. File: frontend/e2e/calls-page-behavior-parity.spec.ts (CALLBACK_CALL const ~line 132; assign route ~line 480; live ref frontend/src/pages/CallsPage.tsx:150,196).

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260717-B27A61" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
