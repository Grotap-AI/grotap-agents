---
id: "11481"
title: "Chore: naming/cobrowse-rename — [ORC2] CHORE: Pay down the Cobrowse->OpenReplay naming debt at the CODE level."
complexity: medium
priority: normal
branch: "case-CASE-20260710-3E9B12"
case_id: "CASE-20260710-3E9B12"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team2"
---

# Task: Chore: naming/cobrowse-rename — [ORC2] CHORE: Pay down the Cobrowse->OpenReplay naming debt at the CODE level.

## Context
Source: owner-approved-matrix-review
Component: naming/cobrowse-rename

## Requirements
[ORC2] CHORE: Pay down the Cobrowse->OpenReplay naming debt at the CODE level.

DEPENDS ON: CASE-20260710-4FA2C1 and CASE-20260710-9D3B7E (Assist feature work) merging first, and CASE-20260710-D27F63 (backend cleanup) - this case renames what remains, to avoid churning files those cases are editing. Dispatch LAST of this group.

CONTEXT: The vendor product Cobrowse.io is gone; we run self-hosted OpenReplay. Frontend already migrated to the lib/openreplay.ts wrapper, but Cobrowse-prefixed identifiers remain (components like CobrowseConsole, CSS class .cb-mask conventions, cobrowse-agent-runner sources, backend naming, e2e screenshot names). This is comprehension debt for new agents/devs.

SCOPE:
1. Inventory every Cobrowse*/cobrowse* identifier in main tree (frontend/, backend/, cobrowse-agent-runner/src/, agent-worker/).
2. Rename CODE-LEVEL identifiers (components, classes, functions, types, file names) to OpenReplay/Assist naming. Keep .cb-mask CSS selector working: if renamed, keep a compat alias so existing masked elements stay masked - masking must NEVER regress.
3. Do NOT rename infra/ops names in this case: the cobrowse-agent-runner/ directory name, the cobrowse-runner systemd unit on grotap-runner-01, R2 buckets, Doppler keys, Railway services. Instead output an OPS CHECKLIST of those remaining renames with exact commands for owner review.
4. Update docs references.

CONSTRAINTS: purely mechanical - zero behavior change; tsc + py_compile + runner tsc must pass. Grep-verify no stale references after rename. Small commits per area so review is tractable.

TOUCH SURFACE: repo-wide mechanical rename (frontend, backend, cobrowse-agent-runner, docs).

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260710-3E9B12" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
