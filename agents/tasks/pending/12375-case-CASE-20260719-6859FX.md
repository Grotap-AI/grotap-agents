---
id: "12375"
title: "Bug: Platform — FIX for CASE-20260719-6859BA (agentquote work_type slug alignment). Branch was S"
complexity: medium
priority: high
branch: "case-CASE-20260719-6859FX"
case_id: "CASE-20260719-6859FX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
team: "team4"
---

# Task: Bug: Platform — FIX for CASE-20260719-6859BA (agentquote work_type slug alignment). Branch was S

## Context
No additional context provided.

## Requirements
FIX for CASE-20260719-6859BA (agentquote work_type slug alignment). Branch was SKIPPED by the review gate as incomplete. The backend canonical work_type slugs are HYPHENATED: backend/app/routers/quotes.py:606 checks body.work_type == 'software-development' (forbids 'shared' package), and backend/app/routers/product_catalog.py uses 'software-development'/'business-automation'/'marketing-automation'. But master's marketing-pages/agentquote.html sends SHORT slugs ('software'/'business'/'marketing'), so the backend "software forbids shared" rule NEVER fires. The 6859BA branch fixed the tile data-val + the isSoftware comparison to hyphenated slugs, but LEFT the WORK_TYPE_LABELS map keyed on the old short slugs, so the Step-5 review screen would render the raw slug 'software-development' instead of the friendly label 'Software Development' (a customer-facing regression) — that is why it was not merged.

FIX SCOPE (marketing-pages/agentquote.html ONLY):
1. Tile data-val for the 3 work-type tiles -> 'software-development' / 'business-automation' / 'marketing-automation'.
2. isSoftware check -> answers.worktype === 'software-development'.
3. WORK_TYPE_LABELS keys -> 'software-development' / 'business-automation' / 'marketing-automation' (mapping to 'Software Development' / 'Business Automation' / 'Marketing Automation'). THIS is the missing piece.
4. VERIFY: selecting each tile shows the friendly label on the Step-5 review card; payload work_type sends the hyphenated slug; picking Software Development still disables/auto-switches away from the Shared package.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-6859FX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
