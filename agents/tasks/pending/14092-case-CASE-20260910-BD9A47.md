---
id: "14092"
title: "Bug: Platform — ERPC test-side follow-up: a whole test class is red on master, and one connectio"
complexity: medium
priority: high
branch: "case-CASE-20260910-BD9A47"
case_id: "CASE-20260910-BD9A47"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "bf39be2c-820d-4e7f-bb41-e670f2848d9b"
team: "team4"
---

# Task: Bug: Platform — ERPC test-side follow-up: a whole test class is red on master, and one connectio

## Context
No additional context provided.

## Requirements
ERPC test-side follow-up: a whole test class is red on master, and one connection-test branch has no coverage.

SURFACE: ScanTap (priority surface #1) — ERP Connector. Both defects are TEST-SIDE; production
behaviour in scantap.py is correct and unchanged. Found by the review gate on 2026-09-10 while
reviewing CASE-20260910-RGB3F1 (merged, 9225a03c). Neither defect was introduced by RGB3F1 — both
were measured on origin/master BEFORE that merge, in a clean worktree.
complexity: simple
touch_surface: scantap-erp-connector

DEFECT 1 — every test in backend/tests/test_scantap_bridge.py::BridgePushStatTests fails on master.
MEASURED on origin/master @3d85655f (clean worktree, no local changes):
    python3 -m pytest backend/tests/test_scantap_bridge.py -q  ->  7 failed, 16 passed
Failure (identical in all 7):
    ValueError: not enough values to unpack (expected 4, got 2)
    at app/routers/scantap.py:3606, `tenant_id, pool, provider, connector_id = await _resolve_bridge_auth(request)`
CAUSE: CASE-20260910-DC5B7F (commit 44db7664, ERPC-3) widened `_resolve_bridge_auth` from a
2-tuple to a 4-tuple `(tenant_id, pool, provider, connector_id_or_none)` (scantap.py:3522-3526)
and updated its own new tests only. The `_push` helper at backend/tests/test_scantap_bridge.py:160
still patches it with `AsyncMock(return_value=(tenant_id, mock_pool))` — a 2-tuple — so bridge_push
raises before it reaches any assertion. All 7 stat/cap tests (rows_received, per-batch stats
accumulation, empty push, 413 on 1001 rows) have been dead since 2026-09-10.
FIX: in backend/tests/test_scantap_bridge.py::BridgePushStatTests._push, return the 4-tuple the
current function returns — `(tenant_id, mock_pool, "sage_bridge", None)` (pick the provider string
the endpoint actually branches on; grep bridge_push for its use of `provider` and `connector_id`
first — if either value now steers behaviour, add a second fixture for the erpc_ / connector_id
path rather than hardcoding one shape). Do NOT change scantap.py to fit the mock.
ACCEPTANCE: `python3 -m pytest backend/tests/test_scantap_bridge.py -q` -> 23 passed, 0 failed;
quote the run in the PR description. Then prove the tests are not tautologies: break one stats key
in bridge_push (e.g. rename the `inventory_items` stats key), show the suite reddens, restore it.

DEFECT 2 — the revoked-connector branch of the sbi_onprem connection test has no coverage.
backend/app/routers/scantap.py:3320 `elif connector_row["revoked_at"] is not None:` ->
`("error", "Connector has been revoked")`. Added by CASE-20260910-... (commit 57ac615d, ERPC-6).
PROVEN BY MUTATION (review gate, 2026-09-10, on the merged tree): deleting that entire elif branch
from scantap.py leaves `pytest backend/tests/test_scantap_bridge_lifecycle.py -q` at 45 passed.
A revoked connector would silently report `online`/`pending` and no test would notice.
CAUSE: RGB3F1 covered the five branches its spec enumerated; the spec was written against the
pre-ERPC-6 handler, which had five. The handler has six.
FIX: add one test to backend/tests/test_scantap_bridge_lifecycle.py::SbiOnpremConnTestTests —
fixture `{"last_seen_at": <fresh tz-aware>, "last_push_at": <tz-aware>, "revoked_at": <tz-aware>}`
asserting `status == "error"` and "revoked" in the message. Use timezone-AWARE datetimes
(erp_connectors.last_seen_at/revoked_at are TIMESTAMPTZ and the handler subtracts
datetime.now(timezone.utc)); revoked must be checked with a FRESH last_seen_at so the test proves
precedence over the freshness branches, not just the null path.
ACCEPTANCE: the deletion mutation above reddens the new test; quote the reddened run and restore
the file. 46 tests in that file after this case (45 today).

DO NOT: change any production behaviour in scantap.py or scantap_sync.py. No new migrations.
No new test files — extend the two that exist.

DONE WHEN: test_scantap_bridge.py is 23/23 green, test_scantap_bridge_lifecycle.py is 46/46 green,
and both mutations above are shown reddening the suite in the PR description.

REVIEW GATE 2026-09-10 17:35 UTC (grotap-agent-06) — ATTEMPT 1 (e173b1ca) REJECTED, not merged.
Tagged rejected/case-CASE-20260910-BD9A47-attempt1; origin/case-CASE-20260910-BD9A47 DELETED so
the queued retry (dispatch 83c27857, pending since 17:20:11) can push — dispatch.sh cuts the
branch fresh from origin/master and pushes without --force.

ALREADY DONE — DO NOT REDO:
* DEFECT 1 was fixed ON MASTER by a peer BEFORE this case was dispatched: commit d3697c10
  "test(scantap): bridge auth mocks return the 4-tuple the erpc-3 routes unpack" (2026-09-10
  16:14 UTC; this case dispatched 16:56). Measured by the gate on current origin/master:
  `python3 -m pytest backend/tests/test_scantap_bridge.py -q` -> 23 passed, 0 failed. Nothing is
  owed for defect 1. Attempt 1's only edit there retyped the mock provider "sage" -> "sage_bridge"
  — DROP that edit: _resolve_bridge_auth's provider is only forwarded to _write_sync_log, and
  bridge_push's connector-touch UPDATE hardcodes provider = 'sage' (scantap.py:3697), so "sage"
  is the truthful fixture.
* DEFECT 2 IS CORRECTLY DELIVERED and is the only thing worth keeping from attempt 1:
  test_revoked_connector_takes_precedence_over_freshness in
  backend/tests/test_scantap_bridge_lifecycle.py (fresh last_seen_at + tz-aware revoked_at,
  asserts status == "error" and "revoked" in the message). Reproduce it as-is.

WHY REJECTED: attempt 1 also rewrote ~120 lines the case told it not to touch. Apart from that
one new test, the entire test_scantap_bridge_lifecycle.py diff is DELETION of test documentation
and diagnostics — the FreshnessColumnTests and ErpConnectorStatusColumnCoverageTest class
docstrings, most per-test docstrings, the inline "# skipped" / "# in_stock summed" markers, and
(the part that matters) the message argument of ~14 assertions, e.g.
  assertIn("updated_at", conflict, "inventory ON CONFLICT DO UPDATE SET must include updated_at")
becomes
  assertIn("updated_at", conflict).
Those messages are how the next red run gets diagnosed. The suite is green either way (69 passed
on the branch) — greenness was never the question; scope was. The case said "extend the two that
exist" and authorized no reformatting.

FIX SCOPE for the retry: branch from CURRENT origin/master (it has moved: 0c12ffe9). Add ONLY the
revoked-precedence test — that file goes 45 -> 46 tests — and make NO other edit to either test
file: no comment removal, no assert-message removal, no provider rename. Then run the mutation
proof the case asks for: delete the `elif connector_row["revoked_at"] is not None:` branch at
backend/app/routers/scantap.py:3320, show the new test reddens, restore the file. Acceptance:
test_scantap_bridge.py 23/23, test_scantap_bridge_lifecycle.py 46/46, and
`git diff origin/master --stat` shows exactly ONE file changed, additions only.


## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260910-BD9A47" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
