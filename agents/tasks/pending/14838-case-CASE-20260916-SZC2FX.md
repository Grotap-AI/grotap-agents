---
id: "14838"
title: "Bug: Platform — database.ts (SZC-2, CASE-20260914-A26C01) exports updateBatchZoneDropped(batchId"
complexity: medium
priority: high
branch: "case-CASE-20260916-SZC2FX"
case_id: "CASE-20260916-SZC2FX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "85476570-fa49-48fc-b047-aa02e961d3a7"
team: "team2"
---

# Task: Bug: Platform — database.ts (SZC-2, CASE-20260914-A26C01) exports updateBatchZoneDropped(batchId

## Context
No additional context provided.

## Requirements
database.ts (SZC-2, CASE-20260914-A26C01) exports updateBatchZoneDropped(batchId, count) which persists the zone-dropped RSSI-gate counter. But scan.tsx (the batch-close flow) is unchanged so it never calls updateBatchZoneDropped. zone_dropped is always 0 in the DB, so syncQueue.ts uploads zone_dropped:0 even when tags were filtered. The gate works (CaptureEngine.zoneDroppedCount increments); only persistence is missing.

Fix: In platform/mobile/scantap/app/(tabs)/scan.tsx, at batch-close (same call site as updateBatchCoverageSummary), call updateBatchZoneDropped(batchId, engine.getZoneDropped()). Import updateBatchZoneDropped from ../db/database. Extend test/syncResume.test.ts to assert zone_dropped is written at close.

Files: platform/mobile/scantap/app/(tabs)/scan.tsx, platform/mobile/scantap/test/syncResume.test.ts

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260916-SZC2FX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
