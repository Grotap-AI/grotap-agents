---
id: "12684"
title: "Bug: backend/app/main.py — Complete the boot-abort removal that case CASE-20260719-B6130E only half-did. Th"
complexity: medium
priority: high
branch: "case-CASE-20260719-B613FX"
case_id: "CASE-20260719-B613FX"
callback_url: "https://api.grotap.com/pipeline/webhook/agent-progress"
dispatch_id: "953f6eb9-9b3d-445e-9d9a-4a507bed3cb2"
team: "team5"
---

# Task: Bug: backend/app/main.py — Complete the boot-abort removal that case CASE-20260719-B6130E only half-did. Th

## Context
Component: backend/app/main.py

## Requirements
Complete the boot-abort removal that case CASE-20260719-B6130E only half-did. That branch ADDED a regression test (backend/tests/test_main_lifespan_no_bootabort.py) asserting the main.py lifespan does NOT import/reference LOADTEST_TEST_ORG_IDS, but it never performed the removal — so the test FAILS against current master. main.py lifespan (backend/app/main.py, ~lines 133-138) still contains `from app.services.strike_engine import LOADTEST_TEST_ORG_IDS` plus an informational logger.info of its count.

Root cause context: the actual P0 boot-abort was already fixed at source — strike_engine.py now WARNS (not raises) when the env var is unset — so this main.py import is harmless informational logging, but the merged guard test forbids ANY reference to the symbol inside lifespan and therefore reddens the suite.

Fix scope (pick the removal, matching the case's stated intent): delete the `from app.services.strike_engine import LOADTEST_TEST_ORG_IDS` line and its `logger.info("LOADTEST_TEST_ORG_IDS: %d org(s)...", len(LOADTEST_TEST_ORG_IDS))` block from the lifespan in backend/app/main.py. Do NOT touch strike_engine.py (its warn-not-raise fix is already live on master). Verify: `cd backend && python3 -m unittest tests.test_main_lifespan_no_bootabort -v` passes, and `python3 -m compileall -q backend/app`.
Files: backend/app/main.py.

## Acceptance Criteria
- [ ] Changes address the reported issue
- [ ] All existing tests pass (tsc --noEmit, py_compile)
- [ ] Branch pushed and ready for review

## Progress Reporting
When available, report progress by running:
```bash
bash ~/grotap-agents/agents/scripts/report-progress.sh "CASE-20260719-B613FX" "<status>" "<message>"
```
Call this at each stage:
- "executing" — when you start building
- "change_review" — after pushing code, before review
- "done" — when task is fully complete
- "failed" — if you encounter an unrecoverable error
